[package]
name = "keal"
version = "0.1.0"
edition = "2021"
description = "Keal — a statically typed scripting language"

[[bin]]
name = "keal"
path = "src/main.rs"

[dependencies]

[profile.release]
opt-level = 3
lto = true
