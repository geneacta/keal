/* Generated by `keal emit-header tests/native-extern/boundary.keal`. Do not edit.
 * The C face of the program's boundary: include this from a companion
 * .c file compiled with `keal build tests/native-extern/boundary.keal companion.c`.
 */
#ifndef KEAL_BOUNDARY_H
#define KEAL_BOUNDARY_H

#include <stdbool.h>
#include <stdint.h>

/* Loading this program as a library rather than running it: `keal_runtime_init`
 * prepares the runtime, and `keal_program_run` runs the program's top level —
 * which is where every top-level `val` and `var` is set, so before it a global
 * is a null pointer. A host that wants them runs the program.
 */
void keal_runtime_init(void);
int keal_program_run(void);

#ifndef KEAL_MIRROR_Vec2
#define KEAL_MIRROR_Vec2
typedef struct Keal_Vec2 {
    double x;
    double y;
} Keal_Vec2;
#endif

int64_t k_bonus(int64_t n);

#endif /* KEAL_BOUNDARY_H */
