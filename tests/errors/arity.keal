fun add(a: Int, b: Int): Int { a + b }
fun greet(name: String, greeting: String = "hi"): String { "${greeting} ${name}" }

class Point(val x: Int, val y: Int)

val tooFew = add(1)
val tooMany = add(1, 2, 3)
val badName = greet(nmae = "Ada")
val twice = add(1, a = 2)
val outOfOrder = greet(greeting = "yo", "Ada")
val badCtor = Point(1)
val listArity = [1, 2].join(", ", "extra")
