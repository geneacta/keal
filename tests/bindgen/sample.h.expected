// Generated by `keal bindgen tests/bindgen/sample.h`. Review before trusting: only what
// crosses the boundary exactly is bound; anything else is skipped
// below, with the reason. The header itself still has to be visible
// to the C compiler — pass `-I...` to `keal build` if it is not.

native "#include \"tests/bindgen/sample.h\""

public extern func add64(a: Int, b: Int): Int

public extern func triple(n: Int): Int

public extern func scale(x: Float, factor: Float): Float

public extern func flag_of(n: Int): Bool

public extern func count_vowels(text: borrow String): Int

public extern func shout(text: borrow String): own String

public extern func reset(): Unit

public extern func tick(): Unit

public extern func vec2_dot(a: Vec2, b: Vec2): Float

public extern func vec2_scale(v: Vec2, k: Float): Vec2

public extern func unnamed_params(arg0: Int, arg1: Float): Int

// skipped plain_int: `int` is not exactly an `int64_t`; wrap it in a function that takes and returns int64_t

// skipped small_float: C `float` is 32-bit; take and return `double`

// skipped version: returns `const char*`, borrowed text Keal must not adopt; wrap it in a function returning a malloc'd copy

// skipped fill: takes `char*`, a mutable buffer Keal cannot promise; take `const char*` and return the result instead

// skipped sum_all: variadic functions do not cross

// skipped on_each: a function-pointer parameter needs the callback story (docs/interop.md, 1d)

// skipped hidden: static or inline, so there is no symbol to link

// skipped mask: `unsigned int` is not exactly an `int64_t`; wrap it in a function that takes and returns int64_t

public extern func guarded_add(a: Int, b: Int): Int
