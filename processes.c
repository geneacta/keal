/* Generated by the Keal compiler. Do not edit. */
/* The runtime the generated C is compiled against.
 *
 * This is the memory model of docs/memory.md made real, for the part of the
 * language the C backend covers: a reference count at the head of every heap
 * object, strings as counted immutable buffers, and checked integer
 * arithmetic so that native code fails where the other two engines fail
 * rather than quietly wrapping.
 *
 * It is emitted into the generated file rather than shipped as a library, so
 * that the output of `keal emit-c` is one self-contained translation unit a
 * C compiler can take without any flags. */

/* This file wants POSIX, and has to say so before the first include.
 *
 * `keal build` compiles with `-std=c11`, which defines `__STRICT_ANSI__`;
 * glibc then withdraws the declarations that are not in ISO C. It does not
 * withdraw all of them evenly — `opendir`, `fork`, `pipe` and `poll` stayed,
 * while `clock_gettime`, `CLOCK_REALTIME` and `localtime_r` vanished from
 * <time.h>, which is why this showed up as three names and not as a wall.
 * Apple's headers declare all of them regardless, so a program that compiled
 * here failed on the most ordinary machine there is.
 *
 * Not defined on Apple, where the same macro goes the other way and HIDES
 * the BSD extensions its headers assume. */
#if !defined(_WIN32) && !defined(__APPLE__)
#define _POSIX_C_SOURCE 200809L
#endif

#include <errno.h>
#include <inttypes.h>
#include <math.h>
#include <stdbool.h>
#include <stdint.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#include <time.h>

/* What the file-system primitives need, on both sides of the platform line.
 * Unconditional: a program reads a directory whether or not it has actors,
 * and `windows.h` guards itself against the copy the actor block includes.
 *
 * `WIN32_LEAN_AND_MEAN` leaves out the sockets, OLE, RPC, shell and
 * cryptography headers that `windows.h` otherwise drags in. Nothing here
 * touches any of them: the whole Windows surface this runtime uses is
 * `CreateThread`, `WaitForSingleObject`, `CloseHandle`, `GetSystemInfo`,
 * the `FindFirstFileW` family, `CreateFileW`, `CreateProcessW`, the
 * attribute and directory calls, and the two character conversions — all
 * of it kernel32, none of it excluded.
 *
 * Measured on Windows 10 with MinGW-W64 UCRT GCC 16.1: 505 headers and
 * 115,160 preprocessed lines become 278 and 79,865, and a translation unit
 * that costs 1,275 ms costs 953 — 25.3% off every file the backend emits,
 * from two concordant passes on an idle machine. The whole suite there went
 * from 449s to 369s, which reaches the same figure by a second route on a
 * workload that is not all C compilation. An empty `main` cost more than an
 * 8,639-line program, because the program is not what is being read. */
#ifdef _WIN32
#ifndef WIN32_LEAN_AND_MEAN
#define WIN32_LEAN_AND_MEAN
#endif
#include <windows.h>
#else
#include <dirent.h>
#include <errno.h>
#include <fcntl.h>
#include <poll.h>
#include <sys/stat.h>
#include <sys/wait.h>
#include <sys/types.h>
#include <unistd.h>
#endif

/* Windows opens stdout in text mode, and text mode turns every `\n` this
 * runtime writes into `\r\n`. The three engines must print the same bytes —
 * that is the invariant the whole test suite rests on — so the native one
 * asks for the bytes it actually wrote. A constructor rather than a call in
 * `main`, so that not one line of generated C changes.
 *
 * `__attribute__((constructor))` is the same GCC/Clang ground the overflow
 * builtins below already stand on. */
#ifdef _WIN32
#include <fcntl.h>
#include <io.h>
__attribute__((constructor)) static void keal_stdio_is_bytes(void) {
    _setmode(_fileno(stdout), _O_BINARY);
    _setmode(_fileno(stderr), _O_BINARY);
}
#else
#include <signal.h>
/* A write to a closed pipe must be an ERROR, not a death.
 *
 * `prog | head` closes the pipe after ten lines. By default the eleventh
 * write raises SIGPIPE and the kernel kills the process, so the program ends
 * at 141 having said nothing. The interpreters do not: Rust's runtime ignores
 * SIGPIPE, so the write returns EPIPE and the failure is reported the way
 * every other failure is — "cannot write to standard output: Broken pipe",
 * exit 1. The tree-walker is this language's specification, so the backend
 * follows it here rather than following Unix tradition on its own. */
__attribute__((constructor)) static void keal_pipe_is_an_error(void) {
    signal(SIGPIPE, SIG_IGN);
}
#endif

/* A generated program uses only the part of this runtime it needs, so the
 * rest must not draw warnings from a caller building with -Wall. */
#if defined(__GNUC__) || defined(__clang__)
#define KEAL_FN __attribute__((unused)) static
/* The same mark for the tables. `KEAL_FN` says FN, and these are data: a
 * program with no `Any` in it never reads the type-info rows, and `-Wall`
 * called them out by name — `unused variable 'keal_ti_list'` — in a
 * consumer's build. The promise above was written for the functions and the
 * data was left out of it. */
#define KEAL_VAL __attribute__((unused)) static
/* And the same for a variable the PROGRAM declared. Keal has no rule
 * against a binding nobody reads — `val boom = f()` for its effect, a
 * `catch (e)` whose handler ignores the message — so the C it emits should
 * not invent one. This says the compiler may keep quiet, not that the
 * variable is useless. */
#define KEAL_LOCAL __attribute__((unused))
#else
#define KEAL_FN static
#define KEAL_VAL static
#define KEAL_LOCAL
#endif

/* ---- what weak references change --------------------------------------- */

/* A `weak` field does not keep its target alive. So an object needs a
 * second count — how many weak references name it — and the backend
 * defines KEAL_WEAK above this file exactly when the program declares a
 * weak field; without it, objects carry one count as they always have.
 *
 * When the strong count reaches zero the object runs its `deinit` and
 * releases its fields, as ever. What changes is the last step: it frees
 * itself only if no weak reference remains. Otherwise it stays as a
 * husk — a header with `rc == 0`, which *is* the "dead" test a weak read
 * makes — until the last weak reference goes and frees it. That is why a
 * weak read is always a safe read: the memory it inspects cannot have
 * been returned while it still names it. */

/* ---- what actors change ----------------------------------------------- */

/* An actor program runs its actors on real OS threads (docs/threads.md);
 * the backend defines KEAL_ACTORS above this file exactly when the program
 * touches the actor types. Two things follow, and only then:
 *
 * * **Counts go atomic.** Addresses (`ActorRef`, `Outbox`), the strings
 *   inside messages (immutable, so a copy shares them), and immutable
 *   globals are values two threads can see at once, and a plain count
 *   cannot say who frees last. The macros below are that one switch: a
 *   program without actors keeps plain counts and pays nothing.
 * * **One lock, one condition.** Every mailbox push, outbox post and
 *   drain happens under `keal_actor_mu`; actor threads sleep on
 *   `keal_actor_cv` until a message lands or the system stops. Message
 *   values themselves need no lock — a message is deep-copied before the
 *   push, and the mutex hand-off orders the copy before the read.
 */
#ifdef KEAL_ACTORS
#include <pthread.h>
#include <stdatomic.h>
#ifdef _WIN32
#include <windows.h>
#else
#include <unistd.h>
#endif
typedef _Atomic int64_t keal_rc_t;
#define KEAL_RC_BUMP(rc) atomic_fetch_add_explicit(&(rc), 1, memory_order_relaxed)
#define KEAL_RC_DROP(rc) (atomic_fetch_sub_explicit(&(rc), 1, memory_order_acq_rel) > 1)

static pthread_mutex_t keal_actor_mu = PTHREAD_MUTEX_INITIALIZER;
static pthread_cond_t keal_actor_cv = PTHREAD_COND_INITIALIZER;

KEAL_FN void keal_actor_lock(void) { pthread_mutex_lock(&keal_actor_mu); }
KEAL_FN void keal_actor_unlock(void) { pthread_mutex_unlock(&keal_actor_mu); }
KEAL_FN void keal_actor_signal(void) { pthread_cond_broadcast(&keal_actor_cv); }
KEAL_FN void keal_actor_wait(void) { pthread_cond_wait(&keal_actor_cv, &keal_actor_mu); }

/* What one `run` shares with its actor threads: how many handlers are in
 * flight, the stop flag, and the first panic — carried back so it is
 * rethrown on the caller's thread, where `try { sys.run() }` can catch
 * it. All fields are guarded by `keal_actor_mu`. */
typedef struct KealRunState {
    int64_t workers;
    int stop;
    int panicked;
    /* Both are static strings in the program's own text, so a panic caught
     * on a worker and rethrown on the thread that called `run` still carries
     * its location and its hint across the handover. */
    const char* panic_at;
    const char* panic_note;
    char panic_msg[1024];
    /* One flag per actor: whether a worker is inside its handler right now.
     * An actor handles its messages one at a time and in order, and that is
     * the whole of the actor model's promise — so two workers must never be
     * in one actor at once, however many actors and however few workers. */
    int8_t* busy;
    int64_t actors;
    /* Where the next worker starts scanning. Without it every worker would
     * begin at actor 0 and the ones at the end of the list would starve. */
    int64_t next;
} KealRunState;

/* How many threads run the actors.
 *
 * Not one per actor. A program with ten thousand actors would ask the
 * operating system for ten thousand threads — eighty megabytes of stacks
 * before a single message is handled, and a scheduler with nothing useful
 * to do. Actors are a way of writing a program, not a way of asking for
 * threads, so the count comes from the machine and the work is shared.
 *
 * `KEAL_ACTOR_WORKERS` overrides it, which is how a test pins the number
 * and how somebody measuring can walk it up and down. */
KEAL_FN int64_t keal_actor_worker_count(int64_t actors) {
    if (actors <= 0) {
        return 0;
    }
    int64_t want = 0;
    const char* env = getenv("KEAL_ACTOR_WORKERS");
    if (env != NULL && env[0] != '\0') {
        want = (int64_t)strtoll(env, NULL, 10);
    }
    if (want <= 0) {
#ifdef _WIN32
        SYSTEM_INFO si;
        GetSystemInfo(&si);
        want = (int64_t)si.dwNumberOfProcessors;
#else
        long n = sysconf(_SC_NPROCESSORS_ONLN);
        want = n > 0 ? (int64_t)n : 1;
#endif
    }
    if (want < 1) {
        want = 1;
    }
    /* More workers than actors is waste: an actor is never run by two at
     * once, so the extra threads would only ever scan and sleep. */
    return want < actors ? want : actors;
}
#else
typedef int64_t keal_rc_t;
#define KEAL_RC_BUMP(rc) ((rc)++)
#define KEAL_RC_DROP(rc) (--(rc) > 0)
#endif

/* ---- the cycle audit ----------------------------------------------------
 *
 * `keal build --audit` defines KEAL_AUDIT, and then every class counts its
 * live objects by name. What is still counted when the program ends is what
 * nothing could free — which, reference counting being what it is, is a
 * cycle. The interpreters answer the same question when KEAL_AUDIT is set in
 * the environment, and print the same report, because a program's answer
 * must not depend on which engine asked.
 *
 * Off, none of this exists and no object pays for it. */
#ifdef KEAL_AUDIT
typedef struct KealAuditRow {
    const char* name;
    int64_t live;
    /* How many of the live ones a top-level binding can reach, filled in
     * by the mark phase further down before the report is printed. */
    int64_t held;
} KealAuditRow;

static KealAuditRow keal_audit_rows[256];
static int keal_audit_used = 0;

#ifdef KEAL_ACTORS
/* Actors count from several threads at once, so the rows go under the lock
 * the scheduler already owns. */
KEAL_FN void keal_actor_lock(void);
KEAL_FN void keal_actor_unlock(void);
#define KEAL_AUDIT_LOCK() keal_actor_lock()
#define KEAL_AUDIT_UNLOCK() keal_actor_unlock()
#else
#define KEAL_AUDIT_LOCK() ((void)0)
#define KEAL_AUDIT_UNLOCK() ((void)0)
#endif

static KealAuditRow* keal_audit_row(const char* name) {
    for (int i = 0; i < keal_audit_used; i++) {
        if (strcmp(keal_audit_rows[i].name, name) == 0) { return &keal_audit_rows[i]; }
    }
    if (keal_audit_used == (int)(sizeof(keal_audit_rows) / sizeof(keal_audit_rows[0]))) {
        return NULL;
    }
    keal_audit_rows[keal_audit_used].name = name;
    keal_audit_rows[keal_audit_used].live = 0;
    keal_audit_rows[keal_audit_used].held = 0;
    return &keal_audit_rows[keal_audit_used++];
}

KEAL_FN void keal_audit_born(const char* name) {
    KEAL_AUDIT_LOCK();
    KealAuditRow* r = keal_audit_row(name);
    if (r != NULL) { r->live++; }
    KEAL_AUDIT_UNLOCK();
}

/* The mark phase's counter: this object is one a top-level binding can
 * reach, so it lived to the end because the program said so. */
KEAL_FN void keal_audit_hold(const char* name) {
    KEAL_AUDIT_LOCK();
    KealAuditRow* r = keal_audit_row(name);
    if (r != NULL) { r->held++; }
    KEAL_AUDIT_UNLOCK();
}

KEAL_FN void keal_audit_died(const char* name) {
    KEAL_AUDIT_LOCK();
    KealAuditRow* r = keal_audit_row(name);
    if (r != NULL) { r->live--; }
    KEAL_AUDIT_UNLOCK();
}

/* The same words, in the same order, as the interpreters print. */
KEAL_FN void keal_audit_report(void) {
    /* After everything the program printed, for the reason in `keal_panic`. */
    fflush(stdout);
    int order[256];
    int n = 0;
    int64_t total = 0;
    for (int i = 0; i < keal_audit_used; i++) {
        if (keal_audit_rows[i].live > 0) {
            order[n++] = i;
            total += keal_audit_rows[i].live;
        }
    }
    if (n == 0) {
        fprintf(stderr, "audit: nothing outlived the program\n");
        return;
    }
    for (int i = 1; i < n; i++) {
        int key = order[i];
        int j = i - 1;
        while (j >= 0 && strcmp(keal_audit_rows[order[j]].name, keal_audit_rows[key].name) > 0) {
            order[j + 1] = order[j];
            j--;
        }
        order[j + 1] = key;
    }
    fprintf(stderr, "audit: %" PRId64 " object(s) outlived the program\n", total);
    for (int i = 0; i < n; i++) {
        fprintf(stderr, "  %" PRId64 " %s\n", keal_audit_rows[order[i]].live,
                keal_audit_rows[order[i]].name);
    }
    /* The verdict the counts alone could not give: what the mark phase
     * reached lived to the end because a top-level binding said so, and
     * what it did not reach outlived its last reference. */
    int64_t lost = 0;
    int64_t kept = 0;
    for (int i = 0; i < n; i++) {
        KealAuditRow* r = &keal_audit_rows[order[i]];
        int64_t held = r->held > r->live ? r->live : r->held;
        lost += r->live - held;
        kept += held;
    }
    if (lost == 0) {
        fprintf(stderr, "  = note: every one of them is held by a top-level binding, which lives to the end of a program by design — none is a cycle\n");
        return;
    }
    fprintf(stderr, "  %" PRId64 " of them are reachable from no top-level binding, so they outlived their last reference — a cycle:\n", lost);
    for (int i = 0; i < n; i++) {
        KealAuditRow* r = &keal_audit_rows[order[i]];
        int64_t held = r->held > r->live ? r->live : r->held;
        if (r->live - held > 0) {
            fprintf(stderr, "    %" PRId64 " %s\n", r->live - held, r->name);
        }
    }
    if (kept > 0) {
        fprintf(stderr, "  the rest are held by a top-level binding, which lives to the end of a program by design\n");
    }
    fprintf(stderr, "  = note: `weak` on the back edge breaks a cycle — see docs/memory.md §5\n");
}
#endif

/* Every heap object begins with its count. A string is that count, a length,
 * and the bytes; `static_bytes` marks a literal, whose bytes are not ours to
 * free. */
typedef struct KealStr {
    keal_rc_t rc;
    int64_t len;
    const char* bytes;
    bool static_bytes;
} KealStr;

/* ---- catchable panics ------------------------------------------------ */

/* `try` blocks count themselves in and out; a panic under an active one
 * records its message and unwinds by poisoned returns — every helper that
 * can panic returns a harmless value right after, and the generated code
 * checks `keal_unwinding` before acting on any result. The backend only
 * emits those checks when the program contains a `try` at all, so programs
 * without one pay nothing and `keal_panic` still ends the process. */
/* Per-thread, so each future actor thread panics, catches and unwinds
 * on its own (docs/threads.md). Single-threaded programs see no change. */
static _Thread_local int64_t keal_try_depth = 0;
static _Thread_local bool keal_unwinding = false;
static _Thread_local char keal_unwind_msg[1024];
/* Whether a `throw` gave the unwind a value of its own. A built-in
 * failure gives none: its message is its value, exactly as `runtime::err`
 * builds a `String` for the interpreters, so `catch (e: String)` catches
 * an overflow on all three engines. */
static _Thread_local bool keal_unwind_has_value = false;
/* Where it happened, and the hint that goes under it, so a panic carried
 * across threads — an actor's, rethrown on the thread that called `run` —
 * still says as much there as it would have said at home. */
static _Thread_local const char* keal_unwind_at = NULL;
static _Thread_local const char* keal_unwind_note = NULL;

/* ---- what a failure says --------------------------------------------- */

/* A compiled program answers a failure with what the interpreters answer:
 * the message, the line it happened on quoted with a caret under it, the
 * hint if the error has one, and the call stack. `docs/language.md` has
 * promised exactly that since before it was true of this engine.
 *
 * None of it is rendered here. `--> file:line:col`, the gutter, the caret
 * and each `in `f` at ...` line are produced at COMPILE time by the same
 * `Sources::render` that writes the interpreters' diagnostics, and reach the
 * program as static strings. Two engines cannot drift apart over a padding
 * rule they do not each own — and a caret placed by counting Unicode scalars
 * is a second implementation of a thing that was already right.
 *
 * The cost of that choice is bytes in the binary rather than code, which is
 * the trade this project takes every time it is offered. */

/* Innermost last, one static string per frame.
 *
 * Everything about this is shaped by a measurement rather than a preference,
 * and the measurement was taken twice because the first shape was wrong both
 * times. On a program that does nothing but call — `fib(30)` three times, 8
 * million calls — the cost per call went 1.6ns (nothing here at all), 10ns,
 * 4.4ns, and then to what it is now.
 *
 * What the two mistakes were, since neither is obvious from the outside:
 *
 * 1. `depth`, `n` and the array as three separate thread-locals, with the
 *    two operations out of line. A thread-local read is not a load on this
 *    platform, it is a call through a thunk, and there were six per Keal
 *    call. One object behind one symbol, read once, inlined: 10ns to 4.4ns.
 *
 * 2. Folding a repeated frame AT THE PUSH, so a runaway recursion would hold
 *    one entry with a count instead of ten thousand entries. That is a real
 *    saving of memory that is never scarce, bought with a compare and a
 *    branch on **every call a program ever makes**. The fold belongs where
 *    its output is read: `(x9999)` is printed by walking equal neighbours at
 *    print time, which happens once, in a program that is already ending.
 *
 * What is left is what a frame costs at minimum: one bounds check, one
 * store, one increment, and one decrement on the way out. `depth` is gone
 * because it was `n` all along.
 *
 * And with no actors there is no second thread to keep a stack for, so there
 * is no thread-local at all: `KEAL_ACTORS` is already the switch that makes
 * this runtime's counts atomic, and it makes this one static. */
#define KEAL_MAX_DEPTH 10000
typedef struct {
    int64_t n;
    const char* at[KEAL_MAX_DEPTH];
} KealStack;
#ifdef KEAL_ACTORS
static _Thread_local KealStack keal_stack;
#else
static KealStack keal_stack;
#endif

KEAL_FN void keal_panic(const char* what, const char* note, const char* at);

/* Out of line and never inlined into `keal_enter`: it runs at most once in a
 * program's life, and a cold branch that carries a 1KB function into every
 * call site is how the fast path stops being fast. */
static void keal_too_deep(const char* at) {
    keal_panic(
        "maximum call depth exceeded",
        "this usually means a recursive call with no base case",
        at);
}

/* Entering a Keal call, from the caller's side: the frame names the callee
 * and points at the call, which is what a stack trace is made of.
 *
 * This is also where a runaway recursion stops. `MAX_DEPTH` is the
 * interpreters' limit and this is the same number, so a program that ends
 * with `maximum call depth exceeded` on `--ast` ends the same way compiled —
 * where it used to run until the operating system took the process down, or
 * simply run. Being a panic rather than a crash, it is catchable, which a
 * segfault never was. */
static inline void keal_enter(const char* frame, const char* at) {
    KealStack* s = &keal_stack;
    if (__builtin_expect(s->n >= KEAL_MAX_DEPTH, 0)) {
        keal_too_deep(at);
        return;
    }
    s->at[s->n++] = frame;
}

static inline void keal_leave(void) {
    KealStack* s = &keal_stack;
    if (__builtin_expect(s->n > 0, 1)) {
        s->n--;
    }
}

/* The trace, innermost first, with equal neighbours folded the way the
 * interpreters fold them: ten thousand identical frames are one line and a
 * count. Done here, once, rather than on every call. */
static void keal_print_frames(void) {
    int64_t i = keal_stack.n - 1;
    while (i >= 0) {
        int64_t j = i;
        while (j > 0 && keal_stack.at[j - 1] == keal_stack.at[i]) {
            j--;
        }
        fputs(keal_stack.at[i], stderr);
        if (i - j > 0) {
            fprintf(stderr, " (x%" PRId64 ")", i - j + 1);
        }
        fputc('\n', stderr);
        i = j - 1;
    }
}

KEAL_FN void keal_panic(const char* what, const char* note, const char* at) {
    if (keal_try_depth > 0) {
        /* A panic while already unwinding keeps the first message. */
        if (!keal_unwinding) {
            keal_unwinding = true;
            keal_unwind_has_value = false;
            snprintf(keal_unwind_msg, sizeof keal_unwind_msg, "%s", what);
            keal_unwind_at = at;
            keal_unwind_note = note;
        }
        return;
    }
    /* What the program already printed comes first, because it happened
     * first. C leaves stdout fully buffered when it is not a terminal, so a
     * program that prints and then fails hands a pipe the failure BEFORE the
     * output, while the interpreters — whose writer flushes each line —
     * hand over the two in the order they occurred. Same words, same exit
     * status, different story. Comparing the streams separately cannot see
     * it, which is why it survived: it is only wrong where they meet. */
    fflush(stdout);
    fprintf(stderr, "runtime error: %s\n", what);
    if (at != NULL) {
        fputs(at, stderr);
    }
    if (note != NULL) {
        fprintf(stderr, "  = note: %s\n", note);
    }
    keal_print_frames();
    exit(1);
}

/* Failures no `catch` can make right end the process even under a `try`. */
KEAL_FN void keal_fatal(const char* what) {
    fflush(stdout);
    fprintf(stderr, "runtime error: %s\n", what);
    exit(1);
}

KEAL_FN void* keal_alloc(size_t n) {
    void* p = malloc(n);
    if (p == NULL) {
        keal_fatal("out of memory");
    }
    return p;
}

/* ---- reference counting ---------------------------------------------- */

/* Returns its argument so that a retain can be written inline, which is what
 * lets the generated code read as expressions rather than as statements. */
KEAL_FN KealStr* keal_str_retain(KealStr* s) {
    if (s != NULL) {
        KEAL_RC_BUMP(s->rc);
    }
    return s;
}

KEAL_FN void keal_str_release(KealStr* s) {
    if (s == NULL) {
        return;
    }
    if (KEAL_RC_DROP(s->rc)) {
        return;
    }
    if (!s->static_bytes) {
        free((void*)s->bytes);
    }
    free(s);
}

/* ---- strings ---------------------------------------------------------- */

KEAL_FN KealStr* keal_str_owning(char* bytes, int64_t len) {
    KealStr* s = (KealStr*)keal_alloc(sizeof(KealStr));
    s->rc = 1;
    s->len = len;
    s->bytes = bytes;
    s->static_bytes = false;
    return s;
}

/* A literal. Its bytes live in the binary, so only the header is allocated,
 * and the count starts at one for the program's own reference. */
KEAL_FN KealStr* keal_str_static(const char* bytes, int64_t len) {
    KealStr* s = (KealStr*)keal_alloc(sizeof(KealStr));
    s->rc = 1;
    s->len = len;
    s->bytes = bytes;
    s->static_bytes = true;
    return s;
}

KEAL_FN KealStr* keal_str_empty(void) {
    return keal_str_static("", 0);
}

KEAL_FN KealStr* keal_concat(KealStr* a, KealStr* b) {
    int64_t len = a->len + b->len;
    char* bytes = (char*)keal_alloc((size_t)len + 1);
    memcpy(bytes, a->bytes, (size_t)a->len);
    memcpy(bytes + a->len, b->bytes, (size_t)b->len);
    bytes[len] = '\0';
    return keal_str_owning(bytes, len);
}

/* Byte order, which for UTF-8 is also code-point order. */
KEAL_FN int keal_str_cmp(KealStr* a, KealStr* b) {
    int64_t n = a->len < b->len ? a->len : b->len;
    int c = memcmp(a->bytes, b->bytes, (size_t)n);
    if (c != 0) {
        return c;
    }
    if (a->len == b->len) {
        return 0;
    }
    return a->len < b->len ? -1 : 1;
}

/* ---- the string ABI a companion translation unit gets ----------------- */

/* Everything else in this runtime is `static`, deliberately: the emitted C is
 * one self-contained translation unit and its internals are its own. But a
 * `.c` file compiled beside it — `keal build prog.keal companion.c` — has to
 * be able to say something to a Keal function that takes or answers a
 * `String`, and `keal emit-header` cannot honestly declare `KealStr*` in a
 * prototype unless the other side can also make one and read one.
 *
 * These five are that, and no more. `KealStr` stays OPAQUE across the
 * boundary — the header declares the tag and not the layout — so what is
 * promised is five names, not a struct that can never change again.
 *
 * Ownership is the same rule the rest of the boundary follows: a Keal
 * function BORROWS its arguments and OWNS what it returns. So a companion
 * releases what it made and what it got back, and nothing else. */
KealStr* keal_abi_str_new(const char* bytes, int64_t len);
const char* keal_abi_str_bytes(KealStr* s);
int64_t keal_abi_str_len(KealStr* s);
KealStr* keal_abi_str_retain(KealStr* s);
void keal_abi_str_release(KealStr* s);

KEAL_FN KealStr* keal_str_from_bytes(const char* bytes, int64_t len) {
    char* copy = (char*)keal_alloc((size_t)len + 1);
    memcpy(copy, bytes, (size_t)len);
    copy[len] = '\0';
    return keal_str_owning(copy, len);
}

/* The five definitions. Each is a thin call into the static one beside it, so
 * a companion and the program agree by construction rather than by care. */
KealStr* keal_abi_str_new(const char* bytes, int64_t len) {
    return keal_str_from_bytes(bytes, len);
}
const char* keal_abi_str_bytes(KealStr* s) { return s == NULL ? "" : s->bytes; }
int64_t keal_abi_str_len(KealStr* s) { return s == NULL ? 0 : s->len; }
KealStr* keal_abi_str_retain(KealStr* s) { return keal_str_retain(s); }
void keal_abi_str_release(KealStr* s) { keal_str_release(s); }

KEAL_FN KealStr* keal_str_from_int(int64_t n) {
    char buf[32];
    int len = snprintf(buf, sizeof buf, "%" PRId64, n);
    return keal_str_from_bytes(buf, len);
}

/* Matches how the other two engines render a float: a whole number keeps
 * one decimal place, so `1.0` does not print as `1`, and everything else
 * prints the SHORTEST decimal that reads back as exactly the same value —
 * the same answer Rust's formatter gives, found here by widening the
 * precision until the round-trip closes. */
KEAL_FN KealStr* keal_str_from_float(double d) {
    /* 5e-324 written out is "0." and 324 digits; the largest double is 309
     * digits before the point. 768 leaves room for both and the sign. */
    char buf[768];
    int len;
    /* `NaN`, as the two interpreters spell it. C's `%g` writes `nan`, and
     * the loop below could never have corrected it: it accepts a precision
     * when `strtod` reads the text back equal to `d`, and no NaN is equal
     * to itself. So every NaN fell through seventeen rounds of that and
     * landed on the C spelling — a three-engine disagreement on output,
     * which is the one thing the three are not allowed to have.
     * `inf` and `-inf` already agree. */
    if (d != d) {
        return keal_str_static("NaN", 3);
    }
    /* The range test first, and the cast second.
     *
     * These were the other way round, so every program that printed a float
     * outside int64_t's range converted one into it — undefined in C, and
     * caught by `-fsanitize=float-cast-overflow` on `1e300`. The range guard
     * behind it made the answer right anyway, which is why it survived: it
     * was undefined behaviour whose result was never used. `&&` sequences
     * left to right, so the cast now happens only where it is defined. */
    if (d > -1e15 && d < 1e15 && d == (double)(int64_t)d) {
        len = snprintf(buf, sizeof buf, "%.1f", d);
        return keal_str_from_bytes(buf, len);
    }
    /* The shortest digits that read back as this value, written out in
     * full — never in scientific notation, because the two interpreters
     * never write one.
     *
     * `%g` switches to an exponent as soon as one leaves [-4, precision),
     * so `1e15` printed as `1e+15` natively and `1000000000000000` on the
     * other two engines, and `1e300` printed as 1e+300 against three
     * hundred zeros. Everything outside [1e-4, 1e15) disagreed. The
     * precision loop could not correct it: `1e+15` reads back exactly, so
     * it was accepted on the first round.
     *
     * `%g` still finds the precision — it is the cheapest way to ask how
     * many significant digits round-trip — and then `%e` says where the
     * point sits, and `%f` writes it there. */
    for (int prec = 1; prec <= 17; prec++) {
        len = snprintf(buf, sizeof buf, "%.*g", prec, d);
        if (strtod(buf, NULL) != d) {
            continue;
        }
        if (strchr(buf, 'e') == NULL && strchr(buf, 'n') == NULL) {
            return keal_str_from_bytes(buf, len);
        }
        /* `inf`, which both other engines also write as `inf`. */
        if (strchr(buf, 'n') != NULL) {
            return keal_str_from_bytes(buf, len);
        }
        /* The digits themselves, laid out by hand.
         *
         * `%.*f` on the value would print what the double exactly IS —
         * 1e300 expands to 1000...0525047602552044..., three hundred digits
         * of binary truth. The other two engines print the shortest digits
         * that read back as this value, `1`, followed by three hundred
         * zeros. So the digits come from `%e`, which gives exactly those,
         * and the point goes where the exponent says. */
        char sci[64];
        snprintf(sci, sizeof sci, "%.*e", prec - 1, d);
        const char* p = sci;
        char sign = '\0';
        if (*p == '-') {
            sign = '-';
            p++;
        }
        char digits[24];
        int nd = 0;
        for (; *p != '\0' && *p != 'e' && nd < (int)sizeof digits - 1; p++) {
            if (*p != '.') {
                digits[nd++] = *p;
            }
        }
        digits[nd] = '\0';
        int exp10 = *p == 'e' ? atoi(p + 1) : 0;

        char* w = buf;
        if (sign != '\0') {
            *w++ = sign;
        }
        if (exp10 >= nd - 1) {
            /* Every digit is before the point, then zeros to reach it. */
            memcpy(w, digits, (size_t)nd);
            w += nd;
            for (int i = 0; i < exp10 - (nd - 1); i++) {
                *w++ = '0';
            }
        } else if (exp10 >= 0) {
            memcpy(w, digits, (size_t)exp10 + 1);
            w += exp10 + 1;
            *w++ = '.';
            memcpy(w, digits + exp10 + 1, (size_t)(nd - exp10 - 1));
            w += nd - exp10 - 1;
        } else {
            *w++ = '0';
            *w++ = '.';
            for (int i = 0; i < -exp10 - 1; i++) {
                *w++ = '0';
            }
            memcpy(w, digits, (size_t)nd);
            w += nd;
        }
        return keal_str_from_bytes(buf, (int)(w - buf));
    }
    len = snprintf(buf, sizeof buf, "%.17g", d);
    return keal_str_from_bytes(buf, len);
}

KEAL_FN KealStr* keal_str_from_bool(bool b) {
    return keal_str_static(b ? "true" : "false", b ? 4 : 5);
}

/* A comparison's outcome, as the word it is written with. The ordinal is
 * the whole value — 0, 1, 2 — the way `true` and `false` are the whole
 * value of a Bool. */
KEAL_FN KealStr* keal_str_from_comp(int64_t c) {
    if (c == 0) { return keal_str_static("less", 4); }
    if (c == 1) { return keal_str_static("equal", 5); }
    return keal_str_static("greater", 7);
}

/* `.length` counts characters, not bytes, as the interpreters do. */
KEAL_FN int64_t keal_str_length(KealStr* s) {
    int64_t n = 0;
    for (int64_t i = 0; i < s->len; i++) {
        if (((unsigned char)s->bytes[i] & 0xC0) != 0x80) {
            n++;
        }
    }
    return n;
}

/* What the interpreters say when standard output will not take a write, word
 * for word, since the three engines answer the same question the same way. */
static void keal_write_failed(void) {
    char msg[160];
    snprintf(msg, sizeof msg, "cannot write to standard output: %s (os error %d)",
             strerror(errno), errno);
    keal_panic(msg, NULL, NULL);
}

KEAL_FN void keal_print(KealStr* s, bool newline) {
    if (!newline) {
        /* A `print` with no newline is usually a prompt, and a prompt sitting
         * in a buffer is a program that looks hung. */
        if (fwrite(s->bytes, 1, (size_t)s->len, stdout) != (size_t)s->len
            || fflush(stdout) != 0) {
            keal_write_failed();
        }
        return;
    }
    /* ONE write, not two. `fwrite` and `fputc` each lock the stream, but not
     * together, so a second actor's line lands between a line and its newline
     * and both come out wrong — two lines joined, then an empty one. The
     * interpreters write a line under a single lock and never tear, so this
     * was a disagreement about output between the engines, visible only with
     * more than one thread printing. */
    size_t n = (size_t)s->len;
    char small[512];
    char* buf = n + 1 <= sizeof small ? small : (char*)keal_alloc(n + 1);
    memcpy(buf, s->bytes, n);
    buf[n] = '\n';
    size_t wrote = fwrite(buf, 1, n + 1, stdout);
    if (buf != small) {
        free(buf);
    }
    if (wrote != n + 1) {
        keal_write_failed();
    }
}

/* ---- string methods ---------------------------------------------------- */

/* Byte offset of the n-th character; `n` past the end returns `len`. */
KEAL_FN int64_t keal_str_char_byte(KealStr* s, int64_t n) {
    int64_t seen = 0;
    for (int64_t i = 0; i < s->len; i++) {
        if (((unsigned char)s->bytes[i] & 0xC0) != 0x80) {
            if (seen == n) {
                return i;
            }
            seen++;
        }
    }
    return s->len;
}

KEAL_FN KealStr* keal_str_substring(KealStr* s, int64_t a, int64_t b, const char* at) {
    int64_t len = keal_str_length(s);
    int64_t ca = a < 0 ? 0 : a;
    int64_t cb = b < 0 ? 0 : b;
    if (ca > cb || cb > len) {
        char msg[128];
        snprintf(msg, sizeof msg,
                 "substring(%" PRId64 ", %" PRId64 ") is out of range for a string of length %" PRId64,
                 ca, cb, len);
        keal_panic(msg, NULL, at);
        return NULL;
    }
    int64_t from = keal_str_char_byte(s, ca);
    int64_t to = keal_str_char_byte(s, cb);
    return keal_str_from_bytes(s->bytes + from, to - from);
}

KEAL_FN KealStr* keal_str_take(KealStr* s, int64_t n) {
    int64_t len = keal_str_length(s);
    int64_t k = n < 0 ? 0 : (n > len ? len : n);
    return keal_str_from_bytes(s->bytes, keal_str_char_byte(s, k));
}

KEAL_FN KealStr* keal_str_drop(KealStr* s, int64_t n) {
    int64_t len = keal_str_length(s);
    int64_t k = n < 0 ? 0 : (n > len ? len : n);
    int64_t from = keal_str_char_byte(s, k);
    return keal_str_from_bytes(s->bytes + from, s->len - from);
}

/* Byte position of `needle` in `s`, or -1. An empty needle is found at 0,
 * matching what the interpreters (and Rust's `find`) say. */
KEAL_FN int64_t keal_str_find_bytes(KealStr* s, KealStr* needle) {
    if (needle->len == 0) {
        return 0;
    }
    if (needle->len > s->len) {
        return -1;
    }
    for (int64_t i = 0; i + needle->len <= s->len; i++) {
        if (memcmp(s->bytes + i, needle->bytes, (size_t)needle->len) == 0) {
            return i;
        }
    }
    return -1;
}

KEAL_FN bool keal_str_contains(KealStr* s, KealStr* needle) {
    return keal_str_find_bytes(s, needle) >= 0;
}

KEAL_FN bool keal_str_starts_with(KealStr* s, KealStr* prefix) {
    return prefix->len <= s->len
        && memcmp(s->bytes, prefix->bytes, (size_t)prefix->len) == 0;
}

KEAL_FN bool keal_str_ends_with(KealStr* s, KealStr* suffix) {
    return suffix->len <= s->len
        && memcmp(s->bytes + (s->len - suffix->len), suffix->bytes,
                  (size_t)suffix->len) == 0;
}

/* The index in characters, not bytes, as the interpreters report it. */
KEAL_FN int64_t keal_str_index_of(KealStr* s, KealStr* needle) {
    int64_t byte = keal_str_find_bytes(s, needle);
    if (byte < 0) {
        return -1;
    }
    int64_t chars = 0;
    for (int64_t i = 0; i < byte; i++) {
        if (((unsigned char)s->bytes[i] & 0xC0) != 0x80) {
            chars++;
        }
    }
    return chars;
}

KEAL_FN KealStr* keal_str_replace(KealStr* s, KealStr* old, KealStr* newer, const char* at) {
    if (old->len == 0) {
        keal_panic("`replace` needs a non-empty search string", NULL, at);
        return NULL;
    }
    /* KealBuf is declared later in this file, so the bytes are collected by
     * hand into a local buffer. */
    int64_t cap = s->len + 16;
    int64_t len = 0;
    char* out = (char*)keal_alloc((size_t)cap);
    int64_t i = 0;
    while (i < s->len) {
        if (i + old->len <= s->len
            && memcmp(s->bytes + i, old->bytes, (size_t)old->len) == 0) {
            while (len + newer->len > cap) {
                cap *= 2;
                char* grown = (char*)keal_alloc((size_t)cap);
                memcpy(grown, out, (size_t)len);
                free(out);
                out = grown;
            }
            memcpy(out + len, newer->bytes, (size_t)newer->len);
            len += newer->len;
            i += old->len;
        } else {
            while (len + 1 > cap) {
                cap *= 2;
                char* grown = (char*)keal_alloc((size_t)cap);
                memcpy(grown, out, (size_t)len);
                free(out);
                out = grown;
            }
            out[len++] = s->bytes[i++];
        }
    }
    KealStr* r = keal_str_from_bytes(out, len);
    free(out);
    return r;
}

KEAL_FN KealStr* keal_str_repeat(KealStr* s, int64_t n, const char* at) {
    if (n < 0) {
        char msg[96];
        snprintf(msg, sizeof msg, "`repeat` needs a non-negative count, got %" PRId64, n);
        keal_panic(msg, NULL, at);
        return NULL;
    }
    int64_t total = s->len * n;
    char* out = (char*)keal_alloc((size_t)(total < 1 ? 1 : total));
    for (int64_t i = 0; i < n; i++) {
        memcpy(out + i * s->len, s->bytes, (size_t)s->len);
    }
    KealStr* r = keal_str_from_bytes(out, total);
    free(out);
    return r;
}

/* The first code point, or -1 for an empty string. */
KEAL_FN int64_t keal_str_code(KealStr* s) {
    if (s->len == 0) {
        return -1;
    }
    unsigned char b0 = (unsigned char)s->bytes[0];
    if (b0 < 0x80) {
        return b0;
    }
    if ((b0 & 0xE0) == 0xC0 && s->len >= 2) {
        return ((int64_t)(b0 & 0x1F) << 6) | (s->bytes[1] & 0x3F);
    }
    if ((b0 & 0xF0) == 0xE0 && s->len >= 3) {
        return ((int64_t)(b0 & 0x0F) << 12) | ((int64_t)(s->bytes[1] & 0x3F) << 6)
            | (s->bytes[2] & 0x3F);
    }
    if ((b0 & 0xF8) == 0xF0 && s->len >= 4) {
        return ((int64_t)(b0 & 0x07) << 18) | ((int64_t)(s->bytes[1] & 0x3F) << 12)
            | ((int64_t)(s->bytes[2] & 0x3F) << 6) | (s->bytes[3] & 0x3F);
    }
    return b0;
}

KEAL_FN KealStr* keal_int_to_char(int64_t n, const char* at) {
    bool valid = n >= 0 && n <= 0x10FFFF && !(n >= 0xD800 && n <= 0xDFFF);
    if (!valid) {
        char msg[96];
        snprintf(msg, sizeof msg, "%" PRId64 " is not a valid character code", n);
        keal_panic(msg, NULL, at);
        return NULL;
    }
    char buf[4];
    int len;
    if (n < 0x80) {
        buf[0] = (char)n;
        len = 1;
    } else if (n < 0x800) {
        buf[0] = (char)(0xC0 | (n >> 6));
        buf[1] = (char)(0x80 | (n & 0x3F));
        len = 2;
    } else if (n < 0x10000) {
        buf[0] = (char)(0xE0 | (n >> 12));
        buf[1] = (char)(0x80 | ((n >> 6) & 0x3F));
        buf[2] = (char)(0x80 | (n & 0x3F));
        len = 3;
    } else {
        buf[0] = (char)(0xF0 | (n >> 18));
        buf[1] = (char)(0x80 | ((n >> 12) & 0x3F));
        buf[2] = (char)(0x80 | ((n >> 6) & 0x3F));
        buf[3] = (char)(0x80 | (n & 0x3F));
        len = 4;
    }
    return keal_str_from_bytes(buf, len);
}

/* `Float.toInt` truncates toward zero and saturates at the edges — the
 * semantics of Rust's `as i64`, which is what the interpreters run. */
KEAL_FN int64_t keal_f2i(double d) {
    if (d != d) {
        return 0;
    }
    if (d >= 9223372036854775807.0) {
        return INT64_MAX;
    }
    if (d <= -9223372036854775808.0) {
        return INT64_MIN;
    }
    return (int64_t)d;
}

KEAL_FN int64_t keal_int_abs(int64_t n) {
    if (n < 0) {
        return (int64_t)(0 - (uint64_t)n);
    }
    return n;
}

KEAL_FN int64_t keal_int_pow(int64_t n, int64_t e, const char* at) {
    if (e < 0) {
        char msg[96];
        snprintf(msg, sizeof msg, "`Int.pow` needs a non-negative exponent, got %" PRId64, e);
        keal_panic(msg, NULL, at);
        return 0;
    }
    int64_t r = 1;
    for (int64_t i = 0; i < e; i++) {
        if (__builtin_mul_overflow(r, n, &r)) {
            char msg[96];
            snprintf(msg, sizeof msg, "integer overflow in %" PRId64 ".pow(%" PRId64 ")", n, e);
            keal_panic(msg, NULL, at);
            return 0;
        }
    }
    return r;
}

/* The integer d-th root: the largest r >= 0 with r**d <= n — the inverse
 * of `**` on the whole numbers, shared by `^/`, `^/=` and `Int.root`. */
KEAL_FN int64_t keal_int_root(int64_t n, int64_t d, const char* at) {
    if (d <= 0) {
        char msg[96];
        snprintf(msg, sizeof msg, "`root` needs a positive degree, got %" PRId64, d);
        keal_panic(msg, NULL, at);
        return 0;
    }
    if (n < 0) {
        keal_panic("cannot take the root of a negative number", NULL, at);
        return 0;
    }
    if (n == 0) {
        return 0;
    }
    int64_t r = (int64_t)floor(pow((double)n, 1.0 / (double)d));
    if (r < 1) {
        r = 1;
    }
    /* Exact fixup: r**d computed with overflow watched, so the float
     * estimate can neither over- nor under-shoot the answer. */
    for (;;) {
        int64_t c = r + 1;
        int64_t acc = 1;
        bool over = false;
        for (int64_t i = 0; i < d; i++) {
            if (__builtin_mul_overflow(acc, c, &acc) || acc > n) {
                over = true;
                break;
            }
        }
        if (over) {
            break;
        }
        r = c;
    }
    for (;;) {
        int64_t acc = 1;
        bool over = false;
        for (int64_t i = 0; i < d; i++) {
            if (__builtin_mul_overflow(acc, r, &acc) || acc > n) {
                over = true;
                break;
            }
        }
        if (!over) {
            break;
        }
        r -= 1;
    }
    return r;
}

/* The d-th root of a float: IEEE all the way — a negative base is NaN, not
 * a panic, exactly as `**` with a fractional exponent would be. */
KEAL_FN double keal_f_root(double x, double d) {
    return pow(x, 1.0 / d);
}

KEAL_FN int64_t keal_int_min(int64_t a, int64_t b) { return a < b ? a : b; }

KEAL_FN int64_t keal_int_max(int64_t a, int64_t b) { return a > b ? a : b; }

/* Adopts a NUL-terminated buffer from C: Keal owns it from here and will
 * free() it when the last reference goes. The other half of `own String`.
 * A NULL from C reads as the empty string. */
KEAL_FN KealStr* keal_str_adopt(char* bytes) {
    if (bytes == NULL) {
        return keal_str_empty();
    }
    return keal_str_owning(bytes, (int64_t)strlen(bytes));
}

/* `s[i]` and `s.get(i)`: one character, as a string. Negative counts from
 * the end. */
KEAL_FN KealStr* keal_str_get(KealStr* s, int64_t i, const char* at) {
    int64_t len = keal_str_length(s);
    int64_t idx = i < 0 ? i + len : i;
    if (idx < 0 || idx >= len) {
        char msg[128];
        snprintf(msg, sizeof msg,
                 "index %" PRId64 " is out of bounds for a string of length %" PRId64, i, len);
        keal_panic(msg, NULL, at);
        return NULL;
    }
    int64_t from = keal_str_char_byte(s, idx);
    int64_t to = keal_str_char_byte(s, idx + 1);
    return keal_str_from_bytes(s->bytes + from, to - from);
}

/* Case mapping and trim are ASCII: the interpreters use Unicode tables the
 * runtime does not carry, so non-ASCII cased letters pass through unchanged.
 * ASCII covers what the language's own tooling needs. */
KEAL_FN KealStr* keal_str_to_lower(KealStr* s) {
    char* out = (char*)keal_alloc((size_t)(s->len < 1 ? 1 : s->len));
    for (int64_t i = 0; i < s->len; i++) {
        char c = s->bytes[i];
        out[i] = (c >= 'A' && c <= 'Z') ? (char)(c + 32) : c;
    }
    KealStr* r = keal_str_from_bytes(out, s->len);
    free(out);
    return r;
}

KEAL_FN KealStr* keal_str_to_upper(KealStr* s) {
    char* out = (char*)keal_alloc((size_t)(s->len < 1 ? 1 : s->len));
    for (int64_t i = 0; i < s->len; i++) {
        char c = s->bytes[i];
        out[i] = (c >= 'a' && c <= 'z') ? (char)(c - 32) : c;
    }
    KealStr* r = keal_str_from_bytes(out, s->len);
    free(out);
    return r;
}

KEAL_FN KealStr* keal_str_trim(KealStr* s) {
    int64_t a = 0;
    int64_t b = s->len;
    while (a < b && (s->bytes[a] == ' ' || s->bytes[a] == '\t' || s->bytes[a] == '\n' || s->bytes[a] == '\r')) {
        a++;
    }
    while (b > a && (s->bytes[b - 1] == ' ' || s->bytes[b - 1] == '\t' || s->bytes[b - 1] == '\n' || s->bytes[b - 1] == '\r')) {
        b--;
    }
    return keal_str_from_bytes(s->bytes + a, b - a);
}

/* ---- lists ------------------------------------------------------------ */

/* An element is one word: an integer, a double, or a pointer. The static
 * type at every use site says which, so the runtime never needs to ask. */
typedef union KealWord {
    int64_t i;
    double d;
    void* p;
} KealWord;

/* The one place an element's type is not statically known is inside the
 * list's own release, where the elements must be let go without any caller
 * present. The releaser is fixed at construction; NULL marks elements with
 * nothing to release. */
typedef struct KealList {
    keal_rc_t rc;
    int64_t len;
    int64_t cap;
    KealWord* data;
    void (*release_elem)(void*);
} KealList;

KEAL_FN KealList* keal_list_new(void (*release_elem)(void*)) {
    KealList* l = (KealList*)keal_alloc(sizeof(KealList));
    l->rc = 1;
    l->len = 0;
    l->cap = 0;
    l->data = NULL;
    l->release_elem = release_elem;
    return l;
}

KEAL_FN KealList* keal_list_retain(KealList* l) {
    if (l != NULL) {
        KEAL_RC_BUMP(l->rc);
    }
    return l;
}

KEAL_FN void keal_list_release(KealList* l) {
    if (l == NULL) {
        return;
    }
    if (KEAL_RC_DROP(l->rc)) {
        return;
    }
    if (l->release_elem != NULL) {
        for (int64_t i = 0; i < l->len; i++) {
            l->release_elem(l->data[i].p);
        }
    }
    free(l->data);
    free(l);
}

KEAL_FN void keal_list_push(KealList* l, KealWord w) {
    if (l->len == l->cap) {
        l->cap = l->cap < 4 ? 4 : l->cap * 2;
        KealWord* grown = (KealWord*)keal_alloc((size_t)l->cap * sizeof(KealWord));
        memcpy(grown, l->data, (size_t)l->len * sizeof(KealWord));
        free(l->data);
        l->data = grown;
    }
    l->data[l->len++] = w;
}

/* Negative indices count from the end, as everywhere in the language. */
KEAL_FN int64_t keal_list_index(KealList* l, int64_t i, const char* at) {
    int64_t k = i < 0 ? i + l->len : i;
    if (k < 0 || k >= l->len) {
        char msg[96];
        snprintf(msg, sizeof msg,
                 "index %" PRId64 " is out of bounds for a list of %" PRId64 " element(s)", i,
                 l->len);
        keal_panic(msg, NULL, at);
        return 0;
    }
    return k;
}

KEAL_FN KealWord keal_list_get(KealList* l, int64_t i, const char* at) {
    int64_t k = keal_list_index(l, i, at);
    if (keal_unwinding) {
        KealWord none = {0};
        return none;
    }
    return l->data[k];
}

/* The old element is handed back to the caller, which knows its type and
 * releases it — the runtime only stores. */
KEAL_FN KealWord keal_list_set(KealList* l, int64_t i, KealWord w, const char* at) {
    int64_t k = keal_list_index(l, i, at);
    if (keal_unwinding) {
        KealWord none = {0};
        return none;
    }
    KealWord old = l->data[k];
    l->data[k] = w;
    return old;
}

/* A shallow copy for `for`, so the loop walks what the list held when it
 * started, whatever the body does to it — the interpreters' rule. Elements
 * are not retained: the copy is consumed by the loop before anything could
 * release the original's references out from under it, because the original
 * itself is still alive across the loop. */
KEAL_FN KealList* keal_list_snapshot(KealList* l) {
    KealList* c = keal_list_new(NULL);
    c->len = l->len;
    c->cap = l->len;
    c->data = (KealWord*)keal_alloc((size_t)(l->len < 1 ? 1 : l->len) * sizeof(KealWord));
    memcpy(c->data, l->data, (size_t)l->len * sizeof(KealWord));
    return c;
}

/* Removal hands the element's reference to the caller, which knows the type
 * and owns it from here. */
KEAL_FN KealWord keal_list_remove_at(KealList* l, int64_t i, const char* at) {
    int64_t idx = i < 0 ? i + l->len : i;
    if (idx < 0 || idx >= l->len) {
        char msg[96];
        snprintf(msg, sizeof msg,
                 "index %" PRId64 " is out of bounds for a list of %" PRId64, i, l->len);
        keal_panic(msg, NULL, at);
        KealWord none = {0};
        return none;
    }
    KealWord out = l->data[idx];
    memmove(l->data + idx, l->data + idx + 1,
            (size_t)(l->len - idx - 1) * sizeof(KealWord));
    l->len--;
    return out;
}

/* Takes ownership of the word, like push. */
KEAL_FN void keal_list_insert_at(KealList* l, int64_t i, KealWord w, const char* at) {
    if (i < 0 || i > l->len) {
        char msg[96];
        snprintf(msg, sizeof msg,
                 "cannot insert at index %" PRId64 " in a list of %" PRId64, i, l->len);
        keal_panic(msg, NULL, at);
        return;
    }
    keal_list_push(l, w);
    memmove(l->data + i + 1, l->data + i, (size_t)(l->len - i - 1) * sizeof(KealWord));
    l->data[i] = w;
}

/* `xs.set(i, v)` — the method, not the indexing form. It counts a negative
 * index from the end like everything else, but its out-of-range message is
 * the method's own: the shorter one `removeAt` gives, not the "element(s)"
 * one `keal_list_index` gives for `xs[i]`. The interpreters draw the same
 * line, so the runtime does too. The displaced element is handed back to
 * the caller, which knows its type and releases it. */
KEAL_FN KealWord keal_list_set_at(KealList* l, int64_t i, KealWord w, const char* at) {
    int64_t idx = i < 0 ? i + l->len : i;
    if (idx < 0 || idx >= l->len) {
        char msg[96];
        snprintf(msg, sizeof msg,
                 "index %" PRId64 " is out of bounds for a list of %" PRId64, i, l->len);
        keal_panic(msg, NULL, at);
        KealWord none = {0};
        return none;
    }
    KealWord old = l->data[idx];
    l->data[idx] = w;
    return old;
}

/* `xs.clear()` — every element let go, front to back, by the releaser fixed
 * at construction, exactly as the list's own death lets them go. The buffer
 * is kept: capacity is not something a program can observe. Nothing user
 * code wrote can run inside the loop, because a `deinit` is queued rather
 * than called from a release. */
KEAL_FN void keal_list_clear(KealList* l) {
    if (l->release_elem != NULL) {
        for (int64_t i = 0; i < l->len; i++) {
            l->release_elem(l->data[i].p);
        }
    }
    l->len = 0;
}

/* Every counted object leads with its count — the memory model's promise —
 * so one raw bump serves whatever a pointer element points at. */
KEAL_FN void keal_word_retain_raw(void* p) {
    if (p != NULL) {
        (*(int64_t*)p)++;
    }
}

/* `dst.addAll(src)`: dst takes a reference of its own to each element. */
KEAL_FN void keal_list_add_all(KealList* dst, KealList* src) {
    int64_t n = src->len;
    for (int64_t i = 0; i < n; i++) {
        KealWord w = src->data[i];
        if (dst->release_elem != NULL) {
            keal_word_retain_raw(w.p);
        }
        keal_list_push(dst, w);
    }
}

/* `take` and `drop` build new lists; each element carried over gets a
 * reference of its own, by the raw bump every counted object supports. */
KEAL_FN KealList* keal_list_take(KealList* l, int64_t n) {
    int64_t k = n < 0 ? 0 : (n > l->len ? l->len : n);
    KealList* out = keal_list_new(l->release_elem);
    for (int64_t i = 0; i < k; i++) {
        if (out->release_elem != NULL) {
            keal_word_retain_raw(l->data[i].p);
        }
        keal_list_push(out, l->data[i]);
    }
    return out;
}

/* `xs.slice(start, end)` — end exclusive, both clamped, and an empty list
 * where they cross. The interpreters clamp rather than panic, so this does
 * too: asking for more than there is answers with what there is. */
KEAL_FN KealList* keal_list_slice(KealList* l, int64_t start, int64_t end) {
    int64_t a = start < 0 ? 0 : (start > l->len ? l->len : start);
    int64_t b = end < 0 ? 0 : (end > l->len ? l->len : end);
    KealList* out = keal_list_new(l->release_elem);
    for (int64_t i = a; i < b; i++) {
        if (out->release_elem != NULL) {
            keal_word_retain_raw(l->data[i].p);
        }
        keal_list_push(out, l->data[i]);
    }
    return out;
}

KEAL_FN KealList* keal_list_drop(KealList* l, int64_t n) {
    int64_t k = n < 0 ? 0 : (n > l->len ? l->len : n);
    KealList* out = keal_list_new(l->release_elem);
    for (int64_t i = k; i < l->len; i++) {
        if (out->release_elem != NULL) {
            keal_word_retain_raw(l->data[i].p);
        }
        keal_list_push(out, l->data[i]);
    }
    return out;
}

/* `xs.reversed()` — a new list, back to front, the receiver untouched;
 * each element carried over gets a reference of its own, the same way
 * `take` and `drop` take theirs. */
KEAL_FN KealList* keal_list_reversed(KealList* l) {
    KealList* out = keal_list_new(l->release_elem);
    for (int64_t i = l->len - 1; i >= 0; i--) {
        if (out->release_elem != NULL) {
            keal_word_retain_raw(l->data[i].p);
        }
        keal_list_push(out, l->data[i]);
    }
    return out;
}

KEAL_FN int keal_cmp_i64(KealWord a, KealWord b) {
    return a.i < b.i ? -1 : (a.i > b.i ? 1 : 0);
}

KEAL_FN int keal_cmp_str(KealWord a, KealWord b) {
    return keal_str_cmp((KealStr*)a.p, (KealStr*)b.p);
}

/* A stable in-place insertion sort by the given comparator. */
KEAL_FN void keal_list_sort_words(KealList* items, int (*cmp)(KealWord, KealWord)) {
    for (int64_t i = 1; i < items->len; i++) {
        KealWord it = items->data[i];
        int64_t j = i - 1;
        while (j >= 0 && cmp(items->data[j], it) > 0) {
            items->data[j + 1] = items->data[j];
            j--;
        }
        items->data[j + 1] = it;
    }
}

KEAL_FN bool keal_key_eq_f64(KealWord a, KealWord b) {
    return a.d == b.d;
}

KEAL_FN bool keal_key_eq_opt_str(KealWord a, KealWord b) {
    KealStr* x = (KealStr*)a.p;
    KealStr* y = (KealStr*)b.p;
    if (x == NULL || y == NULL) {
        return x == y;
    }
    return keal_str_cmp(x, y) == 0;
}

KEAL_FN bool keal_list_contains(KealList* l, KealWord w, bool (*eq)(KealWord, KealWord)) {
    for (int64_t i = 0; i < l->len; i++) {
        if (eq(l->data[i], w)) {
            return true;
        }
    }
    return false;
}

/* `xs.indexOf(v)` — the first index whose element compares equal, and -1
 * when none does, which is the answer both interpreters give. The same
 * equality `contains` scans with, so the two never disagree. */
KEAL_FN int64_t keal_list_index_of(KealList* l, KealWord w, bool (*eq)(KealWord, KealWord)) {
    for (int64_t i = 0; i < l->len; i++) {
        if (eq(l->data[i], w)) {
            return i;
        }
    }
    return -1;
}

/* Structural equality, as the interpreters compare lists: same length,
 * elements equal pairwise. Null equals only null. */
KEAL_FN bool keal_list_eq(KealList* a, KealList* b, bool (*eq)(KealWord, KealWord)) {
    if (a == NULL || b == NULL || a == b) {
        return a == b;
    }
    if (a->len != b->len) {
        return false;
    }
    for (int64_t i = 0; i < a->len; i++) {
        if (!eq(a->data[i], b->data[i])) {
            return false;
        }
    }
    return true;
}

/* forward declaration: maps are declared below, their equality with them. */
typedef struct KealMap KealMap;
KEAL_FN int64_t keal_map_find(KealMap* m, KealWord key);

/* A stable insertion sort of `items` by the parallel Int keys — small,
 * predictable, and stable, which is the semantics the interpreters give. */
KEAL_FN void keal_list_sort_by_i64(KealList* items, KealList* keys) {
    for (int64_t i = 1; i < items->len; i++) {
        KealWord it = items->data[i];
        KealWord k = keys->data[i];
        int64_t j = i - 1;
        while (j >= 0 && keys->data[j].i > k.i) {
            items->data[j + 1] = items->data[j];
            keys->data[j + 1] = keys->data[j];
            j--;
        }
        items->data[j + 1] = it;
        keys->data[j + 1] = k;
    }
}

/* `xs.sum()` over Ints, checked: the language refuses to wrap, so a total
 * that will not fit fails here rather than quietly turning negative — and
 * with the interpreters' wording, so a `catch` reads the same message on
 * all three engines. */
KEAL_FN int64_t keal_list_sum_i64(KealList* l, const char* at) {
    int64_t total = 0;
    for (int64_t i = 0; i < l->len; i++) {
        int64_t r;
        if (__builtin_add_overflow(total, l->data[i].i, &r)) {
            keal_panic("integer overflow in `sum`", NULL, at);
            return 0;
        }
        total = r;
    }
    return total;
}

/* And over Floats, which cannot overflow: a running total added front to
 * back, so the rounding is the interpreters' rounding, element for
 * element. */
KEAL_FN double keal_list_sum_f64(KealList* l) {
    double total = 0.0;
    for (int64_t i = 0; i < l->len; i++) {
        total += l->data[i].d;
    }
    return total;
}

/* ---- maps ------------------------------------------------------------- */

/* Entries in insertion order, which is the language's iteration order, with
 * keys and values interleaved. Lookup is a linear scan: correct first, and
 * honest about it — a hash table can replace the scan without changing any
 * caller.
 *
 * Keys compare by the comparator fixed at construction: one for word-sized
 * keys (Int, Bool, and Float by bit pattern, as the interpreters key them),
 * one for strings by content. */
typedef struct KealMap {
    keal_rc_t rc;
    int64_t len;
    int64_t cap;
    KealWord* data; /* [key, value, key, value, ...] */
    bool (*key_eq)(KealWord, KealWord);
    void (*release_key)(void*);
    void (*release_val)(void*);
    /* A key type with finitely many values indexes this directly.
     *
     * `domain` is how many values the key type has — 2 for a Bool, 3 for a
     * Comp, one per variant for an enum — and `slot[ordinal]` is where that
     * key's entry sits in `data`, or -1. Lookup stops being a scan and
     * becomes a read, without changing anything a program can observe:
     * `data` still holds the entries in the order they were first set,
     * which is what `keys()` promises.
     *
     * NULL for every other key type, and then nothing below behaves
     * differently from how it did. */
    int64_t* slot;
    int64_t domain;
    /* For every other key type: an open-addressed table from hash to the
     * position of the entry in `data`, holding position + 1 so that zero
     * means empty.
     *
     * The entries themselves are untouched — they stay in the order they
     * were first set, which `keys()` promises — and this only says where to
     * look. The interpreters have had a hash map here since they were
     * written; the native backend walked the entries, so the three engines
     * agreed on every answer and disagreed on what it cost.
     *
     * NULL when `key_hash` is NULL, and then the scan below still works. */
    int64_t* bucket;
    int64_t nbuckets;
    uint64_t (*key_hash)(KealWord);
} KealMap;

/* A word key — an Int, a Bool, an enum's ordinal, a Comp, or a Float's bit
 * pattern. Splitmix64's finaliser: cheap, and it moves every input bit. */
KEAL_FN uint64_t keal_hash_word(KealWord k) {
    uint64_t x = (uint64_t)k.i;
    x ^= x >> 30;
    x *= 0xbf58476d1ce4e5b9ULL;
    x ^= x >> 27;
    x *= 0x94d049bb133111ebULL;
    x ^= x >> 31;
    return x;
}

/* A string key, hashed over its bytes so that two equal strings agree
 * whatever their addresses. FNV-1a. A NULL string is its own hash, which is
 * what a nullable key needs. */
KEAL_FN uint64_t keal_hash_str(KealWord k) {
    KealStr* s = (KealStr*)k.p;
    if (s == NULL) {
        return 0xcbf29ce484222325ULL;
    }
    uint64_t h = 0xcbf29ce484222325ULL;
    for (int64_t i = 0; i < s->len; i++) {
        h ^= (uint64_t)(unsigned char)s->bytes[i];
        h *= 0x100000001b3ULL;
    }
    return h;
}

KEAL_FN bool keal_key_eq_word(KealWord a, KealWord b) {
    return a.i == b.i;
}

KEAL_FN bool keal_key_eq_str(KealWord a, KealWord b) {
    return keal_str_cmp((KealStr*)a.p, (KealStr*)b.p) == 0;
}

KEAL_FN KealMap* keal_map_new(bool (*key_eq)(KealWord, KealWord),
                              uint64_t (*key_hash)(KealWord),
                              void (*release_key)(void*), void (*release_val)(void*)) {
    KealMap* m = (KealMap*)keal_alloc(sizeof(KealMap));
    m->rc = 1;
    m->len = 0;
    m->cap = 0;
    m->data = NULL;
    m->key_eq = key_eq;
    m->release_key = release_key;
    m->release_val = release_val;
    m->slot = NULL;
    m->domain = 0;
    m->bucket = NULL;
    m->nbuckets = 0;
    m->key_hash = key_hash;
    return m;
}

/* The same map, with an ordinal index because the key type has finitely
 * many values. `domain` is how many: 2 for a Bool, 3 for a Comp, one per
 * variant for an enum. Nothing a program can observe differs — the entries
 * still sit in the order they were first set — only the cost of finding
 * one, which stops depending on how many there are. */
KEAL_FN KealMap* keal_map_new_closed(bool (*key_eq)(KealWord, KealWord),
                                     uint64_t (*key_hash)(KealWord),
                                     void (*release_key)(void*),
                                     void (*release_val)(void*), int64_t domain) {
    KealMap* m = keal_map_new(key_eq, key_hash, release_key, release_val);
    if (domain <= 0) {
        /* Not a closed key after all — a plain map, so that a caller which
         * passes another map's domain along does the right thing either
         * way. */
        return m;
    }
    m->domain = domain;
    m->slot = (int64_t*)keal_alloc((size_t)domain * sizeof(int64_t));
    for (int64_t i = 0; i < domain; i++) {
        m->slot[i] = -1;
    }
    return m;
}

KEAL_FN KealMap* keal_map_retain(KealMap* m) {
    if (m != NULL) {
        KEAL_RC_BUMP(m->rc);
    }
    return m;
}

KEAL_FN void keal_map_release(KealMap* m) {
    if (m == NULL) {
        return;
    }
    if (KEAL_RC_DROP(m->rc)) {
        return;
    }
    for (int64_t i = 0; i < m->len; i++) {
        if (m->release_key != NULL) {
            m->release_key(m->data[2 * i].p);
        }
        if (m->release_val != NULL) {
            m->release_val(m->data[2 * i + 1].p);
        }
    }
    free(m->data);
    free(m->slot);
    free(m->bucket);
    free(m);
}

/* The entry's index, or -1. */
KEAL_FN int64_t keal_map_find(KealMap* m, KealWord key) {
    if (m->slot != NULL) {
        if (key.i < 0 || key.i >= m->domain) {
            return -1;
        }
        return m->slot[key.i];
    }
    if (m->bucket != NULL) {
        uint64_t mask = (uint64_t)m->nbuckets - 1;
        uint64_t i = m->key_hash(key) & mask;
        while (m->bucket[i] != 0) {
            int64_t at = m->bucket[i] - 1;
            /* The hash says where to look; equality says whether it is the
             * one. Two keys may land in the same bucket and still differ. */
            if (m->key_eq(m->data[2 * at], key)) {
                return at;
            }
            i = (i + 1) & mask;
        }
        return -1;
    }
    for (int64_t i = 0; i < m->len; i++) {
        if (m->key_eq(m->data[2 * i], key)) {
            return i;
        }
    }
    return -1;
}

/* Puts one entry into the bucket table. The table always has a free slot
 * because it is grown before it fills. */
KEAL_FN void keal_map_bucket_put(KealMap* m, KealWord key, int64_t at) {
    uint64_t mask = (uint64_t)m->nbuckets - 1;
    uint64_t i = m->key_hash(key) & mask;
    while (m->bucket[i] != 0) {
        i = (i + 1) & mask;
    }
    m->bucket[i] = at + 1;
}

/* Grows the bucket table to hold `want` entries at half load, and refills it
 * from the entries. Also the way it is built the first time. */
KEAL_FN void keal_map_rehash(KealMap* m, int64_t want) {
    int64_t n = 8;
    while (n < want * 2) {
        n *= 2;
    }
    free(m->bucket);
    m->nbuckets = n;
    m->bucket = (int64_t*)keal_alloc((size_t)n * sizeof(int64_t));
    for (int64_t i = 0; i < n; i++) {
        m->bucket[i] = 0;
    }
    for (int64_t i = 0; i < m->len; i++) {
        keal_map_bucket_put(m, m->data[2 * i], i);
    }
}

/* Rebuilds whichever index this map has, after the entries move. Only a
 * removal moves them, and only by shifting the tail down one — which
 * changes the position of every entry after it, so both kinds of index have
 * to be rebuilt rather than patched. */
KEAL_FN void keal_map_reindex(KealMap* m) {
    if (m->slot != NULL) {
        for (int64_t i = 0; i < m->domain; i++) {
            m->slot[i] = -1;
        }
        for (int64_t i = 0; i < m->len; i++) {
            m->slot[m->data[2 * i].i] = i;
        }
        return;
    }
    if (m->bucket != NULL) {
        keal_map_rehash(m, m->len);
    }
}

/* Takes ownership of both words; on a replaced entry the displaced key and
 * value are released here, since the map is the only one who knows them. */
KEAL_FN void keal_map_set(KealMap* m, KealWord key, KealWord value) {
    int64_t at = keal_map_find(m, key);
    if (at >= 0) {
        if (m->release_key != NULL) {
            m->release_key(m->data[2 * at].p);
        }
        if (m->release_val != NULL) {
            m->release_val(m->data[2 * at + 1].p);
        }
        m->data[2 * at] = key;
        m->data[2 * at + 1] = value;
        return;
    }
    if (m->len == m->cap) {
        m->cap = m->cap < 4 ? 4 : m->cap * 2;
        KealWord* grown = (KealWord*)keal_alloc((size_t)m->cap * 2 * sizeof(KealWord));
        memcpy(grown, m->data, (size_t)m->len * 2 * sizeof(KealWord));
        free(m->data);
        m->data = grown;
    }
    m->data[2 * m->len] = key;
    m->data[2 * m->len + 1] = value;
    if (m->slot != NULL) {
        m->slot[key.i] = m->len;
    } else if (m->key_hash != NULL) {
        /* Half load, so a probe stays short. */
        if (m->bucket == NULL || (m->len + 1) * 2 > m->nbuckets) {
            keal_map_rehash(m, m->len + 1);
        }
        keal_map_bucket_put(m, key, m->len);
    }
    m->len++;
}

/* Removes an entry, keeping the order of the ones around it: a map here
 * remembers the order its keys were first set, and `keys()` promises it, so
 * a removal cannot be the usual swap-with-the-last. */
KEAL_FN void keal_map_remove(KealMap* m, KealWord key) {
    int64_t at = keal_map_find(m, key);
    if (at < 0) {
        return;
    }
    if (m->release_key != NULL) {
        m->release_key(m->data[2 * at].p);
    }
    if (m->release_val != NULL) {
        m->release_val(m->data[2 * at + 1].p);
    }
    memmove(m->data + 2 * at, m->data + 2 * (at + 1),
            (size_t)(m->len - at - 1) * 2 * sizeof(KealWord));
    m->len--;
    keal_map_reindex(m);
}

/* Structural map equality, as the interpreters compare maps: same size, and
 * every entry of `a` present in `b` with an equal value — insertion order
 * does not matter. Keys compare by the map's own comparator. */
/* `m.clear()` — every key and every value let go by the releasers fixed at
 * construction, the way the map's own death lets them go. The buffer stays:
 * capacity is not something a program can observe. */
KEAL_FN void keal_map_clear(KealMap* m) {
    for (int64_t i = 0; i < m->len; i++) {
        if (m->release_key != NULL) {
            m->release_key(m->data[2 * i].p);
        }
        if (m->release_val != NULL) {
            m->release_val(m->data[2 * i + 1].p);
        }
    }
    m->len = 0;
    /* The entries are gone, so the index that says where they were has to
     * go with them. It did not, and the result was not a slow map but a
     * stuck one: `clear` left every bucket holding the position of an entry
     * that no longer counted, the next fill probed past all of them, and
     * the fill after that met a table with no free slot at all and looped
     * for ever. Two clears were needed to reach it, which is why a corpus
     * that fills a map and reads it never went near.
     *
     * `remove` already did this, because removing shifts the tail and moves
     * every position after it. Emptying the map moves them all. */
    keal_map_reindex(m);
}

KEAL_FN bool keal_map_eq(KealMap* a, KealMap* b, bool (*val_eq)(KealWord, KealWord)) {
    if (a == NULL || b == NULL || a == b) {
        return a == b;
    }
    if (a->len != b->len) {
        return false;
    }
    for (int64_t i = 0; i < a->len; i++) {
        int64_t at = keal_map_find(b, a->data[2 * i]);
        if (at < 0) {
            return false;
        }
        if (!val_eq(a->data[2 * i + 1], b->data[2 * at + 1])) {
            return false;
        }
    }
    return true;
}

/* A snapshot of the keys, for iteration; borrows, like the list snapshot. */
KEAL_FN KealList* keal_map_keys_snapshot(KealMap* m) {
    KealList* l = keal_list_new(NULL);
    for (int64_t i = 0; i < m->len; i++) {
        keal_list_push(l, m->data[2 * i]);
    }
    return l;
}

/* ---- strings as lists -------------------------------------------------- */

KEAL_FN void rel_keal_str_thunk(void* p);

/* One string per character, as `chars()` and `for (c in s)` see them. */
KEAL_FN KealList* keal_str_chars(KealStr* s) {
    KealList* l = keal_list_new(rel_keal_str_thunk);
    int64_t i = 0;
    while (i < s->len) {
        int64_t j = i + 1;
        while (j < s->len && ((unsigned char)s->bytes[j] & 0xC0) == 0x80) {
            j++;
        }
        keal_list_push(l, (KealWord){ .p = keal_str_from_bytes(s->bytes + i, j - i) });
        i = j;
    }
    return l;
}

/* An empty separator splits into characters; otherwise the separator's
 * occurrences bound the parts, empty parts included — Rust's `split`. */
KEAL_FN KealList* keal_str_split(KealStr* s, KealStr* sep) {
    if (sep->len == 0) {
        return keal_str_chars(s);
    }
    KealList* l = keal_list_new(rel_keal_str_thunk);
    int64_t start = 0;
    int64_t i = 0;
    while (i + sep->len <= s->len) {
        if (memcmp(s->bytes + i, sep->bytes, (size_t)sep->len) == 0) {
            keal_list_push(l, (KealWord){ .p = keal_str_from_bytes(s->bytes + start, i - start) });
            i += sep->len;
            start = i;
        } else {
            i++;
        }
    }
    keal_list_push(l, (KealWord){ .p = keal_str_from_bytes(s->bytes + start, s->len - start) });
    return l;
}

/* `join` over strings; the default separator is the caller's business. */
KEAL_FN KealStr* keal_list_join_str(KealList* l, KealStr* sep) {
    int64_t total = 0;
    for (int64_t i = 0; i < l->len; i++) {
        total += ((KealStr*)l->data[i].p)->len;
    }
    if (l->len > 1) {
        total += sep->len * (l->len - 1);
    }
    char* out = (char*)keal_alloc((size_t)(total < 1 ? 1 : total));
    int64_t at = 0;
    for (int64_t i = 0; i < l->len; i++) {
        if (i > 0) {
            memcpy(out + at, sep->bytes, (size_t)sep->len);
            at += sep->len;
        }
        KealStr* part = (KealStr*)l->data[i].p;
        memcpy(out + at, part->bytes, (size_t)part->len);
        at += part->len;
    }
    KealStr* r = keal_str_from_bytes(out, total);
    free(out);
    return r;
}

/* ---- closures --------------------------------------------------------- */

/* A function value: the count, the code, and how to drop the environment.
 * Each lambda's captures follow this header in a struct of its own; the call
 * site casts `fn` to the signature the static type promises. */
typedef void (*KealCode)(void);

typedef struct KealClosure {
    keal_rc_t rc;
    KealCode fn;
    void (*drop)(struct KealClosure* self);
    /* A fresh closure whose captured values are deep copies — the spawn
     * semantics: an actor's state is its own. NULL when the captures do
     * not copy, which the checker keeps unreachable through `spawn`. */
    struct KealClosure* (*copy)(struct KealClosure* self);
} KealClosure;

KEAL_FN KealClosure* keal_fn_copy_captures(KealClosure* c) {
    if (c == NULL || c->copy == NULL) {
        keal_panic("this handler's captures do not copy", NULL, NULL);
        return NULL;
    }
    return c->copy(c);
}

KEAL_FN KealClosure* keal_fn_retain(KealClosure* c) {
    if (c != NULL) {
        KEAL_RC_BUMP(c->rc);
    }
    return c;
}

KEAL_FN void keal_fn_release(KealClosure* c) {
    if (c == NULL) {
        return;
    }
    if (KEAL_RC_DROP(c->rc)) {
        return;
    }
    c->drop(c);
}

/* ---- building strings ------------------------------------------------- */

/* A growable buffer, used by the rendering functions the compiler generates
 * for each class. Concatenating with `keal_concat` would allocate once per
 * field; this allocates once per object. */
typedef struct KealBuf {
    char* data;
    int64_t len;
    int64_t cap;
} KealBuf;

KEAL_FN void keal_buf_init(KealBuf* b) {
    b->cap = 32;
    b->len = 0;
    b->data = (char*)keal_alloc((size_t)b->cap);
}

KEAL_FN void keal_buf_reserve(KealBuf* b, int64_t extra) {
    if (b->len + extra <= b->cap) {
        return;
    }
    while (b->cap < b->len + extra) {
        b->cap *= 2;
    }
    char* grown = (char*)keal_alloc((size_t)b->cap);
    memcpy(grown, b->data, (size_t)b->len);
    free(b->data);
    b->data = grown;
}

KEAL_FN void keal_buf_bytes(KealBuf* b, const char* s, int64_t n) {
    keal_buf_reserve(b, n);
    memcpy(b->data + b->len, s, (size_t)n);
    b->len += n;
}

KEAL_FN void keal_buf_lit(KealBuf* b, const char* s) {
    keal_buf_bytes(b, s, (int64_t)strlen(s));
}

/* Takes a reference and consumes it, which is what the generated rendering
 * code wants: it makes a string, appends it, and is done with it. */
KEAL_FN void keal_buf_str(KealBuf* b, KealStr* s) {
    keal_buf_bytes(b, s->bytes, s->len);
    keal_str_release(s);
}

KEAL_FN KealStr* keal_buf_finish(KealBuf* b) {
    keal_buf_reserve(b, 1);
    b->data[b->len] = '\0';
    return keal_str_owning(b->data, b->len);
}

/* How a string appears *inside* a rendered value: quoted and escaped, so
 * that `["a", "b"]` reads differently from `[a, b]`. Matches what the
 * interpreters print. */
KEAL_FN KealStr* keal_str_repr(KealStr* s) {
    KealBuf b;
    keal_buf_init(&b);
    keal_buf_lit(&b, "\"");
    for (int64_t i = 0; i < s->len; i++) {
        char c = s->bytes[i];
        switch (c) {
            case '"': keal_buf_lit(&b, "\\\""); break;
            case '\\': keal_buf_lit(&b, "\\\\"); break;
            case '\n': keal_buf_lit(&b, "\\n"); break;
            case '\t': keal_buf_lit(&b, "\\t"); break;
            case '\r': keal_buf_lit(&b, "\\r"); break;
            default: keal_buf_bytes(&b, &c, 1); break;
        }
    }
    keal_buf_lit(&b, "\"");
    keal_str_release(s);
    return keal_buf_finish(&b);
}

/* ---- Any --------------------------------------------------------------- */

/* A value whose type is not known statically: a tag and a payload, two
 * words in all, exactly as `keal layout` prices it. The tag is a pointer
 * to the type's info — what `is` compares, what `typeOf` names, and what
 * tells retain/release whether the payload owns a reference. NULL tag
 * means the `Any` holds null. Only same-layout values enter an `Any`
 * natively (the backend refuses the rest by name), so what the tag says
 * is always what the payload is. */
typedef struct KealTypeInfo {
    const char* name;
    void (*retain)(void* p);
    void (*release)(void* p);
    KealStr* (*show)(KealWord w);
    bool (*eq)(KealWord a, KealWord b);
} KealTypeInfo;

typedef struct KealAny {
    const KealTypeInfo* ti;
    KealWord w;
} KealAny;

/* A list or map element slot is one word, and an `Any` is two — so inside
 * a container an `Any` lives behind one counted pointer. */
typedef struct KealAnyBox {
    keal_rc_t rc;
    KealAny a;
} KealAnyBox;

KEAL_FN KealAny keal_any_retain(KealAny a) {
    if (a.ti != NULL && a.ti->retain != NULL) {
        a.ti->retain(a.w.p);
    }
    return a;
}

KEAL_FN void keal_any_release(KealAny a) {
    if (a.ti != NULL && a.ti->release != NULL) {
        a.ti->release(a.w.p);
    }
}

KEAL_FN KealAny keal_any_null(void) {
    KealAny a;
    a.ti = NULL;
    a.w.i = 0;
    return a;
}

/* A pointer payload whose value may be null: a null pointer in, null out. */
KEAL_FN KealAny keal_any_of_ptr(const KealTypeInfo* ti, void* p) {
    if (p == NULL) {
        return keal_any_null();
    }
    KealAny a;
    a.ti = ti;
    a.w.p = p;
    return a;
}

KEAL_FN void* keal_any_box(KealAny a) {
    KealAnyBox* b = (KealAnyBox*)keal_alloc(sizeof(KealAnyBox));
    b->rc = 1;
    b->a = a;
    return b;
}

KEAL_FN void keal_any_box_release(void* p) {
    KealAnyBox* b = (KealAnyBox*)p;
    if (b == NULL) {
        return;
    }
    if (KEAL_RC_DROP(b->rc)) {
        return;
    }
    keal_any_release(b->a);
    free(b);
}

/* Equality the way the interpreters' values_equal answers it: same tag,
 * then by structure for data and by identity for instances and boxes'
 * contents follow the same rule. Two nulls are equal. */
KEAL_FN bool keal_any_eq(KealAny a, KealAny b) {
    if (a.ti != b.ti) {
        return false;
    }
    if (a.ti == NULL) {
        return true;
    }
    return a.ti->eq(a.w, b.w);
}

KEAL_FN bool keal_any_box_eq(KealWord a, KealWord b) {
    return keal_any_eq(((KealAnyBox*)a.p)->a, ((KealAnyBox*)b.p)->a);
}

/* Rendering inside a container: strings quoted, like the interpreters. */
KEAL_FN KealStr* keal_any_repr(KealAny a) {
    if (a.ti == NULL) {
        return keal_str_static("null", 4);
    }
    return a.ti->show(a.w);
}

/* What `println` and `${...}` produce: a bare string stays bare. */
KEAL_FN KealStr* keal_any_display(KealAny a);

KEAL_FN KealStr* keal_any_type_name(KealAny a) {
    if (a.ti == NULL) {
        return keal_str_static("Null", 4);
    }
    return keal_str_static(a.ti->name, (int64_t)strlen(a.ti->name));
}

static void keal_any_ti_retain_str(void* p) { keal_str_retain((KealStr*)p); }
static void keal_any_ti_release_str(void* p) { keal_str_release((KealStr*)p); }
static KealStr* keal_any_ti_show_int(KealWord w) { return keal_str_from_int(w.i); }
static KealStr* keal_any_ti_show_float(KealWord w) { return keal_str_from_float(w.d); }
static KealStr* keal_any_ti_show_bool(KealWord w) { return keal_str_from_bool((bool)w.i); }
static KealStr* keal_any_ti_show_str(KealWord w) {
    return keal_str_repr(keal_str_retain((KealStr*)w.p));
}
static bool keal_any_ti_eq_word(KealWord a, KealWord b) { return a.i == b.i; }
static bool keal_any_ti_eq_float(KealWord a, KealWord b) { return a.d == b.d; }
static bool keal_any_ti_eq_str(KealWord a, KealWord b) {
    return keal_str_cmp((KealStr*)a.p, (KealStr*)b.p) == 0;
}
/* Instances compare by identity inside an `Any`, exactly as the
 * interpreters compare dynamic values; the structural `==` a record
 * enjoys is the checker's rewrite to `equals`, which `Any` never takes. */
KEAL_FN bool keal_any_ptr_eq(KealWord a, KealWord b) { return a.p == b.p; }

KEAL_VAL const KealTypeInfo keal_ti_int = {
    "Int", NULL, NULL, keal_any_ti_show_int, keal_any_ti_eq_word
};
KEAL_VAL const KealTypeInfo keal_ti_float = {
    "Float", NULL, NULL, keal_any_ti_show_float, keal_any_ti_eq_float
};
KEAL_VAL const KealTypeInfo keal_ti_bool = {
    "Bool", NULL, NULL, keal_any_ti_show_bool, keal_any_ti_eq_word
};
KEAL_VAL const KealTypeInfo keal_ti_str = {
    "String", keal_any_ti_retain_str, keal_any_ti_release_str,
    keal_any_ti_show_str, keal_any_ti_eq_str
};

/* The one list that fits in an `Any` is `List<Any>` — same layout seen
 * from every side, which is what keeps `is List` honest natively. */
static void keal_any_ti_retain_list(void* p) { keal_list_retain((KealList*)p); }
static void keal_any_ti_release_list(void* p) { keal_list_release((KealList*)p); }
static KealStr* keal_any_ti_show_list(KealWord w) {
    KealList* l = (KealList*)w.p;
    KealBuf b;
    keal_buf_init(&b);
    keal_buf_lit(&b, "[");
    for (int64_t i = 0; i < l->len; i++) {
        if (i > 0) {
            keal_buf_lit(&b, ", ");
        }
        keal_buf_str(&b, keal_any_repr(((KealAnyBox*)l->data[i].p)->a));
    }
    keal_buf_lit(&b, "]");
    return keal_buf_finish(&b);
}
static bool keal_any_ti_eq_list(KealWord a, KealWord b) {
    return keal_list_eq((KealList*)a.p, (KealList*)b.p, keal_any_box_eq);
}
KEAL_VAL const KealTypeInfo keal_ti_list = {
    "List", keal_any_ti_retain_list, keal_any_ti_release_list,
    keal_any_ti_show_list, keal_any_ti_eq_list
};

KEAL_FN KealStr* keal_any_display(KealAny a) {
    if (a.ti == NULL) {
        return keal_str_static("null", 4);
    }
    if (a.ti == &keal_ti_str) {
        return keal_str_retain((KealStr*)a.w.p);
    }
    return a.ti->show(a.w);
}

/* Comparing two strings either of which may be absent. Two absent strings
 * are equal; an absent one equals nothing else. */
KEAL_FN bool keal_opt_str_eq(KealStr* a, KealStr* b) {
    if (a == NULL || b == NULL) {
        return a == b;
    }
    return keal_str_cmp(a, b) == 0;
}

/* ---- nullable values -------------------------------------------------- */

/* `Int?` and `Float?`: a tag beside the value, exactly as `keal layout`
 * prices them. `Bool?` instead uses a spare pattern of its byte — 0, 1, and
 * 2 for null — which is the layout table's promise kept. */
typedef struct KealOptI64 {
    bool has;
    int64_t v;
} KealOptI64;

typedef struct KealOptF64 {
    bool has;
    double v;
} KealOptF64;

/* Tagged optionals flowing into an `Any`: present becomes the value's own
 * tag, absent becomes the null `Any` — one more case the tag had free. */
KEAL_FN KealAny keal_any_of_opt_i64(KealOptI64 o) {
    KealAny a;
    if (!o.has) {
        return keal_any_null();
    }
    a.ti = &keal_ti_int;
    a.w.i = o.v;
    return a;
}

KEAL_FN KealAny keal_any_of_opt_f64(KealOptF64 o) {
    KealAny a;
    if (!o.has) {
        return keal_any_null();
    }
    a.ti = &keal_ti_float;
    a.w.d = o.v;
    return a;
}

KEAL_FN KealAny keal_any_of_opt_bool(int8_t o) {
    KealAny a;
    if (o == 2) {
        return keal_any_null();
    }
    a.ti = &keal_ti_bool;
    a.w.i = (int64_t)o;
    return a;
}

#ifdef KEAL_AUDIT
/* ---- the audit's mark phase ------------------------------------------- */

/* The rule the counts alone could not give, and the same rule both
 * interpreters apply — it has to be the same, or a program's answer would
 * depend on which engine asked. A top-level binding lives to the end of a
 * program by design, and so does everything it holds: all of that is
 * reachable from a root. Anything alive that no root reaches outlived its
 * last reference, which nothing but a cycle can do.
 *
 * The walk follows only the edges that hold an object up. A `weak` field
 * holds nothing up, so the generated walk skips one: were it followed, the
 * back edge of a cycle would report its own cycle as reachable, which is
 * exactly the answer this exists to stop giving.
 *
 * None of it costs a program anything: it is compiled only under
 * `keal build --audit`. */

typedef void (*KealAuditMark)(void*);

/* A shape is walked by pairing its release function — which the backend
 * already emits, and already hands to every list, map and closure — with
 * the mark that walks the same fields. Both come from one place in the
 * backend, so the two cannot drift apart, and no runtime structure had to
 * grow a field to carry the second one. */
typedef struct KealAuditPair {
    void* rel;
    KealAuditMark mark;
} KealAuditPair;

static KealAuditPair keal_audit_pairs[512];
static int keal_audit_pairs_used = 0;

KEAL_FN void keal_audit_pair(void* rel, KealAuditMark mark) {
    if (rel == NULL) { return; }
    for (int i = 0; i < keal_audit_pairs_used; i++) {
        if (keal_audit_pairs[i].rel == rel) { return; }
    }
    if (keal_audit_pairs_used == (int)(sizeof keal_audit_pairs / sizeof keal_audit_pairs[0])) {
        keal_fatal("audit: too many shapes to walk");
    }
    keal_audit_pairs[keal_audit_pairs_used].rel = rel;
    keal_audit_pairs[keal_audit_pairs_used].mark = mark;
    keal_audit_pairs_used++;
}

static KealAuditMark keal_audit_mark_for(void* rel) {
    for (int i = 0; i < keal_audit_pairs_used; i++) {
        if (keal_audit_pairs[i].rel == rel) { return keal_audit_pairs[i].mark; }
    }
    /* Silence here would under-count, and under-counting reports a cycle
     * that is not one. An audit that cannot walk a shape says so. */
    keal_fatal("audit: a shape the mark phase does not know how to walk");
    return NULL;
}

/* Every object is visited once, which is what makes a walk over the very
 * cycles it is looking for terminate. */
static void** keal_audit_seen = NULL;
static size_t keal_audit_seen_len = 0;
static size_t keal_audit_seen_cap = 0;

static bool keal_audit_first(void* p) {
    for (size_t i = 0; i < keal_audit_seen_len; i++) {
        if (keal_audit_seen[i] == p) { return false; }
    }
    if (keal_audit_seen_len == keal_audit_seen_cap) {
        size_t cap = keal_audit_seen_cap == 0 ? 64 : keal_audit_seen_cap * 2;
        void** grown = (void**)realloc(keal_audit_seen, cap * sizeof(void*));
        if (grown == NULL) { keal_fatal("out of memory"); }
        keal_audit_seen = grown;
        keal_audit_seen_cap = cap;
    }
    keal_audit_seen[keal_audit_seen_len++] = p;
    return true;
}

/* The mark phase's own scratch, freed once it has answered — an audit that
 * leaked would be the last thing to leak. */
KEAL_FN void keal_audit_done(void) {
    free(keal_audit_seen);
    keal_audit_seen = NULL;
    keal_audit_seen_len = 0;
    keal_audit_seen_cap = 0;
}

/* A string holds nothing, and neither does a scalar. */
KEAL_FN void keal_str_mark(void* p) { (void)p; }

KEAL_FN void keal_any_mark(KealAny a);

KEAL_FN void keal_list_mark(void* p) {
    KealList* l = (KealList*)p;
    if (l == NULL || !keal_audit_first(l)) { return; }
    /* No element releaser means the elements are scalars, which hold
     * nothing — the same test the release path makes. */
    if (l->release_elem == NULL) { return; }
    KealAuditMark m = keal_audit_mark_for((void*)l->release_elem);
    for (int64_t i = 0; i < l->len; i++) { m(l->data[i].p); }
}

KEAL_FN void keal_map_mark(void* p) {
    KealMap* m = (KealMap*)p;
    if (m == NULL || !keal_audit_first(m)) { return; }
    KealAuditMark mk = m->release_key == NULL ? NULL : keal_audit_mark_for((void*)m->release_key);
    KealAuditMark mv = m->release_val == NULL ? NULL : keal_audit_mark_for((void*)m->release_val);
    for (int64_t i = 0; i < m->len; i++) {
        if (mk != NULL) { mk(m->data[i * 2].p); }
        if (mv != NULL) { mv(m->data[i * 2 + 1].p); }
    }
}

/* A closure's captures live in a struct only its own code knows the shape
 * of, so its walk is paired with its `drop` — the one function the header
 * already carries that names that shape. */
KEAL_FN void keal_fn_mark(void* p) {
    KealClosure* c = (KealClosure*)p;
    if (c == NULL || !keal_audit_first(c)) { return; }
    keal_audit_mark_for((void*)c->drop)(c);
}

KEAL_FN void keal_any_mark(KealAny a) {
    if (a.ti == NULL || a.ti->release == NULL) { return; }
    keal_audit_mark_for((void*)a.ti->release)(a.w.p);
}

KEAL_FN void keal_any_box_mark(void* p) {
    KealAnyBox* b = (KealAnyBox*)p;
    if (b == NULL || !keal_audit_first(b)) { return; }
    keal_any_mark(b->a);
}

#endif

/* `String.toInt`: trimmed, an optional sign, digits, nothing else, and no
 * overflow — the grammar Rust's `i64::from_str` accepts, which is what the
 * interpreters run. */
KEAL_FN KealOptI64 keal_str_to_int(KealStr* s) {
    KealOptI64 none = { false, 0 };
    int64_t a = 0;
    int64_t b = s->len;
    while (a < b && (s->bytes[a] == ' ' || s->bytes[a] == '\t' || s->bytes[a] == '\n' || s->bytes[a] == '\r')) {
        a++;
    }
    while (b > a && (s->bytes[b - 1] == ' ' || s->bytes[b - 1] == '\t' || s->bytes[b - 1] == '\n' || s->bytes[b - 1] == '\r')) {
        b--;
    }
    if (a >= b) {
        return none;
    }
    bool neg = false;
    if (s->bytes[a] == '+' || s->bytes[a] == '-') {
        neg = s->bytes[a] == '-';
        a++;
    }
    if (a >= b) {
        return none;
    }
    int64_t value = 0;
    for (int64_t i = a; i < b; i++) {
        char c = s->bytes[i];
        if (c < '0' || c > '9') {
            return none;
        }
        if (__builtin_mul_overflow(value, 10, &value)) {
            return none;
        }
        int64_t d = neg ? -(c - '0') : (c - '0');
        if (__builtin_add_overflow(value, d, &value)) {
            return none;
        }
    }
    KealOptI64 some = { true, value };
    return some;
}

/* `String.toFloat`: validated against the grammar Rust's `f64::from_str`
 * accepts — no hex floats, at least one digit — then read by `strtod`. */
KEAL_FN KealOptF64 keal_str_to_float(KealStr* s) {
    KealOptF64 none = { false, 0.0 };
    int64_t a = 0;
    int64_t b = s->len;
    while (a < b && (s->bytes[a] == ' ' || s->bytes[a] == '\t' || s->bytes[a] == '\n' || s->bytes[a] == '\r')) {
        a++;
    }
    while (b > a && (s->bytes[b - 1] == ' ' || s->bytes[b - 1] == '\t' || s->bytes[b - 1] == '\n' || s->bytes[b - 1] == '\r')) {
        b--;
    }
    if (a >= b || b - a >= 512) {
        return none;
    }
    char buf[512];
    memcpy(buf, s->bytes + a, (size_t)(b - a));
    buf[b - a] = '\0';
    /* Validate: [+-]? (inf | infinity | nan | digits[.digits][e[+-]digits]
     * with a digit somewhere and no hex). */
    const char* p = buf;
    if (*p == '+' || *p == '-') {
        p++;
    }
    bool word = strcmp(p, "inf") == 0 || strcmp(p, "infinity") == 0 || strcmp(p, "nan") == 0
        || strcmp(p, "NaN") == 0 || strcmp(p, "Inf") == 0;
    if (!word) {
        bool digit = false;
        bool dot = false;
        bool exp = false;
        for (const char* q = p; *q; q++) {
            char c = *q;
            if (c >= '0' && c <= '9') {
                digit = true;
            } else if (c == '.' && !dot && !exp) {
                dot = true;
            } else if ((c == 'e' || c == 'E') && !exp && digit) {
                exp = true;
                if (q[1] == '+' || q[1] == '-') {
                    q++;
                }
                if (q[1] < '0' || q[1] > '9') {
                    return none;
                }
            } else {
                return none;
            }
        }
        if (!digit) {
            return none;
        }
    }
    char* end = NULL;
    double v = strtod(buf, &end);
    if (end == NULL || *end != '\0') {
        return none;
    }
    KealOptF64 some = { true, v };
    return some;
}

/* ---- cells ------------------------------------------------------------ */

/* A mutable variable some closure captures. The frame and every closure
 * share the one cell, so an assignment through any of them is seen by all —
 * the interpreters' semantics, kept rather than approximated. */
typedef struct KealCell {
    keal_rc_t rc;
    KealWord w;
    void (*release_inner)(void*);
} KealCell;

KEAL_FN KealCell* keal_cell_new(void (*release_inner)(void*)) {
    KealCell* c = (KealCell*)keal_alloc(sizeof(KealCell));
    c->rc = 1;
    c->w.i = 0;
    c->release_inner = release_inner;
    return c;
}

KEAL_FN KealCell* keal_cell_retain(KealCell* c) {
    if (c != NULL) {
        KEAL_RC_BUMP(c->rc);
    }
    return c;
}

#ifdef KEAL_AUDIT
/* A captured `var` lives in a shared cell, so the walk goes through one. */
KEAL_FN void keal_cell_mark(void* p) {
    KealCell* c = (KealCell*)p;
    if (c == NULL || !keal_audit_first(c)) { return; }
    if (c->release_inner == NULL) { return; }
    keal_audit_mark_for((void*)c->release_inner)(c->w.p);
}
#endif

KEAL_FN void keal_cell_release(KealCell* c) {
    if (c == NULL) {
        return;
    }
    if (KEAL_RC_DROP(c->rc)) {
        return;
    }
    if (c->release_inner != NULL) {
        c->release_inner(c->w.p);
    }
    free(c);
}

#ifdef KEAL_AUDIT
/* The shapes the runtime owns, paired once. What a program adds — one per
 * class, one per lambda, one per element type a container was made for —
 * the backend pairs beside the release it already emits, so a shape and
 * the walk over it are written in the same place and cannot drift. */
KEAL_FN void keal_audit_pair_runtime(void) {
    keal_audit_pair((void*)keal_any_box_release, keal_any_box_mark);
    keal_audit_pair((void*)keal_any_ti_release_str, keal_str_mark);
    keal_audit_pair((void*)keal_any_ti_release_list, keal_list_mark);
    keal_audit_pair((void*)keal_cell_release, keal_cell_mark);
    keal_audit_pair((void*)keal_str_release, keal_str_mark);
    keal_audit_pair((void*)keal_list_release, keal_list_mark);
    keal_audit_pair((void*)keal_map_release, keal_map_mark);
    keal_audit_pair((void*)keal_fn_release, keal_fn_mark);
}
#endif


/* ---- the host --------------------------------------------------------- */

/* Filled by main before anything runs; what `args()` returns. */
static int keal_argc = 0;
static char** keal_argv = NULL;

KEAL_FN void rel_keal_str_thunk(void* p) { keal_str_release((KealStr*)p); }

KEAL_FN KealList* keal_args(void) {
    KealList* l = keal_list_new(rel_keal_str_thunk);
    for (int i = 0; i < keal_argc; i++) {
        keal_list_push(l, (KealWord){ .p = keal_str_from_bytes(keal_argv[i],
                                                              (int64_t)strlen(keal_argv[i])) });
    }
    return l;
}

#ifdef _WIN32
/* A Keal string is UTF-8, and the ANSI entry points read it as the active
 * code page. That is not merely lossy: it is SELF-CONSISTENT, because the
 * same wrong conversion happens on the way out again — a program that makes
 * a directory called `日本` and lists it back sees `日本`, while what is on
 * disk is `æ—¥æœ¬` and no other tool on the machine can open it. Worse, a
 * file some other program created cannot be seen at all: `isDir` answers
 * false about a directory that exists, and `listDir` returns question marks
 * where the code page has no room. Nothing written in Keal can detect any of
 * this, because every test that makes its own tree agrees with itself.
 *
 * So the file system uses the wide entry points, and these two convert at
 * the boundary. Free the result of `keal_widen_bytes` with `free`. */
static wchar_t* keal_widen_bytes(const char* utf8, int64_t len) {
    int n = MultiByteToWideChar(CP_UTF8, 0, utf8, (int)len, NULL, 0);
    if (n < 0) {
        return NULL;
    }
    wchar_t* w = (wchar_t*)malloc(((size_t)n + 1) * sizeof(wchar_t));
    if (w == NULL) {
        return NULL;
    }
    if (n > 0) {
        MultiByteToWideChar(CP_UTF8, 0, utf8, (int)len, w, n);
    }
    w[n] = L'\0';
    return w;
}

/* A path as the wide entry points want it. NULL on failure, which every
 * caller turns into the same absence a missing file gives. */
static wchar_t* keal_wpath(KealStr* path) {
    if (path->len < 0) {
        return NULL;
    }
    return keal_widen_bytes(path->bytes, path->len);
}

/* And back: a name the file system handed over, as the UTF-8 a Keal string
 * is made of. */
static KealStr* keal_narrow(const wchar_t* w) {
    int n = WideCharToMultiByte(CP_UTF8, 0, w, -1, NULL, 0, NULL, NULL);
    if (n <= 0) {
        return keal_str_from_bytes("", 0);
    }
    char* buf = (char*)malloc((size_t)n);
    if (buf == NULL) {
        return keal_str_from_bytes("", 0);
    }
    WideCharToMultiByte(CP_UTF8, 0, w, -1, buf, n, NULL, NULL);
    KealStr* out = keal_str_from_bytes(buf, (int64_t)(n - 1));
    free(buf);
    return out;
}
#endif

/* A file, opened by the name the program actually gave. On Windows that
 * means the wide entry point: `fopen` reads the name as the active code
 * page, so a file called `été` would be created under another name and a
 * file some other program wrote could not be found at all. */
static FILE* keal_open(KealStr* path, const char* cpath, bool writing) {
#ifdef _WIN32
    (void)cpath;
    wchar_t* wp = keal_wpath(path);
    if (wp == NULL) {
        return NULL;
    }
    FILE* f = _wfopen(wp, writing ? L"wb" : L"rb");
    free(wp);
    return f;
#else
    (void)path;
    return fopen(cpath, writing ? "wb" : "rb");
#endif
}

/* NULL on any failure: the caller's `?:` decides what absence means. */
KEAL_FN KealStr* keal_read_file(KealStr* path) {
    char cpath[4096];
    if (path->len >= (int64_t)sizeof cpath) {
        return NULL;
    }
    memcpy(cpath, path->bytes, (size_t)path->len);
    cpath[path->len] = '\0';
    FILE* f = keal_open(path, cpath, false);
    if (f == NULL) {
        return NULL;
    }
    if (fseek(f, 0, SEEK_END) != 0) {
        fclose(f);
        return NULL;
    }
    long size = ftell(f);
    if (size < 0) {
        fclose(f);
        return NULL;
    }
    rewind(f);
    char* bytes = (char*)keal_alloc((size_t)size + 1);
    size_t got = fread(bytes, 1, (size_t)size, f);
    fclose(f);
    if (got != (size_t)size) {
        free(bytes);
        return NULL;
    }
    bytes[size] = '\0';
    return keal_str_owning(bytes, (int64_t)size);
}

/* `s.reversed()` — by CHARACTER, not by byte. Reversing the bytes of a
 * multi-byte character produces something that is not text at all, and the
 * interpreters reverse `chars()`, so this walks the UTF-8 lead bytes and
 * copies each character whole. Malformed input is copied byte by byte rather
 * than rejected: a Keal string is bytes, and refusing here would make
 * `reversed` the only method that inspects them. */
KEAL_FN KealStr* keal_str_reversed(KealStr* s) {
    char* out = (char*)keal_alloc((size_t)s->len + 1);
    int64_t at = s->len;
    int64_t i = 0;
    while (i < s->len) {
        unsigned char lead = (unsigned char)s->bytes[i];
        int64_t width = 1;
        if (lead >= 0xF0) {
            width = 4;
        } else if (lead >= 0xE0) {
            width = 3;
        } else if (lead >= 0xC0) {
            width = 2;
        }
        if (i + width > s->len) {
            width = 1;
        }
        at -= width;
        memcpy(out + at, s->bytes + i, (size_t)width);
        i += width;
    }
    out[s->len] = '\0';
    return keal_str_owning(out, s->len);
}

KEAL_FN bool keal_write_file(KealStr* path, KealStr* content) {
    char cpath[4096];
    if (path->len >= (int64_t)sizeof cpath) {
        return false;
    }
    memcpy(cpath, path->bytes, (size_t)path->len);
    cpath[path->len] = '\0';
    FILE* f = keal_open(path, cpath, true);
    if (f == NULL) {
        return false;
    }
    size_t wrote = fwrite(content->bytes, 1, (size_t)content->len, f);
    int closed = fclose(f);
    return wrote == (size_t)content->len && closed == 0;
}

/* ---- the clock --------------------------------------------------------- */

/* Seconds since the Unix epoch, with whatever fraction the platform keeps —
 * the same number `SystemTime::now()` gives the two interpreters. UTC, and
 * only UTC: the prelude's calendar is written over this, and a local time
 * would mean reading `struct tm`, whose field order C does not fix. */
KEAL_FN double keal_time(void) {
#ifdef _WIN32
    /* 100-nanosecond ticks since 1601-01-01, which is 11644473600 seconds
     * before the Unix epoch. */
    FILETIME ft;
    GetSystemTimeAsFileTime(&ft);
    unsigned long long ticks =
        ((unsigned long long)ft.dwHighDateTime << 32) | ft.dwLowDateTime;
    return (double)ticks / 10000000.0 - 11644473600.0;
#else
    struct timespec ts;
    if (clock_gettime(CLOCK_REALTIME, &ts) != 0) {
        return 0.0;
    }
    return (double)ts.tv_sec + (double)ts.tv_nsec / 1000000000.0;
#endif
}

/* Seconds east of UTC at a given instant — which depends on where the
 * machine thinks it is and on which side of a daylight-saving change the
 * instant falls, so it takes the instant rather than reading "now".
 *
 * The C standard says `struct tm` contains certain members but not in what
 * order, and the two interpreters call this same pair of functions from Rust
 * where a wrong guess about that order would be silent. So neither side ever
 * reads a field: the pointer goes straight into `strftime`, which prints the
 * offset as `+0200`, and only that string is read. Zero on any failure,
 * which is UTC — a true answer rather than a plausible wrong one. */
KEAL_FN int64_t keal_local_offset(int64_t at) {
    time_t when = (time_t)at;
    struct tm storage;
    struct tm* broken;
#ifdef _WIN32
    if (localtime_s(&storage, &when) != 0) {
        return 0;
    }
    broken = &storage;
#else
    broken = localtime_r(&when, &storage);
    if (broken == NULL) {
        return 0;
    }
#endif
    char text[8];
    if (strftime(text, sizeof text, "%z", broken) != 5) {
        return 0;
    }
    /* The sign is required, not merely allowed: a library answering `00200`
     * would otherwise be read as `+02:00` off the wrong two digits, and one
     * answering `Z` or nothing — which is what `%Z` gives where it has been
     * confused for `%z` — must fall through to UTC rather than to a
     * plausible number. */
    if (text[0] != '+' && text[0] != '-') {
        return 0;
    }
    for (int i = 1; i < 5; i++) {
        if (text[i] < '0' || text[i] > '9') {
            return 0;
        }
    }
    int64_t sign = text[0] == '-' ? -1 : 1;
    int64_t hours = (text[1] - '0') * 10 + (text[2] - '0');
    int64_t minutes = (text[3] - '0') * 10 + (text[4] - '0');
    return sign * (hours * 3600 + minutes * 60);
}

/* ---- numbers, randomness, and a line of input --------------------------- */

/* `abs` on an `Int`. Wraps at INT64_MIN exactly as Rust's `i64::abs` does in
 * a release build, through unsigned arithmetic — signed negation there is
 * undefined in C, and undefined is not a behaviour the three engines can
 * agree on. */
KEAL_FN int64_t keal_abs_i64(int64_t n) {
    return n < 0 ? (int64_t)(0u - (uint64_t)n) : n;
}

/* The interpreters' generator, algorithm for algorithm: xorshift64 with a
 * multiply, seeded from the clock on first use. The numbers differ from run
 * to run because the seed does; the shape of them does not differ between
 * engines, and a fixed seed would give the same sequence on all three. */
static uint64_t keal_rng_state = 0;

KEAL_FN double keal_random(void) {
    if (keal_rng_state == 0) {
        keal_rng_state = (uint64_t)(keal_time() * 1000000000.0) | 1u;
        if (keal_rng_state == 0) {
            keal_rng_state = 0x2545F4914F6CDD1DULL;
        }
    }
    keal_rng_state ^= keal_rng_state >> 12;
    keal_rng_state ^= keal_rng_state << 25;
    keal_rng_state ^= keal_rng_state >> 27;
    uint64_t scaled = (keal_rng_state * 0x2545F4914F6CDD1DULL) >> 11;
    return (double)scaled / (double)(1ULL << 53);
}

/* `randomInt(lo, hi)` — `lo` included, `hi` excluded, and an empty range is
 * the interpreters' error in the interpreters' words. */
KEAL_FN int64_t keal_random_int(int64_t lo, int64_t hi, const char* at) {
    if (hi <= lo) {
        char msg[96];
        snprintf(msg, sizeof msg,
                 "randomInt(%" PRId64 ", %" PRId64 ") has an empty range", lo, hi);
        keal_panic(msg, NULL, at);
        return lo;
    }
    return lo + (int64_t)(keal_random() * (double)(hi - lo));
}

/* One line of standard input, without its ending, and NULL at end of file —
 * so `readLine() ?: ""` is how a program says it does not mind. Both a bare
 * newline and a carriage return before it are stripped, because a file
 * written on one platform is read on another. */
KEAL_FN KealStr* keal_read_line(void) {
    size_t cap = 128;
    size_t len = 0;
    char* buf = (char*)malloc(cap);
    if (buf == NULL) {
        return NULL;
    }
    int c;
    while ((c = fgetc(stdin)) != EOF && c != '\n') {
        if (len + 1 >= cap) {
            size_t grown = cap * 2;
            char* bigger = (char*)realloc(buf, grown);
            if (bigger == NULL) {
                free(buf);
                return NULL;
            }
            buf = bigger;
            cap = grown;
        }
        buf[len++] = (char)c;
    }
    if (c == EOF && len == 0) {
        free(buf);
        return NULL;
    }
    while (len > 0 && buf[len - 1] == '\r') {
        len--;
    }
    KealStr* out = keal_str_from_bytes(buf, (int64_t)len);
    free(buf);
    return out;
}

/* ---- the file system --------------------------------------------------- */

/* Four primitives, and no more: a name in the global namespace is reserved
 * for good and cannot ever be redefined, so only a system call earns one.
 * `exists`, `isFile`, `isDir` and `walkDir` are written over these in the
 * prelude, where a program that wants its own may shadow them. */

/* A path, as C wants it. False when it will not fit, which every caller
 * turns into the same absence a missing file gives. */
static bool keal_cpath(KealStr* path, char* out, size_t cap) {
    if (path->len < 0 || (size_t)path->len >= cap) {
        return false;
    }
    memcpy(out, path->bytes, (size_t)path->len);
    out[path->len] = '\0';
    return true;
}

/* Byte-lexicographic, which is what Rust's `sort` on a `String` is: the
 * three engines have to print one order, and a directory hands its entries
 * out in whatever order its file system pleases. */
static int keal_name_cmp(const void* a, const void* b) {
    KealStr* x = *(KealStr* const*)a;
    KealStr* y = *(KealStr* const*)b;
    int64_t n = x->len < y->len ? x->len : y->len;
    int c = n > 0 ? memcmp(x->bytes, y->bytes, (size_t)n) : 0;
    if (c != 0) {
        return c;
    }
    return x->len < y->len ? -1 : (x->len > y->len ? 1 : 0);
}

/* 0 nothing, 1 a file, 2 a directory. Deliberately an integer: the prelude
 * gives this three names, and this one stays the primitive. */
KEAL_FN int64_t keal_path_kind(KealStr* path) {
#ifdef _WIN32
    wchar_t* wp = keal_wpath(path);
    if (wp == NULL) {
        return 0;
    }
    DWORD attr = GetFileAttributesW(wp);
    free(wp);
    if (attr == INVALID_FILE_ATTRIBUTES) {
        return 0;
    }
    return (attr & FILE_ATTRIBUTE_DIRECTORY) ? 2 : 1;
#else
    char cpath[4096];
    if (!keal_cpath(path, cpath, sizeof cpath)) {
        return 0;
    }
    struct stat st;
    if (stat(cpath, &st) != 0) {
        return 0;
    }
    return S_ISDIR(st.st_mode) ? 2 : 1;
#endif
}

/* The entry names, sorted, without `.` and `..`. NULL when the path is not
 * a directory — the caller's `?:` decides what that means. */
KEAL_FN KealList* keal_list_dir(KealStr* path) {
    KealStr** names = NULL;
    int64_t count = 0;
    int64_t cap = 0;

#ifdef _WIN32
    /* The pattern is built in wide characters too: a directory whose own
     * name is not representable in the code page could not otherwise be
     * opened at all. */
    wchar_t* wp = keal_wpath(path);
    if (wp == NULL) {
        return NULL;
    }
    size_t wlen = wcslen(wp);
    wchar_t* pattern = (wchar_t*)malloc((wlen + 3) * sizeof(wchar_t));
    if (pattern == NULL) {
        free(wp);
        return NULL;
    }
    memcpy(pattern, wp, wlen * sizeof(wchar_t));
    pattern[wlen] = L'\\';
    pattern[wlen + 1] = L'*';
    pattern[wlen + 2] = L'\0';
    free(wp);
    WIN32_FIND_DATAW found;
    HANDLE h = FindFirstFileW(pattern, &found);
    free(pattern);
    if (h == INVALID_HANDLE_VALUE) {
        return NULL;
    }
    do {
        if (wcscmp(found.cFileName, L".") == 0 || wcscmp(found.cFileName, L"..") == 0) {
            continue;
        }
        KealStr* name = keal_narrow(found.cFileName);
#else
    char cpath[4096];
    if (!keal_cpath(path, cpath, sizeof cpath)) {
        return NULL;
    }
    DIR* d = opendir(cpath);
    if (d == NULL) {
        return NULL;
    }
    struct dirent* entry;
    while ((entry = readdir(d)) != NULL) {
        if (strcmp(entry->d_name, ".") == 0 || strcmp(entry->d_name, "..") == 0) {
            continue;
        }
        KealStr* name = keal_str_from_bytes(entry->d_name, (int64_t)strlen(entry->d_name));
#endif
        if (count == cap) {
            int64_t grown = cap == 0 ? 8 : cap * 2;
            KealStr** bigger = (KealStr**)realloc(names, (size_t)grown * sizeof *names);
            if (bigger == NULL) {
                keal_str_release(name);
                break;
            }
            names = bigger;
            cap = grown;
        }
        names[count++] = name;
#ifdef _WIN32
    } while (FindNextFileW(h, &found));
    FindClose(h);
#else
    }
    closedir(d);
#endif

    if (count > 1) {
        qsort(names, (size_t)count, sizeof *names, keal_name_cmp);
    }
    KealList* l = keal_list_new(rel_keal_str_thunk);
    for (int64_t i = 0; i < count; i++) {
        keal_list_push(l, (KealWord){ .p = names[i] });
    }
    free(names);
    return l;
}

/* Makes the directory and every parent it needs, and says true when the
 * directory is there afterwards — so making one twice is not a failure. */
KEAL_FN bool keal_make_dir(KealStr* path) {
#ifdef _WIN32
    /* Walked as wide characters. A separator is one code unit in UTF-16 and
     * cannot appear inside another character, so cutting the string at one
     * is safe in a way that cutting UTF-8 bytes would not be. */
    wchar_t* wp = keal_wpath(path);
    if (wp == NULL) {
        return false;
    }
    for (wchar_t* at = wp; *at != L'\0'; at++) {
        /* A leading separator is the root, and a repeated one is nothing. */
        if ((*at != L'/' && *at != L'\\') || at == wp || at[-1] == L'\0') {
            continue;
        }
        wchar_t was = *at;
        *at = L'\0';
        CreateDirectoryW(wp, NULL);
        *at = was;
    }
    CreateDirectoryW(wp, NULL);
    free(wp);
#else
    char cpath[4096];
    if (!keal_cpath(path, cpath, sizeof cpath)) {
        return false;
    }
    for (char* at = cpath; *at != '\0'; at++) {
        /* A leading separator is the root, and a repeated one is nothing. */
        if (*at != '/' || at == cpath || at[-1] == '\0') {
            continue;
        }
        char was = *at;
        *at = '\0';
        mkdir(cpath, 0777);
        *at = was;
    }
    mkdir(cpath, 0777);
#endif
    return keal_path_kind(path) == 2;
}

/* One file, or one empty directory. Not a tree: a recursive delete behind a
 * one-word name is how a program loses what it did not mean to. */
KEAL_FN bool keal_remove_path(KealStr* path) {
#ifdef _WIN32
    wchar_t* wp = keal_wpath(path);
    if (wp == NULL) {
        return false;
    }
    bool gone;
    if (keal_path_kind(path) == 2) {
        gone = RemoveDirectoryW(wp) != 0;
    } else {
        gone = DeleteFileW(wp) != 0;
    }
    free(wp);
    return gone;
#else
    char cpath[4096];
    if (!keal_cpath(path, cpath, sizeof cpath)) {
        return false;
    }
    if (keal_path_kind(path) == 2) {
        return rmdir(cpath) == 0;
    }
    return remove(cpath) == 0;
#endif
}

/* ---- running another program ------------------------------------------- */

/* `runCommand(argv)` — the exit code, the standard output and the standard
 * error, or NULL when the program could not be STARTED, which is a different
 * thing from a program that ran and failed. No shell: the list is the
 * argument vector, so a path with a space in it stays one argument.
 *
 * Two things here are not obvious and both were measured rather than
 * reasoned about. The first is that reading one stream to the end before the
 * other DEADLOCKS as soon as the child fills the other's pipe buffer — 4096
 * bytes on Windows, 65536 here — which is not a stress case but any command
 * that writes a result and a warning. So both streams are drained at once:
 * `poll` on this side, a thread per stream on the other. The second is that
 * on Windows the ANSI entry points appear to round-trip UTF-8 perfectly into
 * the child's own argv while the command line Windows actually built is
 * mojibake — correct in a test, wrong for any child that reads the wide
 * command line. So the wide entry points, and a conversion at the boundary.
 */

/* [exit code, standard output, standard error], the code as text because
 * that is the shape the interpreters return and the three have to agree. */
static KealList* keal_run_result(int64_t code, KealBuf* out, KealBuf* err) {
    char digits[24];
    snprintf(digits, sizeof digits, "%" PRId64, code);
    KealList* l = keal_list_new(rel_keal_str_thunk);
    keal_list_push(l, (KealWord){ .p = keal_str_from_bytes(digits, (int64_t)strlen(digits)) });
    keal_list_push(l, (KealWord){ .p = keal_str_from_bytes(out->data, out->len) });
    keal_list_push(l, (KealWord){ .p = keal_str_from_bytes(err->data, err->len) });
    return l;
}

#ifdef _WIN32

/* One argument, quoted the way `CommandLineToArgvW` unquotes. Backslashes
 * are only escapes next to a quote: a run before a literal quote is doubled
 * and the quote escaped, a run before the CLOSING quote is doubled, and
 * anything else is left alone. Without the second rule `"C:\dir\"` ends in an
 * escaped quote, the string never closes, and the next argument is swallowed. */
static void keal_win_quote(const wchar_t* arg, KealBuf* line) {
    size_t len = wcslen(arg);
    bool bare = len > 0;
    for (size_t i = 0; i < len; i++) {
        if (arg[i] == L' ' || arg[i] == L'\t' || arg[i] == L'"') {
            bare = false;
            break;
        }
    }
    if (bare) {
        keal_buf_bytes(line, (const char*)arg, (int64_t)(len * sizeof(wchar_t)));
        return;
    }
    wchar_t q = L'"';
    keal_buf_bytes(line, (const char*)&q, (int64_t)sizeof q);
    for (size_t i = 0; i < len;) {
        size_t slashes = 0;
        while (i < len && arg[i] == L'\\') {
            slashes++;
            i++;
        }
        wchar_t bs = L'\\';
        if (i == len) {
            for (size_t k = 0; k < slashes * 2; k++) {
                keal_buf_bytes(line, (const char*)&bs, (int64_t)sizeof bs);
            }
            break;
        }
        if (arg[i] == L'"') {
            for (size_t k = 0; k < slashes * 2 + 1; k++) {
                keal_buf_bytes(line, (const char*)&bs, (int64_t)sizeof bs);
            }
        } else {
            for (size_t k = 0; k < slashes; k++) {
                keal_buf_bytes(line, (const char*)&bs, (int64_t)sizeof bs);
            }
        }
        keal_buf_bytes(line, (const char*)&arg[i], (int64_t)sizeof(wchar_t));
        i++;
    }
    keal_buf_bytes(line, (const char*)&q, (int64_t)sizeof q);
}

static wchar_t* keal_widen(const char* utf8, int64_t len) {
    int n = MultiByteToWideChar(CP_UTF8, 0, utf8, (int)len, NULL, 0);
    wchar_t* w = (wchar_t*)malloc(((size_t)n + 1) * sizeof(wchar_t));
    if (w == NULL) {
        return NULL;
    }
    if (n > 0) {
        MultiByteToWideChar(CP_UTF8, 0, utf8, (int)len, w, n);
    }
    w[n] = L'\0';
    return w;
}

typedef struct {
    HANDLE h;
    KealBuf buf;
} KealReader;

/* The other direction. A thread of its own for the same reason the two
 * readers have theirs: a child that answers before it has finished reading
 * fills the output pipe while the parent is still filling the input one, and
 * neither moves. */
typedef struct {
    HANDLE h;
    const char* bytes;
    int64_t len;
} KealWriter;

static DWORD WINAPI keal_feed(LPVOID arg) {
    KealWriter* w = (KealWriter*)arg;
    int64_t sent = 0;
    while (sent < w->len) {
        DWORD chunk = (DWORD)((w->len - sent) > 65536 ? 65536 : (w->len - sent));
        DWORD put = 0;
        /* A child that stopped reading is not a failure: it ran, and its exit
         * code is the answer. */
        if (!WriteFile(w->h, w->bytes + sent, chunk, &put, NULL) || put == 0) {
            break;
        }
        sent += (int64_t)put;
    }
    CloseHandle(w->h);
    return 0;
}

static DWORD WINAPI keal_drain(LPVOID arg) {
    KealReader* r = (KealReader*)arg;
    char chunk[16384];
    DWORD got;
    while (ReadFile(r->h, chunk, sizeof chunk, &got, NULL) && got > 0) {
        keal_buf_bytes(&r->buf, chunk, (int64_t)got);
    }
    return 0;
}

KEAL_FN KealList* keal_run_command(KealList* argv, KealStr* input) {
    if (argv->len == 0) {
        return NULL;
    }
    /* The command line, as wide characters, program included as token zero.
     *
     * That token is ALL the naming CreateProcessW gets: lpApplicationName
     * below is NULL on purpose. Given a name there, Windows completes it with
     * the current drive and directory and stops — it never consults the
     * search path — so `runCommand(["git", ...])` failed to start on Windows
     * while both interpreters, which go through execvp's POSIX equivalent in
     * Rust, resolved it. The failure wore the language's ordinary answer for
     * an absent command, `null`, so a program could not tell "not installed"
     * from "installed and unreachable".
     *
     * Passing NULL moves the naming into the line, which is the form that
     * searches. What lpApplicationName bought was the unquoted-path
     * ambiguity: `C:\Program Files\x.exe` unquoted invites Windows to try
     * `C:\Program.exe` first. keal_win_quote already quotes any token holding
     * a space, so that protection is kept where it was, one layer down. */
    KealBuf line;
    keal_buf_init(&line);
    for (int64_t i = 0; i < argv->len; i++) {
        KealStr* a = (KealStr*)argv->data[i].p;
        wchar_t* w = keal_widen(a->bytes, a->len);
        if (w == NULL) {
            free(line.data);
            return NULL;
        }
        if (i > 0) {
            wchar_t sp = L' ';
            keal_buf_bytes(&line, (const char*)&sp, (int64_t)sizeof sp);
        }
        keal_win_quote(w, &line);
        free(w);
    }
    wchar_t nul = L'\0';
    keal_buf_bytes(&line, (const char*)&nul, (int64_t)sizeof nul);

    SECURITY_ATTRIBUTES sa;
    sa.nLength = sizeof sa;
    sa.lpSecurityDescriptor = NULL;
    sa.bInheritHandle = TRUE;
    HANDLE or_ = NULL, ow = NULL, er = NULL, ew = NULL, nulh = INVALID_HANDLE_VALUE;
    HANDLE ir = NULL, iw = NULL;
    if (!CreatePipe(&or_, &ow, &sa, 0) || !CreatePipe(&er, &ew, &sa, 0)) {
        goto fail;
    }
    /* The parent's read ends must not be inherited, or the child holds them
     * open and ReadFile never reaches end of file. */
    SetHandleInformation(or_, HANDLE_FLAG_INHERIT, 0);
    SetHandleInformation(er, HANDLE_FLAG_INHERIT, 0);
    /* Nothing to send is the case that was here before this took an input:
     * the child reads NUL and sees end of file at once. */
    int64_t to_send = input == NULL ? 0 : input->len;
    if (to_send > 0) {
        if (!CreatePipe(&ir, &iw, &sa, 0)) {
            goto fail;
        }
        SetHandleInformation(iw, HANDLE_FLAG_INHERIT, 0);
    } else {
        nulh = CreateFileW(L"NUL", GENERIC_READ, FILE_SHARE_READ | FILE_SHARE_WRITE,
                           &sa, OPEN_EXISTING, 0, NULL);
    }

    STARTUPINFOW si;
    ZeroMemory(&si, sizeof si);
    si.cb = sizeof si;
    si.dwFlags = STARTF_USESTDHANDLES;
    si.hStdInput = to_send > 0 ? ir : nulh;
    si.hStdOutput = ow;
    si.hStdError = ew;
    PROCESS_INFORMATION pi;
    ZeroMemory(&pi, sizeof pi);
    if (!CreateProcessW(NULL, (wchar_t*)line.data, NULL, NULL, TRUE, 0, NULL, NULL, &si, &pi)) {
        goto fail;
    }
    /* And the parent must drop the write ends, or end of file never comes. */
    if (ir != NULL) {
        CloseHandle(ir);
        ir = NULL;
    }
    CloseHandle(ow);
    ow = NULL;
    CloseHandle(ew);
    ew = NULL;
    if (nulh != INVALID_HANDLE_VALUE) {
        CloseHandle(nulh);
        nulh = INVALID_HANDLE_VALUE;
    }

    KealReader ro;
    KealReader re;
    ro.h = or_;
    re.h = er;
    keal_buf_init(&ro.buf);
    keal_buf_init(&re.buf);
    HANDLE t1 = CreateThread(NULL, 0, keal_drain, &ro, 0, NULL);
    HANDLE t2 = CreateThread(NULL, 0, keal_drain, &re, 0, NULL);
    KealWriter wi;
    HANDLE t3 = NULL;
    if (iw != NULL) {
        wi.h = iw;
        wi.bytes = input->bytes;
        wi.len = to_send;
        t3 = CreateThread(NULL, 0, keal_feed, &wi, 0, NULL);
        if (t3 == NULL) {
            CloseHandle(iw);
        }
        iw = NULL;
    }
    WaitForSingleObject(pi.hProcess, INFINITE);
    if (t3 != NULL) {
        WaitForSingleObject(t3, INFINITE);
        CloseHandle(t3);
    }
    if (t1 != NULL) {
        WaitForSingleObject(t1, INFINITE);
        CloseHandle(t1);
    }
    if (t2 != NULL) {
        WaitForSingleObject(t2, INFINITE);
        CloseHandle(t2);
    }
    DWORD code = 0;
    GetExitCodeProcess(pi.hProcess, &code);
    /* An image can RESOLVE and then fail to LOAD: CreateProcessW succeeds,
     * the process object exists, and the child dies before its entry point
     * runs. A missing DLL is the common way. On this side that is a nonzero
     * exit code; on POSIX the very same failure — a missing shared object —
     * makes `execve` fail, so the errno pipe above reports it as "could not
     * be started" and the caller gets null. Mapping the two load-time
     * statuses to null is not a heuristic, then: it is what makes the two
     * platforms answer the same question the same way.
     *
     * A crash is NOT included. 0xC0000005 and its neighbours mean the program
     * ran and then died, which is a failure and not an absence, and the
     * caller is owed the difference. */
    if (code == 0xC0000135u || code == 0xC0000142u) {
        CloseHandle(pi.hProcess);
        CloseHandle(pi.hThread);
        CloseHandle(or_);
        CloseHandle(er);
        free(line.data);
        free(ro.buf.data);
        free(re.buf.data);
        return NULL;
    }
    CloseHandle(pi.hProcess);
    CloseHandle(pi.hThread);
    CloseHandle(or_);
    CloseHandle(er);
    free(line.data);
    KealList* result = keal_run_result((int64_t)(int32_t)code, &ro.buf, &re.buf);
    free(ro.buf.data);
    free(re.buf.data);
    return result;

fail:
    if (ir) CloseHandle(ir);
    if (iw) CloseHandle(iw);
    if (or_) CloseHandle(or_);
    if (ow) CloseHandle(ow);
    if (er) CloseHandle(er);
    if (ew) CloseHandle(ew);
    if (nulh != INVALID_HANDLE_VALUE) CloseHandle(nulh);
    free(line.data);
    return NULL;
}

#else

KEAL_FN KealList* keal_run_command(KealList* argv, KealStr* input) {
    if (argv->len == 0) {
        return NULL;
    }
    char** words = (char**)calloc((size_t)argv->len + 1, sizeof(char*));
    if (words == NULL) {
        return NULL;
    }
    for (int64_t i = 0; i < argv->len; i++) {
        KealStr* a = (KealStr*)argv->data[i].p;
        words[i] = (char*)malloc((size_t)a->len + 1);
        if (words[i] == NULL) {
            for (int64_t k = 0; k < i; k++) { free(words[k]); }
            free(words);
            return NULL;
        }
        memcpy(words[i], a->bytes, (size_t)a->len);
        words[i][a->len] = '\0';
    }

    int outp[2], errp[2], failp[2], inp[2];
    if (pipe(outp) != 0) { goto cleanup_words; }
    if (pipe(errp) != 0) { close(outp[0]); close(outp[1]); goto cleanup_words; }
    if (pipe(inp) != 0) {
        close(outp[0]); close(outp[1]); close(errp[0]); close(errp[1]);
        goto cleanup_words;
    }
    /* How the child says `exec` failed. Closed automatically when exec
     * succeeds, so an empty read is success — the same way the interpreters
     * tell "could not start" from "started and failed". */
    if (pipe(failp) != 0) {
        close(outp[0]); close(outp[1]); close(errp[0]); close(errp[1]);
        close(inp[0]); close(inp[1]);
        goto cleanup_words;
    }
    fcntl(failp[1], F_SETFD, FD_CLOEXEC);

    pid_t pid = fork();
    if (pid < 0) {
        close(outp[0]); close(outp[1]); close(errp[0]); close(errp[1]);
        close(failp[0]); close(failp[1]); close(inp[0]); close(inp[1]);
        goto cleanup_words;
    }
    if (pid == 0) {
        close(outp[0]);
        close(errp[0]);
        close(failp[0]);
        close(inp[1]);
        dup2(inp[0], STDIN_FILENO);
        dup2(outp[1], STDOUT_FILENO);
        dup2(errp[1], STDERR_FILENO);
        close(inp[0]);
        close(outp[1]);
        close(errp[1]);
        execvp(words[0], words);
        int e = errno;
        ssize_t ignored = write(failp[1], &e, sizeof e);
        (void)ignored;
        _exit(127);
    }

    close(outp[1]);
    close(errp[1]);
    close(failp[1]);
    close(inp[0]);
    /* NON-BLOCKING, and this is not an optimisation. `poll` promises only
     * that ONE byte can be written; a blocking `write` of 64 KiB returns when
     * it has placed all 64 KiB, so the parent sits in the write while the
     * child fills the output pipe it is no longer draining, and neither
     * moves. That is the deadlock the two readers were already written to
     * avoid, one level down: it is not enough to wait for all three
     * descriptors together if one of them can still block once chosen. */
    fcntl(inp[1], F_SETFL, O_NONBLOCK);
    /* Nothing to send is the case that was here before this took an input:
     * close at once, and the child reads end of file exactly as it did from
     * /dev/null. */
    int64_t sent = 0;
    int64_t to_send = input == NULL ? 0 : input->len;
    if (to_send == 0) {
        close(inp[1]);
        inp[1] = -1;
    }

    KealBuf out;
    KealBuf err;
    keal_buf_init(&out);
    keal_buf_init(&err);
    /* Both streams drained together. Taking one to the end first deadlocks
     * the moment the child fills the other's buffer, which is any command
     * that writes a result and a warning. */
    /* All three descriptors in one wait. Writing the input to the end before
     * reading deadlocks the moment the child answers before it has finished
     * reading — it fills the output pipe while the parent is still filling
     * the input one, and neither moves. That is not a stress case: it is any
     * command that streams. */
    struct pollfd fds[3];
    fds[0].fd = outp[0];
    fds[1].fd = errp[0];
    fds[2].fd = inp[1];
    fds[0].events = fds[1].events = POLLIN;
    fds[2].events = POLLOUT;
    int open_streams = fds[2].fd < 0 ? 2 : 3;
    while (open_streams > 0) {
        fds[0].revents = fds[1].revents = fds[2].revents = 0;
        if (poll(fds, 3, -1) < 0) {
            if (errno == EINTR) { continue; }
            break;
        }
        for (int i = 0; i < 2; i++) {
            if (fds[i].fd < 0 || fds[i].revents == 0) {
                continue;
            }
            char chunk[16384];
            ssize_t got = read(fds[i].fd, chunk, sizeof chunk);
            if (got > 0) {
                keal_buf_bytes(i == 0 ? &out : &err, chunk, (int64_t)got);
            } else if (got == 0 || errno != EINTR) {
                close(fds[i].fd);
                fds[i].fd = -1;
                open_streams--;
            }
        }
        if (fds[2].fd >= 0 && fds[2].revents != 0) {
            int64_t left = to_send - sent;
            ssize_t put = left > 0
                ? write(fds[2].fd, input->bytes + sent, (size_t)(left > 65536 ? 65536 : left))
                : 0;
            if (put > 0) {
                sent += (int64_t)put;
            }
            /* A child that stopped reading is not a failure — it ran, and its
             * exit code is the answer. SIGPIPE is ignored process-wide, so
             * this comes back as EPIPE rather than as a death. */
            if (sent >= to_send || (put < 0 && errno != EINTR && errno != EAGAIN)
                || (fds[2].revents & (POLLERR | POLLHUP)) != 0) {
                close(fds[2].fd);
                fds[2].fd = -1;
                open_streams--;
            }
        }
    }
    if (fds[2].fd >= 0) {
        close(fds[2].fd);
    }

    int failed = 0;
    ssize_t got_fail = read(failp[0], &failed, sizeof failed);
    close(failp[0]);

    int status = 0;
    while (waitpid(pid, &status, 0) < 0 && errno == EINTR) {
    }
    for (int64_t i = 0; i < argv->len; i++) { free(words[i]); }
    free(words);

    if (got_fail == (ssize_t)sizeof failed) {
        /* `exec` never ran the program, so nothing started. */
        free(out.data);
        free(err.data);
        return NULL;
    }
    int64_t code = WIFEXITED(status) ? WEXITSTATUS(status) : -1;
    KealList* result = keal_run_result(code, &out, &err);
    free(out.data);
    free(err.data);
    return result;

cleanup_words:
    for (int64_t i = 0; i < argv->len; i++) { free(words[i]); }
    free(words);
    return NULL;
}

#endif

/* ---- checked integer arithmetic --------------------------------------- */

/* The interpreter and the VM both refuse to wrap, so native code must not
 * either: a program that overflows should fail the same way whichever engine
 * runs it. */
KEAL_FN int64_t keal_add(int64_t a, int64_t b, const char* at) {
    int64_t r;
    if (__builtin_add_overflow(a, b, &r)) {
        keal_panic("integer overflow", NULL, at);
        return 0;
    }
    return r;
}

KEAL_FN int64_t keal_sub(int64_t a, int64_t b, const char* at) {
    int64_t r;
    if (__builtin_sub_overflow(a, b, &r)) {
        keal_panic("integer overflow", NULL, at);
        return 0;
    }
    return r;
}

KEAL_FN int64_t keal_mul(int64_t a, int64_t b, const char* at) {
    int64_t r;
    if (__builtin_mul_overflow(a, b, &r)) {
        keal_panic("integer overflow", NULL, at);
        return 0;
    }
    return r;
}

KEAL_FN int64_t keal_div(int64_t a, int64_t b, const char* at) {
    if (b == 0) {
        keal_panic("division by zero", NULL, at);
        return 0;
    }
    if (a == INT64_MIN && b == -1) {
        keal_panic("integer overflow", NULL, at);
        return 0;
    }
    return a / b;
}

/* The shifts.
 *
 * An Int has 64 bits, so 0 through 63 name a shift and everything else names
 * a mistake — which panics, rather than being left undefined the way C
 * leaves it. Every alternative reading a language could pick instead (clamp,
 * count modulo 64, saturate) turns a bug into a number the program carries.
 *
 * `shl` truncates: the bits that leave the top are gone. It is the one place
 * in Keal where a value is not checked, and it is deliberate — these
 * operators are defined on the 64 bits of an Int, not on the number those
 * bits spell.
 *
 * All three go through uint64_t: shifting a signed value past its sign bit
 * is undefined in C, and `>>` on a negative one is implementation-defined. */
KEAL_FN bool keal_shift_ok(int64_t by, const char* op, const char* at) {
    if (by < 0) {
        char msg[96];
        snprintf(msg, sizeof msg, "`%s` needs a shift count of 0 or more, got %" PRId64, op, by);
        keal_panic(msg, NULL, at);
        return false;
    }
    if (by > 63) {
        char msg[96];
        snprintf(msg, sizeof msg, "`%s` cannot shift an Int by %" PRId64 ": it has 64 bits", op, by);
        keal_panic(msg, NULL, at);
        return false;
    }
    return true;
}

KEAL_FN int64_t keal_shl(int64_t a, int64_t b, const char* at) {
    if (!keal_shift_ok(b, "shl", at)) { return 0; }
    return (int64_t)((uint64_t)a << b);
}

KEAL_FN int64_t keal_shr(int64_t a, int64_t b, const char* at) {
    if (!keal_shift_ok(b, "shr", at)) { return 0; }
    /* Arithmetic: the sign is carried in at the top, so `-8 shr 1` is `-4`
     * the way `-8 / 2` is. */
    if (a < 0) {
        return (int64_t)~((~(uint64_t)a) >> b);
    }
    return (int64_t)((uint64_t)a >> b);
}

KEAL_FN int64_t keal_ushr(int64_t a, int64_t b, const char* at) {
    if (!keal_shift_ok(b, "ushr", at)) { return 0; }
    return (int64_t)((uint64_t)a >> b);
}

KEAL_FN int64_t keal_rem(int64_t a, int64_t b, const char* at) {
    if (b == 0) {
        keal_panic("remainder by zero", NULL, at);
        return 0;
    }
    if (a == INT64_MIN && b == -1) {
        return 0;
    }
    return a % b;
}

/* ---- the drop hook ---------------------------------------------------- */

/* Objects whose last reference died wait here, whole and holding one
 * resurrected reference, for the next statement boundary — where their
 * class's `drop` runs and the release resumes. FIFO: death order is drop
 * order. A `drop` body's own deaths join the same sweep; the guard stops
 * the sweep from recursing into itself. */
typedef struct KealPendingDrop {
    void* obj;
    void (*run)(void*);
    struct KealPendingDrop* next;
} KealPendingDrop;

static _Thread_local KealPendingDrop* keal_drops_head = NULL;
static _Thread_local KealPendingDrop* keal_drops_tail = NULL;
static _Thread_local bool keal_draining = false;

KEAL_FN void keal_queue_drop(void* obj, void (*run)(void*)) {
    KealPendingDrop* n = (KealPendingDrop*)keal_alloc(sizeof(KealPendingDrop));
    n->obj = obj;
    n->run = run;
    n->next = NULL;
    if (keal_drops_tail == NULL) {
        keal_drops_head = n;
    } else {
        keal_drops_tail->next = n;
    }
    keal_drops_tail = n;
}

KEAL_FN void keal_drain_drops(void) {
    if (keal_draining) {
        return;
    }
    keal_draining = true;
    while (keal_drops_head != NULL) {
        KealPendingDrop* n = keal_drops_head;
        keal_drops_head = n->next;
        if (keal_drops_head == NULL) {
            keal_drops_tail = NULL;
        }
        void (*run)(void*) = n->run;
        void* obj = n->obj;
        free(n);
        run(obj);
        /* A panic inside a `drop` stops the sweep; what remains drains
         * at the next boundary. */
        if (keal_unwinding) {
            break;
        }
    }
    keal_draining = false;
}

/* The thrown value itself, live only while `keal_unwind_has_value`. */
static _Thread_local KealAny keal_unwind_value;

/* A `throw` of a value. The value crosses whole — tag and payload, the
 * `Any` machinery — and its rendering is the message a `catch (e)` reads,
 * the same text the interpreters give since both take it from `display`.
 * Uncaught, this ends the program printing that message, like any panic. */
KEAL_FN void keal_throw_value(KealAny v, const char* at) {
    KealStr* m = keal_any_display(v);
    /* Only a fresh unwind adopts the value: one already unwinding keeps
     * the first, which is the rule the message already follows. */
    bool fresh = keal_try_depth > 0 && !keal_unwinding;
    keal_panic(m->bytes, NULL, at);
    if (fresh) {
        keal_unwind_value = keal_any_retain(v);
        keal_unwind_has_value = true;
    }
    keal_str_release(m);
}

/* Does the value being unwound answer to this tag? A message-only unwind
 * answers to `String`, which is what it is. */
KEAL_FN bool keal_unwind_is(const KealTypeInfo* ti) {
    return keal_unwind_has_value ? keal_unwind_value.ti == ti : ti == &keal_ti_str;
}

/* `catch (e: Any)`: anything but null, as `is Any` reads it everywhere. */
KEAL_FN bool keal_unwind_is_any(void) {
    return !keal_unwind_has_value || keal_unwind_value.ti != NULL;
}

/* Ends the unwind at a typed `catch`: hands the value over, owned. */
KEAL_FN KealAny keal_unwind_value_take(void) {
    keal_unwinding = false;
    if (!keal_unwind_has_value) {
        KealAny a;
        a.ti = &keal_ti_str;
        a.w.p = keal_str_from_bytes(keal_unwind_msg, (int64_t)strlen(keal_unwind_msg));
        return a;
    }
    keal_unwind_has_value = false;
    return keal_unwind_value;
}

/* Ends the unwind at a `catch`: hands the message over and clears the
 * flag, so the handler runs like any other code. A value the clause did
 * not ask for dies here — the message is all it wanted. */
KEAL_FN KealStr* keal_unwind_take(void) {
    keal_unwinding = false;
    if (keal_unwind_has_value) {
        keal_unwind_has_value = false;
        keal_any_release(keal_unwind_value);
    }
    return keal_str_from_bytes(keal_unwind_msg, (int64_t)strlen(keal_unwind_msg));
}


/* Where this program can fail, and what its source looks like there.
 * Rendered at compile time by the same code that renders the
 * interpreters' diagnostics, so the three engines cannot drift apart
 * over a caret. `KF` lines are stack frames. */
static const char KL0[] = "  --> <prelude>:648:41\n    |\n648 |         return \"${sign}${twoDigits(east / 3600)}:${twoDigits((east / 60) % 60)}\"\n    |                                         ^\n";
static const char KF1[] = "  in `twoDigits` at <prelude>:648:26";
static const char KL2[] = "  --> <prelude>:648:26\n    |\n648 |         return \"${sign}${twoDigits(east / 3600)}:${twoDigits((east / 60) % 60)}\"\n    |                          ^\n";
static const char KL3[] = "  --> <prelude>:648:68\n    |\n648 |         return \"${sign}${twoDigits(east / 3600)}:${twoDigits((east / 60) % 60)}\"\n    |                                                                    ^\n";
static const char KL4[] = "  --> <prelude>:648:74\n    |\n648 |         return \"${sign}${twoDigits(east / 3600)}:${twoDigits((east / 60) % 60)}\"\n    |                                                                          ^\n";
static const char KF5[] = "  in `twoDigits` at <prelude>:648:52";
static const char KL6[] = "  --> <prelude>:648:52\n    |\n648 |         return \"${sign}${twoDigits(east / 3600)}:${twoDigits((east / 60) % 60)}\"\n    |                                                    ^\n";
static const char KF7[] = "  in `padStart` at <prelude>:653:19";
static const char KL8[] = "  --> <prelude>:653:19\n    |\n653 |         return \"${padStart(\"${this.year}\", 4, \"0\")}-${twoDigits(this.month)}-${twoDigits(this.day)}\"\n    |                   ^\n";
static const char KF9[] = "  in `twoDigits` at <prelude>:653:55";
static const char KL10[] = "  --> <prelude>:653:55\n    |\n653 |         return \"${padStart(\"${this.year}\", 4, \"0\")}-${twoDigits(this.month)}-${twoDigits(this.day)}\"\n    |                                                       ^\n";
static const char KF11[] = "  in `twoDigits` at <prelude>:653:80";
static const char KL12[] = "  --> <prelude>:653:80\n    |\n653 |         return \"${padStart(\"${this.year}\", 4, \"0\")}-${twoDigits(this.month)}-${twoDigits(this.day)}\"\n    |                                                                                ^\n";
static const char KF13[] = "  in `twoDigits` at <prelude>:658:19";
static const char KL14[] = "  --> <prelude>:658:19\n    |\n658 |         return \"${twoDigits(this.hour)}:${twoDigits(this.minute)}:${twoDigits(this.second)}\"\n    |                   ^\n";
static const char KF15[] = "  in `twoDigits` at <prelude>:658:43";
static const char KL16[] = "  --> <prelude>:658:43\n    |\n658 |         return \"${twoDigits(this.hour)}:${twoDigits(this.minute)}:${twoDigits(this.second)}\"\n    |                                           ^\n";
static const char KF17[] = "  in `twoDigits` at <prelude>:658:69";
static const char KL18[] = "  --> <prelude>:658:69\n    |\n658 |         return \"${twoDigits(this.hour)}:${twoDigits(this.minute)}:${twoDigits(this.second)}\"\n    |                                                                     ^\n";
static const char KF19[] = "  in `daysFromCivil` at <prelude>:667:20";
static const char KL20[] = "  --> <prelude>:667:20\n    |\n667 |         val days = daysFromCivil(this.year, this.month, this.day)\n    |                    ^\n";
static const char KL21[] = "  --> <prelude>:668:31\n    |\n668 |         val ofDay = this.hour * 3600 + this.minute * 60 + this.second\n    |                               ^\n";
static const char KL22[] = "  --> <prelude>:668:52\n    |\n668 |         val ofDay = this.hour * 3600 + this.minute * 60 + this.second\n    |                                                    ^\n";
static const char KL23[] = "  --> <prelude>:668:38\n    |\n668 |         val ofDay = this.hour * 3600 + this.minute * 60 + this.second\n    |                                      ^\n";
static const char KL24[] = "  --> <prelude>:668:57\n    |\n668 |         val ofDay = this.hour * 3600 + this.minute * 60 + this.second\n    |                                                         ^\n";
static const char KL25[] = "  --> <prelude>:669:21\n    |\n669 |         return days * 86400 + ofDay - this.offset\n    |                     ^\n";
static const char KL26[] = "  --> <prelude>:669:29\n    |\n669 |         return days * 86400 + ofDay - this.offset\n    |                             ^\n";
static const char KL27[] = "  --> <prelude>:669:37\n    |\n669 |         return days * 86400 + ofDay - this.offset\n    |                                     ^\n";
static const char KF28[] = "  in `utcAt` at <prelude>:673:44";
static const char KL29[] = "  --> <prelude>:673:44\n    |\n673 |     public func inUtc(): DateTime { return utcAt(this.epochSeconds()) }\n    |                                            ^\n";
static const char KF30[] = "  in `localAt` at <prelude>:676:50";
static const char KL31[] = "  --> <prelude>:676:50\n    |\n676 |     public func inLocalTime(): DateTime { return localAt(this.epochSeconds()) }\n    |                                                  ^\n";
static const char KL32[] = "  --> <prelude>:586:22\n    |\n586 |     if (pad == \"\") { throw \"`padStart` needs a non-empty pad\" }\n    |                      ^\n";
static const char KL33[] = "  --> <prelude>:594:22\n    |\n594 |     if (pad == \"\") { throw \"`padEnd` needs a non-empty pad\" }\n    |                      ^\n";
static const char KL34[] = "  --> <prelude>:605:69\n    |\n605 |     if (text.endsWith(\"\\n\")) { text = text.substring(0, text.length - 1) }\n    |                                                                     ^\n";
static const char KL35[] = "  --> <prelude>:605:44\n    |\n605 |     if (text.endsWith(\"\\n\")) { text = text.substring(0, text.length - 1) }\n    |                                            ^\n";
static const char KF36[] = "  in `padStart` at <prelude>:679:41";
static const char KL37[] = "  --> <prelude>:679:41\n    |\n679 | func twoDigits(n: Int): String { return padStart(\"${n}\", 2, \"0\") }\n    |                                         ^\n";
static const char KL38[] = "  --> <prelude>:684:15\n    |\n684 |     var q = a / b\n    |               ^\n";
static const char KL39[] = "  --> <prelude>:685:11\n    |\n685 |     if (a % b != 0 and ((a < 0) != (b < 0))) { q = q - 1 }\n    |           ^\n";
static const char KL40[] = "  --> <prelude>:685:54\n    |\n685 |     if (a % b != 0 and ((a < 0) != (b < 0))) { q = q - 1 }\n    |                                                      ^\n";
static const char KF41[] = "  in `floorDiv` at <prelude>:689:49";
static const char KL42[] = "  --> <prelude>:689:49\n    |\n689 | func floorMod(a: Int, b: Int): Int { return a - floorDiv(a, b) * b }\n    |                                                 ^\n";
static const char KL43[] = "  --> <prelude>:689:64\n    |\n689 | func floorMod(a: Int, b: Int): Int { return a - floorDiv(a, b) * b }\n    |                                                                ^\n";
static const char KL44[] = "  --> <prelude>:689:47\n    |\n689 | func floorMod(a: Int, b: Int): Int { return a - floorDiv(a, b) * b }\n    |                                               ^\n";
static const char KF45[] = "  in `floorDiv` at <prelude>:697:16";
static const char KL46[] = "  --> <prelude>:697:16\n    |\n697 |     val days = floorDiv(seconds, 86400)\n    |                ^\n";
static const char KF47[] = "  in `floorMod` at <prelude>:698:16";
static const char KL48[] = "  --> <prelude>:698:16\n    |\n698 |     val rest = floorMod(seconds, 86400)\n    |                ^\n";
static const char KL49[] = "  --> <prelude>:700:18\n    |\n700 |     val z = days + 719468\n    |                  ^\n";
static const char KF50[] = "  in `floorDiv` at <prelude>:701:15";
static const char KL51[] = "  --> <prelude>:701:15\n    |\n701 |     val era = floorDiv(z, 146097)\n    |               ^\n";
static const char KL52[] = "  --> <prelude>:702:23\n    |\n702 |     val doe = z - era * 146097\n    |                       ^\n";
static const char KL53[] = "  --> <prelude>:702:17\n    |\n702 |     val doe = z - era * 146097\n    |                 ^\n";
static const char KL54[] = "  --> <prelude>:703:26\n    |\n703 |     val yoe = (doe - doe / 1460 + doe / 36524 - doe / 146096) / 365\n    |                          ^\n";
static const char KL55[] = "  --> <prelude>:703:20\n    |\n703 |     val yoe = (doe - doe / 1460 + doe / 36524 - doe / 146096) / 365\n    |                    ^\n";
static const char KL56[] = "  --> <prelude>:703:39\n    |\n703 |     val yoe = (doe - doe / 1460 + doe / 36524 - doe / 146096) / 365\n    |                                       ^\n";
static const char KL57[] = "  --> <prelude>:703:33\n    |\n703 |     val yoe = (doe - doe / 1460 + doe / 36524 - doe / 146096) / 365\n    |                                 ^\n";
static const char KL58[] = "  --> <prelude>:703:53\n    |\n703 |     val yoe = (doe - doe / 1460 + doe / 36524 - doe / 146096) / 365\n    |                                                     ^\n";
static const char KL59[] = "  --> <prelude>:703:47\n    |\n703 |     val yoe = (doe - doe / 1460 + doe / 36524 - doe / 146096) / 365\n    |                                               ^\n";
static const char KL60[] = "  --> <prelude>:703:63\n    |\n703 |     val yoe = (doe - doe / 1460 + doe / 36524 - doe / 146096) / 365\n    |                                                               ^\n";
static const char KL61[] = "  --> <prelude>:704:26\n    |\n704 |     val doy = doe - (365 * yoe + yoe / 4 - yoe / 100)\n    |                          ^\n";
static const char KL62[] = "  --> <prelude>:704:38\n    |\n704 |     val doy = doe - (365 * yoe + yoe / 4 - yoe / 100)\n    |                                      ^\n";
static const char KL63[] = "  --> <prelude>:704:32\n    |\n704 |     val doy = doe - (365 * yoe + yoe / 4 - yoe / 100)\n    |                                ^\n";
static const char KL64[] = "  --> <prelude>:704:48\n    |\n704 |     val doy = doe - (365 * yoe + yoe / 4 - yoe / 100)\n    |                                                ^\n";
static const char KL65[] = "  --> <prelude>:704:42\n    |\n704 |     val doy = doe - (365 * yoe + yoe / 4 - yoe / 100)\n    |                                          ^\n";
static const char KL66[] = "  --> <prelude>:704:19\n    |\n704 |     val doy = doe - (365 * yoe + yoe / 4 - yoe / 100)\n    |                   ^\n";
static const char KL67[] = "  --> <prelude>:705:17\n    |\n705 |     val mp = (5 * doy + 2) / 153\n    |                 ^\n";
static const char KL68[] = "  --> <prelude>:705:23\n    |\n705 |     val mp = (5 * doy + 2) / 153\n    |                       ^\n";
static const char KL69[] = "  --> <prelude>:705:28\n    |\n705 |     val mp = (5 * doy + 2) / 153\n    |                            ^\n";
static const char KL70[] = "  --> <prelude>:706:24\n    |\n706 |     val d = doy - (153 * mp + 2) / 5 + 1\n    |                        ^\n";
static const char KL71[] = "  --> <prelude>:706:29\n    |\n706 |     val d = doy - (153 * mp + 2) / 5 + 1\n    |                             ^\n";
static const char KL72[] = "  --> <prelude>:706:34\n    |\n706 |     val d = doy - (153 * mp + 2) / 5 + 1\n    |                                  ^\n";
static const char KL73[] = "  --> <prelude>:706:17\n    |\n706 |     val d = doy - (153 * mp + 2) / 5 + 1\n    |                 ^\n";
static const char KL74[] = "  --> <prelude>:706:38\n    |\n706 |     val d = doy - (153 * mp + 2) / 5 + 1\n    |                                      ^\n";
static const char KL75[] = "  --> <prelude>:707:16\n    |\n707 |     var m = mp + 3\n    |                ^\n";
static const char KL76[] = "  --> <prelude>:708:28\n    |\n708 |     if (mp >= 10) { m = mp - 9 }\n    |                            ^\n";
static const char KL77[] = "  --> <prelude>:709:23\n    |\n709 |     var y = yoe + era * 400\n    |                       ^\n";
static const char KL78[] = "  --> <prelude>:709:17\n    |\n709 |     var y = yoe + era * 400\n    |                 ^\n";
static const char KL79[] = "  --> <prelude>:710:25\n    |\n710 |     if (m <= 2) { y = y + 1 }\n    |                         ^\n";
static const char KL80[] = "  --> <prelude>:713:33\n    |\n713 |     val weekday = floorMod(days + 4, 7)\n    |                                 ^\n";
static const char KF81[] = "  in `floorMod` at <prelude>:713:19";
static const char KL82[] = "  --> <prelude>:713:19\n    |\n713 |     val weekday = floorMod(days + 4, 7)\n    |                   ^\n";
static const char KL83[] = "  --> <prelude>:714:35\n    |\n714 |     return DateTime(y, m, d, rest / 3600, (rest / 60) % 60, rest % 60, weekday, 0)\n    |                                   ^\n";
static const char KL84[] = "  --> <prelude>:714:49\n    |\n714 |     return DateTime(y, m, d, rest / 3600, (rest / 60) % 60, rest % 60, weekday, 0)\n    |                                                 ^\n";
static const char KL85[] = "  --> <prelude>:714:55\n    |\n714 |     return DateTime(y, m, d, rest / 3600, (rest / 60) % 60, rest % 60, weekday, 0)\n    |                                                       ^\n";
static const char KL86[] = "  --> <prelude>:714:66\n    |\n714 |     return DateTime(y, m, d, rest / 3600, (rest / 60) % 60, rest % 60, weekday, 0)\n    |                                                                  ^\n";
static const char KL87[] = "  --> <prelude>:725:33\n    |\n725 |     val shifted = utcAt(seconds + east)\n    |                                 ^\n";
static const char KF88[] = "  in `utcAt` at <prelude>:725:19";
static const char KL89[] = "  --> <prelude>:725:19\n    |\n725 |     val shifted = utcAt(seconds + east)\n    |                   ^\n";
static const char KF90[] = "  in `localAt` at <prelude>:732:43";
static const char KL91[] = "  --> <prelude>:732:43\n    |\n732 | public func localNow(): DateTime { return localAt(time().toInt()) }\n    |                                           ^\n";
static const char KL92[] = "  --> <prelude>:738:29\n    |\n738 |     if (month <= 2) { y = y - 1 }\n    |                             ^\n";
static const char KF93[] = "  in `floorDiv` at <prelude>:739:15";
static const char KL94[] = "  --> <prelude>:739:15\n    |\n739 |     val era = floorDiv(y, 400)\n    |               ^\n";
static const char KL95[] = "  --> <prelude>:740:23\n    |\n740 |     val yoe = y - era * 400\n    |                       ^\n";
static const char KL96[] = "  --> <prelude>:740:17\n    |\n740 |     val yoe = y - era * 400\n    |                 ^\n";
static const char KL97[] = "  --> <prelude>:741:20\n    |\n741 |     var mp = month - 3\n    |                    ^\n";
static const char KL98[] = "  --> <prelude>:742:34\n    |\n742 |     if (month <= 2) { mp = month + 9 }\n    |                                  ^\n";
static const char KL99[] = "  --> <prelude>:743:20\n    |\n743 |     val doy = (153 * mp + 2) / 5 + day - 1\n    |                    ^\n";
static const char KL100[] = "  --> <prelude>:743:25\n    |\n743 |     val doy = (153 * mp + 2) / 5 + day - 1\n    |                         ^\n";
static const char KL101[] = "  --> <prelude>:743:30\n    |\n743 |     val doy = (153 * mp + 2) / 5 + day - 1\n    |                              ^\n";
static const char KL102[] = "  --> <prelude>:743:34\n    |\n743 |     val doy = (153 * mp + 2) / 5 + day - 1\n    |                                  ^\n";
static const char KL103[] = "  --> <prelude>:743:40\n    |\n743 |     val doy = (153 * mp + 2) / 5 + day - 1\n    |                                        ^\n";
static const char KL104[] = "  --> <prelude>:744:19\n    |\n744 |     val doe = yoe * 365 + yoe / 4 - yoe / 100 + doy\n    |                   ^\n";
static const char KL105[] = "  --> <prelude>:744:31\n    |\n744 |     val doe = yoe * 365 + yoe / 4 - yoe / 100 + doy\n    |                               ^\n";
static const char KL106[] = "  --> <prelude>:744:25\n    |\n744 |     val doe = yoe * 365 + yoe / 4 - yoe / 100 + doy\n    |                         ^\n";
static const char KL107[] = "  --> <prelude>:744:41\n    |\n744 |     val doe = yoe * 365 + yoe / 4 - yoe / 100 + doy\n    |                                         ^\n";
static const char KL108[] = "  --> <prelude>:744:35\n    |\n744 |     val doe = yoe * 365 + yoe / 4 - yoe / 100 + doy\n    |                                   ^\n";
static const char KL109[] = "  --> <prelude>:744:47\n    |\n744 |     val doe = yoe * 365 + yoe / 4 - yoe / 100 + doy\n    |                                               ^\n";
static const char KL110[] = "  --> <prelude>:745:16\n    |\n745 |     return era * 146097 + doe - 719468\n    |                ^\n";
static const char KL111[] = "  --> <prelude>:745:25\n    |\n745 |     return era * 146097 + doe - 719468\n    |                         ^\n";
static const char KL112[] = "  --> <prelude>:745:31\n    |\n745 |     return era * 146097 + doe - 719468\n    |                               ^\n";
static const char KF113[] = "  in `utcAt` at <prelude>:749:41";
static const char KL114[] = "  --> <prelude>:749:41\n    |\n749 | public func utcNow(): DateTime { return utcAt(time().toInt()) }\n    |                                         ^\n";
static const char KL115[] = "  --> <prelude>:757:12\n    |\n757 |     return names[month]\n    |            ^\n";
static const char KL116[] = "  --> <prelude>:764:12\n    |\n764 |     return names[weekday]\n    |            ^\n";
static const char KL117[] = "  --> <prelude>:770:17\n    |\n770 |     return year % 4 == 0 and (year % 100 != 0 or year % 400 == 0)\n    |                 ^\n";
static const char KL118[] = "  --> <prelude>:770:36\n    |\n770 |     return year % 4 == 0 and (year % 100 != 0 or year % 400 == 0)\n    |                                    ^\n";
static const char KL119[] = "  --> <prelude>:770:55\n    |\n770 |     return year % 4 == 0 and (year % 100 != 0 or year % 400 == 0)\n    |                                                       ^\n";
static const char KL120[] = "  --> <prelude>:808:68\n    |\n808 |     if (base.endsWith(\"/\")) { base = base.substring(0, base.length - 1) }\n    |                                                                    ^\n";
static const char KL121[] = "  --> <prelude>:808:43\n    |\n808 |     if (base.endsWith(\"/\")) { base = base.substring(0, base.length - 1) }\n    |                                           ^\n";
static const char KF122[] = "  in `isDir` at <prelude>:811:13";
static const char KL123[] = "  --> <prelude>:811:13\n    |\n811 |         if (isDir(child)) {\n    |             ^\n";
static const char KF124[] = "  in `walkDir` at <prelude>:812:24";
static const char KL125[] = "  --> <prelude>:812:24\n    |\n812 |             out.addAll(walkDir(child))\n    |                        ^\n";
static const char KL126[] = "  --> tests/programs/processes.keal:10:1\n   |\n10 | assert(runCommand([]) == null, \"an empty vector names no program\")\n   | ^\n";
static const char KL127[] = "  --> tests/programs/processes.keal:11:1\n   |\n11 | assert(runCommand([\"keal-no-such-program-1234\"]) == null, \"nor does a missing one\")\n   | ^\n";
static const char KF128[] = "  in `isFile` at tests/programs/processes.keal:15:5";
static const char KL129[] = "  --> tests/programs/processes.keal:15:5\n   |\n15 | if (isFile(\"/bin/sh\")) {\n   |     ^\n";
static const char KL130[] = "  --> tests/programs/processes.keal:17:5\n   |\n17 |     assert(ok != null, \"a shell that exists starts\")\n   |     ^\n";
static const char KL131[] = "  --> tests/programs/processes.keal:19:5\n   |\n19 |     assert(r.size == 3, \"three parts: code, output, error\")\n   |     ^\n";
static const char KL132[] = "  --> tests/programs/processes.keal:20:12\n   |\n20 |     assert(r[0] == \"3\", \"the exit code is the child's: ${r[0]}\")\n   |            ^\n";
static const char KL133[] = "  --> tests/programs/processes.keal:20:58\n   |\n20 |     assert(r[0] == \"3\", \"the exit code is the child's: ${r[0]}\")\n   |                                                          ^\n";
static const char KL134[] = "  --> tests/programs/processes.keal:20:5\n   |\n20 |     assert(r[0] == \"3\", \"the exit code is the child's: ${r[0]}\")\n   |     ^\n";
static const char KL135[] = "  --> tests/programs/processes.keal:21:18\n   |\n21 |     assert(lines(r[1]) == [\"out\"], \"standard output: ${r[1]}\")\n   |                  ^\n";
static const char KF136[] = "  in `lines` at tests/programs/processes.keal:21:12";
static const char KL137[] = "  --> tests/programs/processes.keal:21:12\n   |\n21 |     assert(lines(r[1]) == [\"out\"], \"standard output: ${r[1]}\")\n   |            ^\n";
static const char KL138[] = "  --> tests/programs/processes.keal:21:56\n   |\n21 |     assert(lines(r[1]) == [\"out\"], \"standard output: ${r[1]}\")\n   |                                                        ^\n";
static const char KL139[] = "  --> tests/programs/processes.keal:21:5\n   |\n21 |     assert(lines(r[1]) == [\"out\"], \"standard output: ${r[1]}\")\n   |     ^\n";
static const char KL140[] = "  --> tests/programs/processes.keal:22:18\n   |\n22 |     assert(lines(r[2]) == [\"err\"], \"standard error, kept apart: ${r[2]}\")\n   |                  ^\n";
static const char KF141[] = "  in `lines` at tests/programs/processes.keal:22:12";
static const char KL142[] = "  --> tests/programs/processes.keal:22:12\n   |\n22 |     assert(lines(r[2]) == [\"err\"], \"standard error, kept apart: ${r[2]}\")\n   |            ^\n";
static const char KL143[] = "  --> tests/programs/processes.keal:22:67\n   |\n22 |     assert(lines(r[2]) == [\"err\"], \"standard error, kept apart: ${r[2]}\")\n   |                                                                   ^\n";
static const char KL144[] = "  --> tests/programs/processes.keal:22:5\n   |\n22 |     assert(lines(r[2]) == [\"err\"], \"standard error, kept apart: ${r[2]}\")\n   |     ^\n";
static const char KL145[] = "  --> tests/programs/processes.keal:26:33\n   |\n26 |     assert(failed.size == 3 and failed[0] == \"42\", \"a failure still reports\")\n   |                                 ^\n";
static const char KL146[] = "  --> tests/programs/processes.keal:26:5\n   |\n26 |     assert(failed.size == 3 and failed[0] == \"42\", \"a failure still reports\")\n   |     ^\n";
static const char KL147[] = "  --> tests/programs/processes.keal:30:12\n   |\n30 |     assert(spaced[1] == \"a b c\", \"one argument stayed one argument: ${spaced[1]}\")\n   |            ^\n";
static const char KL148[] = "  --> tests/programs/processes.keal:30:71\n   |\n30 |     assert(spaced[1] == \"a b c\", \"one argument stayed one argument: ${spaced[1]}\")\n   |                                                                       ^\n";
static const char KL149[] = "  --> tests/programs/processes.keal:30:5\n   |\n30 |     assert(spaced[1] == \"a b c\", \"one argument stayed one argument: ${spaced[1]}\")\n   |     ^\n";
static const char KF150[] = "  in `isFile` at tests/programs/processes.keal:43:13";
static const char KL151[] = "  --> tests/programs/processes.keal:43:13\n   |\n43 | val shell = isFile(\"/bin/sh\") ? \"sh\" : \"cmd\"\n   |             ^\n";
static const char KF152[] = "  in `isFile` at tests/programs/processes.keal:44:12";
static const char KL153[] = "  --> tests/programs/processes.keal:44:12\n   |\n44 | val flag = isFile(\"/bin/sh\") ? \"-c\" : \"/c\"\n   |            ^\n";
static const char KF154[] = "  in `isFile` at tests/programs/processes.keal:45:5";
static const char KL155[] = "  --> tests/programs/processes.keal:45:5\n   |\n45 | if (isFile(\"/bin/sh\") or isFile(\"C:/Windows/System32/cmd.exe\")) {\n   |     ^\n";
static const char KF156[] = "  in `isFile` at tests/programs/processes.keal:45:26";
static const char KL157[] = "  --> tests/programs/processes.keal:45:26\n   |\n45 | if (isFile(\"/bin/sh\") or isFile(\"C:/Windows/System32/cmd.exe\")) {\n   |                          ^\n";
static const char KL158[] = "  --> tests/programs/processes.keal:47:5\n   |\n47 |     assert(named.size == 3, \"`${shell}` is on this machine, but not reachable by name\")\n   |     ^\n";
static const char KL159[] = "  --> tests/programs/processes.keal:48:12\n   |\n48 |     assert(named[0] == \"7\", \"the named program ran: ${named[0]}\")\n   |            ^\n";
static const char KL160[] = "  --> tests/programs/processes.keal:48:55\n   |\n48 |     assert(named[0] == \"7\", \"the named program ran: ${named[0]}\")\n   |                                                       ^\n";
static const char KL161[] = "  --> tests/programs/processes.keal:48:5\n   |\n48 |     assert(named[0] == \"7\", \"the named program ran: ${named[0]}\")\n   |     ^\n";
static const char KL162[] = "  --> tests/programs/processes.keal:55:1\n   |\n55 | assert(runCommand([\"./nowhere-at-all/\" + shell, flag, \"exit 3\"]) == null,\n   | ^\n";
static const char KF163[] = "  in `isFile` at tests/programs/processes.keal:61:5";
static const char KL164[] = "  --> tests/programs/processes.keal:61:5\n   |\n61 | if (isFile(\"/bin/sh\")) {\n   |     ^\n";
static const char KL165[] = "  --> tests/programs/processes.keal:63:33\n   |\n63 |     assert(viaDir.size == 3 and viaDir[0] == \"5\", \"a path with a directory is used as one\")\n   |                                 ^\n";
static const char KL166[] = "  --> tests/programs/processes.keal:63:5\n   |\n63 |     assert(viaDir.size == 3 and viaDir[0] == \"5\", \"a path with a directory is used as one\")\n   |     ^\n";
static const char KF167[] = "  in `isFile` at tests/programs/processes.keal:74:13";
static const char KL168[] = "  --> tests/programs/processes.keal:74:13\n   |\n74 | val built = isFile(\"target/release/keal.exe\") ? \"./target/release/keal.exe\" : \"./target/release/keal\"\n   |             ^\n";
static const char KL169[] = "  --> tests/programs/processes.keal:76:26\n   |\n76 | assert(own.size == 3 and own[0] == \"0\",\n   |                          ^\n";
static const char KL170[] = "  --> tests/programs/processes.keal:76:1\n   |\n76 | assert(own.size == 3 and own[0] == \"0\",\n   | ^\n";
static const char KF171[] = "  in `isFile` at tests/programs/processes.keal:81:5";
static const char KL172[] = "  --> tests/programs/processes.keal:81:5\n   |\n81 | if (isFile(\"/bin/sh\")) {\n   |     ^\n";
static const char KL173[] = "  --> tests/programs/processes.keal:83:12\n   |\n83 |     assert(fed[1] == \"bonjour\", \"the child read what it was sent: ${fed[1]}\")\n   |            ^\n";
static const char KL174[] = "  --> tests/programs/processes.keal:83:69\n   |\n83 |     assert(fed[1] == \"bonjour\", \"the child read what it was sent: ${fed[1]}\")\n   |                                                                     ^\n";
static const char KL175[] = "  --> tests/programs/processes.keal:83:5\n   |\n83 |     assert(fed[1] == \"bonjour\", \"the child read what it was sent: ${fed[1]}\")\n   |     ^\n";
static const char KL176[] = "  --> tests/programs/processes.keal:85:12\n   |\n85 |     assert(counted[1].trim() == \"5\", \"and all of it: ${counted[1].trim()}\")\n   |            ^\n";
static const char KL177[] = "  --> tests/programs/processes.keal:85:56\n   |\n85 |     assert(counted[1].trim() == \"5\", \"and all of it: ${counted[1].trim()}\")\n   |                                                        ^\n";
static const char KL178[] = "  --> tests/programs/processes.keal:85:5\n   |\n85 |     assert(counted[1].trim() == \"5\", \"and all of it: ${counted[1].trim()}\")\n   |     ^\n";
static const char KL179[] = "  --> tests/programs/processes.keal:87:12\n   |\n87 |     assert(none[1].trim() == \"end\", \"an empty input is end of file at once\")\n   |            ^\n";
static const char KL180[] = "  --> tests/programs/processes.keal:87:5\n   |\n87 |     assert(none[1].trim() == \"end\", \"an empty input is end of file at once\")\n   |     ^\n";
static const char KL181[] = "  --> tests/programs/processes.keal:97:38\n   |\n97 |     while (k < 15) { big = big + big k++ }\n   |                                      ^\n";
static const char KL182[] = "  --> tests/programs/processes.keal:99:12\n   |\n99 |     assert(echoed[1].length == big.length, \"every byte came back: ${echoed[1].length}\")\n   |            ^\n";
static const char KL183[] = "  --> tests/programs/processes.keal:99:69\n   |\n99 |     assert(echoed[1].length == big.length, \"every byte came back: ${echoed[1].length}\")\n   |                                                                     ^\n";
static const char KL184[] = "  --> tests/programs/processes.keal:99:5\n   |\n99 |     assert(echoed[1].length == big.length, \"every byte came back: ${echoed[1].length}\")\n   |     ^\n";
static const char KL185[] = "  --> tests/programs/processes.keal:100:12\n    |\n100 |     assert(echoed[0] == \"0\", \"and the child ended well\")\n    |            ^\n";
static const char KL186[] = "  --> tests/programs/processes.keal:100:5\n    |\n100 |     assert(echoed[0] == \"0\", \"and the child ended well\")\n    |     ^\n";
static const char KL187[] = "  --> tests/programs/processes.keal:105:31\n    |\n105 |     assert(deaf.size == 3 and deaf[0] == \"7\", \"a child that ignores its input still reports\")\n    |                               ^\n";
static const char KL188[] = "  --> tests/programs/processes.keal:105:5\n    |\n105 |     assert(deaf.size == 3 and deaf[0] == \"7\", \"a child that ignores its input still reports\")\n    |     ^\n";

static KealStr* _str0 = NULL;  /* T */
static KealStr* _str1 = NULL;  /* Z */
static KealStr* _str2 = NULL;  /* + */
static KealStr* _str3 = NULL;  /* - */
static KealStr* _str4 = NULL;  /* : */
static KealStr* _str5 = NULL;  /* 0 */
static KealStr* _str6 = NULL;  /*  */
static KealStr* _str7 = NULL;  /* `padStart` needs a non-empty pad */
static KealStr* _str8 = NULL;  /* `padEnd` needs a non-empty pad */
static KealStr* _str9 = NULL;  /*   */
static KealStr* _str10 = NULL;  /* January */
static KealStr* _str11 = NULL;  /* February */
static KealStr* _str12 = NULL;  /* March */
static KealStr* _str13 = NULL;  /* April */
static KealStr* _str14 = NULL;  /* May */
static KealStr* _str15 = NULL;  /* June */
static KealStr* _str16 = NULL;  /* July */
static KealStr* _str17 = NULL;  /* August */
static KealStr* _str18 = NULL;  /* September */
static KealStr* _str19 = NULL;  /* October */
static KealStr* _str20 = NULL;  /* November */
static KealStr* _str21 = NULL;  /* December */
static KealStr* _str22 = NULL;  /* ? */
static KealStr* _str23 = NULL;  /* Sunday */
static KealStr* _str24 = NULL;  /* Monday */
static KealStr* _str25 = NULL;  /* Tuesday */
static KealStr* _str26 = NULL;  /* Wednesday */
static KealStr* _str27 = NULL;  /* Thursday */
static KealStr* _str28 = NULL;  /* Friday */
static KealStr* _str29 = NULL;  /* Saturday */
static KealStr* _str30 = NULL;  /* / */
static KealStr* _str31 = NULL;  /* an empty vector names no program */
static KealStr* _str32 = NULL;  /* keal-no-such-program-1234 */
static KealStr* _str33 = NULL;  /* nor does a missing one */
static KealStr* _str34 = NULL;  /* /bin/sh */
static KealStr* _str35 = NULL;  /* -c */
static KealStr* _str36 = NULL;  /* echo out; echo err 1>&2; exit 3 */
static KealStr* _str37 = NULL;  /* a shell that exists starts */
static KealStr* _str38 = NULL;  /* three parts: code, output, error */
static KealStr* _str39 = NULL;  /* 3 */
static KealStr* _str40 = NULL;  /* the exit code is the child's:  */
static KealStr* _str41 = NULL;  /* out */
static KealStr* _str42 = NULL;  /* standard output:  */
static KealStr* _str43 = NULL;  /* err */
static KealStr* _str44 = NULL;  /* standard error, kept apart:  */
static KealStr* _str45 = NULL;  /* exit 42 */
static KealStr* _str46 = NULL;  /* 42 */
static KealStr* _str47 = NULL;  /* a failure still reports */
static KealStr* _str48 = NULL;  /* printf %s "$1" */
static KealStr* _str49 = NULL;  /* sh */
static KealStr* _str50 = NULL;  /* a b c */
static KealStr* _str51 = NULL;  /* one argument stayed one argument:  */
static KealStr* _str52 = NULL;  /* cmd */
static KealStr* _str53 = NULL;  /* /c */
static KealStr* _str54 = NULL;  /* C:/Windows/System32/cmd.exe */
static KealStr* _str55 = NULL;  /* exit 7 */
static KealStr* _str56 = NULL;  /* ` */
static KealStr* _str57 = NULL;  /* ` is on this machine, but not reachable … */
static KealStr* _str58 = NULL;  /* 7 */
static KealStr* _str59 = NULL;  /* the named program ran:  */
static KealStr* _str60 = NULL;  /* ./nowhere-at-all/ */
static KealStr* _str61 = NULL;  /* exit 3 */
static KealStr* _str62 = NULL;  /* a path with a directory in it is not loo… */
static KealStr* _str63 = NULL;  /* /bin/../bin/sh */
static KealStr* _str64 = NULL;  /* exit 5 */
static KealStr* _str65 = NULL;  /* 5 */
static KealStr* _str66 = NULL;  /* a path with a directory is used as one */
static KealStr* _str67 = NULL;  /* target/release/keal.exe */
static KealStr* _str68 = NULL;  /* ./target/release/keal.exe */
static KealStr* _str69 = NULL;  /* ./target/release/keal */
static KealStr* _str70 = NULL;  /* version */
static KealStr* _str71 = NULL;  /* a relative path with directories runs, o… */
static KealStr* _str72 = NULL;  /* cat */
static KealStr* _str73 = NULL;  /* bonjour */
static KealStr* _str74 = NULL;  /* the child read what it was sent:  */
static KealStr* _str75 = NULL;  /* wc -c */
static KealStr* _str76 = NULL;  /* 12345 */
static KealStr* _str77 = NULL;  /* and all of it:  */
static KealStr* _str78 = NULL;  /* cat; echo end */
static KealStr* _str79 = NULL;  /* end */
static KealStr* _str80 = NULL;  /* an empty input is end of file at once */
static KealStr* _str81 = NULL;  /* 0123456789 */
static KealStr* _str82 = NULL;  /* every byte came back:  */
static KealStr* _str83 = NULL;  /* and the child ended well */
static KealStr* _str84 = NULL;  /* a child that ignores its input still rep… */

static void keal_init_literals(void) {
    _str0 = keal_str_static("T", 1);
    _str1 = keal_str_static("Z", 1);
    _str2 = keal_str_static("+", 1);
    _str3 = keal_str_static("-", 1);
    _str4 = keal_str_static(":", 1);
    _str5 = keal_str_static("0", 1);
    _str6 = keal_str_static("", 0);
    _str7 = keal_str_static("`padStart` needs a non-empty pad", 32);
    _str8 = keal_str_static("`padEnd` needs a non-empty pad", 30);
    _str9 = keal_str_static("\n", 1);
    _str10 = keal_str_static("January", 7);
    _str11 = keal_str_static("February", 8);
    _str12 = keal_str_static("March", 5);
    _str13 = keal_str_static("April", 5);
    _str14 = keal_str_static("May", 3);
    _str15 = keal_str_static("June", 4);
    _str16 = keal_str_static("July", 4);
    _str17 = keal_str_static("August", 6);
    _str18 = keal_str_static("September", 9);
    _str19 = keal_str_static("October", 7);
    _str20 = keal_str_static("November", 8);
    _str21 = keal_str_static("December", 8);
    _str22 = keal_str_static("?", 1);
    _str23 = keal_str_static("Sunday", 6);
    _str24 = keal_str_static("Monday", 6);
    _str25 = keal_str_static("Tuesday", 7);
    _str26 = keal_str_static("Wednesday", 9);
    _str27 = keal_str_static("Thursday", 8);
    _str28 = keal_str_static("Friday", 6);
    _str29 = keal_str_static("Saturday", 8);
    _str30 = keal_str_static("/", 1);
    _str31 = keal_str_static("an empty vector names no program", 32);
    _str32 = keal_str_static("keal-no-such-program-1234", 25);
    _str33 = keal_str_static("nor does a missing one", 22);
    _str34 = keal_str_static("/bin/sh", 7);
    _str35 = keal_str_static("-c", 2);
    _str36 = keal_str_static("echo out; echo err 1>&2; exit 3", 31);
    _str37 = keal_str_static("a shell that exists starts", 26);
    _str38 = keal_str_static("three parts: code, output, error", 32);
    _str39 = keal_str_static("3", 1);
    _str40 = keal_str_static("the exit code is the child's: ", 30);
    _str41 = keal_str_static("out", 3);
    _str42 = keal_str_static("standard output: ", 17);
    _str43 = keal_str_static("err", 3);
    _str44 = keal_str_static("standard error, kept apart: ", 28);
    _str45 = keal_str_static("exit 42", 7);
    _str46 = keal_str_static("42", 2);
    _str47 = keal_str_static("a failure still reports", 23);
    _str48 = keal_str_static("printf %s \"$1\"", 14);
    _str49 = keal_str_static("sh", 2);
    _str50 = keal_str_static("a b c", 5);
    _str51 = keal_str_static("one argument stayed one argument: ", 34);
    _str52 = keal_str_static("cmd", 3);
    _str53 = keal_str_static("/c", 2);
    _str54 = keal_str_static("C:/Windows/System32/cmd.exe", 27);
    _str55 = keal_str_static("exit 7", 6);
    _str56 = keal_str_static("`", 1);
    _str57 = keal_str_static("` is on this machine, but not reachable by name", 47);
    _str58 = keal_str_static("7", 1);
    _str59 = keal_str_static("the named program ran: ", 23);
    _str60 = keal_str_static("./nowhere-at-all/", 17);
    _str61 = keal_str_static("exit 3", 6);
    _str62 = keal_str_static("a path with a directory in it is not looked up on the search path", 65);
    _str63 = keal_str_static("/bin/../bin/sh", 14);
    _str64 = keal_str_static("exit 5", 6);
    _str65 = keal_str_static("5", 1);
    _str66 = keal_str_static("a path with a directory is used as one", 38);
    _str67 = keal_str_static("target/release/keal.exe", 23);
    _str68 = keal_str_static("./target/release/keal.exe", 25);
    _str69 = keal_str_static("./target/release/keal", 21);
    _str70 = keal_str_static("version", 7);
    _str71 = keal_str_static("a relative path with directories runs, or you are not at the repository root: ", 78);
    _str72 = keal_str_static("cat", 3);
    _str73 = keal_str_static("bonjour", 7);
    _str74 = keal_str_static("the child read what it was sent: ", 33);
    _str75 = keal_str_static("wc -c", 5);
    _str76 = keal_str_static("12345", 5);
    _str77 = keal_str_static("and all of it: ", 15);
    _str78 = keal_str_static("cat; echo end", 13);
    _str79 = keal_str_static("end", 3);
    _str80 = keal_str_static("an empty input is end of file at once", 37);
    _str81 = keal_str_static("0123456789", 10);
    _str82 = keal_str_static("every byte came back: ", 22);
    _str83 = keal_str_static("and the child ended well", 24);
    _str84 = keal_str_static("a child that ignores its input still reports", 44);
}

void keal_runtime_init(void) {
    static bool done = false;
    if (done) { return; }
    done = true;
    keal_init_literals();
}

typedef struct K_DateTime K_DateTime;

struct K_DateTime {
    keal_rc_t rc;
    int64_t k_year;
    int64_t k_month;
    int64_t k_day;
    int64_t k_hour;
    int64_t k_minute;
    int64_t k_second;
    int64_t k_weekday;
    int64_t k_offset;
};

static KealStr* k_shell;
static KealStr* k_flag;
static KealStr* k_built;
static KealList* k_own;

K_DateTime* K_DateTime_retain(K_DateTime* o);
void K_DateTime_release(K_DateTime* o);
KealStr* K_DateTime_show(K_DateTime* o);
K_DateTime* K_DateTime_new(int64_t k_year, int64_t k_month, int64_t k_day, int64_t k_hour, int64_t k_minute, int64_t k_second, int64_t k_weekday, int64_t k_offset);
KealStr* K_DateTime_m_iso(K_DateTime* self);
KealStr* K_DateTime_m_zone(K_DateTime* self);
KealStr* K_DateTime_m_date(K_DateTime* self);
KealStr* K_DateTime_m_clock(K_DateTime* self);
KealStr* K_DateTime_m_toString(K_DateTime* self);
int64_t K_DateTime_m_epochSeconds(K_DateTime* self);
K_DateTime* K_DateTime_m_inUtc(K_DateTime* self);
K_DateTime* K_DateTime_m_inLocalTime(K_DateTime* self);
bool K_DateTime_m_equals(K_DateTime* self, K_DateTime* k_other);
K_DateTime* K_DateTime_m_with(K_DateTime* self, int64_t k_year, int64_t k_month, int64_t k_day, int64_t k_hour, int64_t k_minute, int64_t k_second, int64_t k_weekday, int64_t k_offset);
KealStr* k_padStart(KealStr* k_s, int64_t k_width, KealStr* k_pad);
KealStr* k_padEnd(KealStr* k_s, int64_t k_width, KealStr* k_pad);
KealList* k_lines(KealStr* k_s);
KealStr* k_twoDigits(int64_t k_n);
int64_t k_floorDiv(int64_t k_a, int64_t k_b);
int64_t k_floorMod(int64_t k_a, int64_t k_b);
K_DateTime* k_utcAt(int64_t k_seconds);
K_DateTime* k_localAt(int64_t k_seconds);
K_DateTime* k_localNow(void);
int64_t k_daysFromCivil(int64_t k_year, int64_t k_month, int64_t k_day);
K_DateTime* k_utcNow(void);
KealStr* k_monthName(int64_t k_month);
KealStr* k_weekdayName(int64_t k_weekday);
bool k_isLeapYear(int64_t k_year);
bool k_exists(KealStr* k_path);
bool k_isFile(KealStr* k_path);
bool k_isDir(KealStr* k_path);
KealList* k_walkDir(KealStr* k_path);
int keal_program_run(void);

static void rel_keal_str(void* p) { keal_str_release((KealStr*)p); }

K_DateTime* K_DateTime_retain(K_DateTime* o) {
    if (o != NULL) { KEAL_RC_BUMP(o->rc); }
    return o;
}

void K_DateTime_release(K_DateTime* o) {
    if (o == NULL) { return; }
    if (KEAL_RC_DROP(o->rc)) { return; }
    free(o);
}

KealStr* K_DateTime_show(K_DateTime* o) {
    return K_DateTime_m_toString(o);
}

K_DateTime* K_DateTime_new(int64_t k_year, int64_t k_month, int64_t k_day, int64_t k_hour, int64_t k_minute, int64_t k_second, int64_t k_weekday, int64_t k_offset) {
    K_DateTime* self = (K_DateTime*)keal_alloc(sizeof(K_DateTime));
    self->rc = 1;
    self->k_year = k_year;
    self->k_month = k_month;
    self->k_day = k_day;
    self->k_hour = k_hour;
    self->k_minute = k_minute;
    self->k_second = k_second;
    self->k_weekday = k_weekday;
    self->k_offset = k_offset;
    return self;
}

KealStr* K_DateTime_m_iso(K_DateTime* self) {
    KealStr* _t1 = K_DateTime_m_date(self);
    KealStr* _t2 = keal_concat(_t1, keal_str_retain(_str0));
    KealStr* _t3 = K_DateTime_m_clock(self);
    KealStr* _t4 = keal_concat(_t2, _t3);
    KealStr* _t5 = K_DateTime_m_zone(self);
    KealStr* _t6 = keal_concat(_t4, _t5);
    keal_str_release(_t5);
    keal_str_release(_t4);
    keal_str_release(_t3);
    keal_str_release(_t2);
    keal_str_release(_t1);
    return _t6;
    keal_str_release(_t5);
    keal_str_release(_t4);
    keal_str_release(_t3);
    keal_str_release(_t2);
    keal_str_release(_t1);
}

KealStr* K_DateTime_m_zone(K_DateTime* self) {
    const bool _t1 = (self->k_offset == INT64_C(0));
    if (_t1) {
        KealStr* _t2 = keal_str_retain(_str1);
        return _t2;
    }
    KEAL_LOCAL int64_t k_east = self->k_offset;
    KealStr* _t3 = keal_str_retain(_str2);
    KEAL_LOCAL KealStr* k_sign = keal_str_retain(_t3);
    const bool _t4 = (k_east < INT64_C(0));
    if (_t4) {
        KealStr* _t5 = keal_str_retain(_str3);
        keal_str_release(k_sign);
        k_sign = keal_str_retain(_t5);
        k_east = (-k_east);
        keal_str_release(_t5);
    }
    KealStr* _t6 = keal_str_retain(k_sign);
    const int64_t _t7 = keal_div(k_east, INT64_C(3600), KL0);
    keal_enter(KF1, KL2);
    KealStr* _t8 = k_twoDigits(_t7);
    keal_leave();
    KealStr* _t9 = keal_concat(_t6, _t8);
    KealStr* _t10 = keal_concat(_t9, keal_str_retain(_str4));
    const int64_t _t11 = keal_div(k_east, INT64_C(60), KL3);
    const int64_t _t12 = keal_rem(_t11, INT64_C(60), KL4);
    keal_enter(KF5, KL6);
    KealStr* _t13 = k_twoDigits(_t12);
    keal_leave();
    KealStr* _t14 = keal_concat(_t10, _t13);
    keal_str_release(_t13);
    keal_str_release(_t10);
    keal_str_release(_t9);
    keal_str_release(_t8);
    keal_str_release(_t6);
    keal_str_release(k_sign);
    keal_str_release(_t3);
    return _t14;
    keal_str_release(_t13);
    keal_str_release(_t10);
    keal_str_release(_t9);
    keal_str_release(_t8);
    keal_str_release(_t6);
    keal_str_release(k_sign);
    keal_str_release(_t3);
}

KealStr* K_DateTime_m_date(K_DateTime* self) {
    KealStr* _t1 = keal_str_from_int(self->k_year);
    KealStr* _t2 = keal_str_retain(_str5);
    keal_enter(KF7, KL8);
    KealStr* _t3 = k_padStart(_t1, INT64_C(4), _t2);
    keal_leave();
    KealStr* _t4 = keal_concat(_t3, keal_str_retain(_str3));
    keal_enter(KF9, KL10);
    KealStr* _t5 = k_twoDigits(self->k_month);
    keal_leave();
    KealStr* _t6 = keal_concat(_t4, _t5);
    KealStr* _t7 = keal_concat(_t6, keal_str_retain(_str3));
    keal_enter(KF11, KL12);
    KealStr* _t8 = k_twoDigits(self->k_day);
    keal_leave();
    KealStr* _t9 = keal_concat(_t7, _t8);
    keal_str_release(_t8);
    keal_str_release(_t7);
    keal_str_release(_t6);
    keal_str_release(_t5);
    keal_str_release(_t4);
    keal_str_release(_t3);
    keal_str_release(_t2);
    keal_str_release(_t1);
    return _t9;
    keal_str_release(_t8);
    keal_str_release(_t7);
    keal_str_release(_t6);
    keal_str_release(_t5);
    keal_str_release(_t4);
    keal_str_release(_t3);
    keal_str_release(_t2);
    keal_str_release(_t1);
}

KealStr* K_DateTime_m_clock(K_DateTime* self) {
    keal_enter(KF13, KL14);
    KealStr* _t1 = k_twoDigits(self->k_hour);
    keal_leave();
    KealStr* _t2 = keal_concat(_t1, keal_str_retain(_str4));
    keal_enter(KF15, KL16);
    KealStr* _t3 = k_twoDigits(self->k_minute);
    keal_leave();
    KealStr* _t4 = keal_concat(_t2, _t3);
    KealStr* _t5 = keal_concat(_t4, keal_str_retain(_str4));
    keal_enter(KF17, KL18);
    KealStr* _t6 = k_twoDigits(self->k_second);
    keal_leave();
    KealStr* _t7 = keal_concat(_t5, _t6);
    keal_str_release(_t6);
    keal_str_release(_t5);
    keal_str_release(_t4);
    keal_str_release(_t3);
    keal_str_release(_t2);
    keal_str_release(_t1);
    return _t7;
    keal_str_release(_t6);
    keal_str_release(_t5);
    keal_str_release(_t4);
    keal_str_release(_t3);
    keal_str_release(_t2);
    keal_str_release(_t1);
}

KealStr* K_DateTime_m_toString(K_DateTime* self) {
    KealStr* _t1 = K_DateTime_m_iso(self);
    return _t1;
}

int64_t K_DateTime_m_epochSeconds(K_DateTime* self) {
    keal_enter(KF19, KL20);
    const int64_t _t1 = k_daysFromCivil(self->k_year, self->k_month, self->k_day);
    keal_leave();
    KEAL_LOCAL int64_t k_days = _t1;
    const int64_t _t2 = keal_mul(self->k_hour, INT64_C(3600), KL21);
    const int64_t _t3 = keal_mul(self->k_minute, INT64_C(60), KL22);
    const int64_t _t4 = keal_add(_t2, _t3, KL23);
    const int64_t _t5 = keal_add(_t4, self->k_second, KL24);
    KEAL_LOCAL int64_t k_ofDay = _t5;
    const int64_t _t6 = keal_mul(k_days, INT64_C(86400), KL25);
    const int64_t _t7 = keal_add(_t6, k_ofDay, KL26);
    const int64_t _t8 = keal_sub(_t7, self->k_offset, KL27);
    int64_t _t9 = _t8;
    return _t9;
}

K_DateTime* K_DateTime_m_inUtc(K_DateTime* self) {
    const int64_t _t1 = K_DateTime_m_epochSeconds(self);
    keal_enter(KF28, KL29);
    K_DateTime* _t2 = k_utcAt(_t1);
    keal_leave();
    return _t2;
}

K_DateTime* K_DateTime_m_inLocalTime(K_DateTime* self) {
    const int64_t _t1 = K_DateTime_m_epochSeconds(self);
    keal_enter(KF30, KL31);
    K_DateTime* _t2 = k_localAt(_t1);
    keal_leave();
    return _t2;
}

bool K_DateTime_m_equals(K_DateTime* self, K_DateTime* k_other) {
    bool _t1;
    if (!(self->k_year == k_other->k_year)) {
        _t1 = false;
    } else {
        _t1 = ((self->k_year == k_other->k_year) && (self->k_month == k_other->k_month));
    }
    bool _t2;
    if (!(_t1)) {
        _t2 = false;
    } else {
        _t2 = (_t1 && (self->k_day == k_other->k_day));
    }
    bool _t3;
    if (!(_t2)) {
        _t3 = false;
    } else {
        _t3 = (_t2 && (self->k_hour == k_other->k_hour));
    }
    bool _t4;
    if (!(_t3)) {
        _t4 = false;
    } else {
        _t4 = (_t3 && (self->k_minute == k_other->k_minute));
    }
    bool _t5;
    if (!(_t4)) {
        _t5 = false;
    } else {
        _t5 = (_t4 && (self->k_second == k_other->k_second));
    }
    bool _t6;
    if (!(_t5)) {
        _t6 = false;
    } else {
        _t6 = (_t5 && (self->k_weekday == k_other->k_weekday));
    }
    bool _t7;
    if (!(_t6)) {
        _t7 = false;
    } else {
        _t7 = (_t6 && (self->k_offset == k_other->k_offset));
    }
    bool _t8 = _t7;
    return _t8;
}

K_DateTime* K_DateTime_m_with(K_DateTime* self, int64_t k_year, int64_t k_month, int64_t k_day, int64_t k_hour, int64_t k_minute, int64_t k_second, int64_t k_weekday, int64_t k_offset) {
    K_DateTime* _t1 = K_DateTime_new(k_year, k_month, k_day, k_hour, k_minute, k_second, k_weekday, k_offset);
    return _t1;
}

KealStr* k_padStart(KealStr* k_s, int64_t k_width, KealStr* k_pad) {
    KealStr* _t1 = keal_str_retain(k_pad);
    KealStr* _t2 = keal_str_retain(_str6);
    const bool _t3 = keal_str_cmp(_t1, _t2) == 0;
    const bool _t4 = _t3;
    if (_t4) {
        KealStr* _t5 = keal_str_retain(_str7);
        keal_panic(_t5->bytes, NULL, KL32);
        keal_str_release(_t5);
    }
    KealStr* _t6 = keal_str_retain(k_s);
    KEAL_LOCAL KealStr* k_out = keal_str_retain(_t6);
    while (1) {
        KealStr* _t7 = keal_str_retain(k_out);
        const bool _t8 = (keal_str_length(_t7) < k_width);
        if (!_t8) {
            keal_str_release(_t7);
            break;
        }
        KealStr* _t9 = keal_str_retain(k_pad);
        KealStr* _t10 = keal_str_retain(k_out);
        KealStr* _t11 = keal_concat(_t9, _t10);
        keal_str_release(k_out);
        k_out = keal_str_retain(_t11);
        keal_str_release(_t11);
        keal_str_release(_t10);
        keal_str_release(_t9);
        keal_str_release(_t7);
    }
    KealStr* _t12 = keal_str_retain(k_out);
    keal_str_release(k_out);
    keal_str_release(_t6);
    keal_str_release(_t2);
    keal_str_release(_t1);
    return _t12;
    keal_str_release(k_out);
    keal_str_release(_t6);
    keal_str_release(_t2);
    keal_str_release(_t1);
}

KealStr* k_padEnd(KealStr* k_s, int64_t k_width, KealStr* k_pad) {
    KealStr* _t1 = keal_str_retain(k_pad);
    KealStr* _t2 = keal_str_retain(_str6);
    const bool _t3 = keal_str_cmp(_t1, _t2) == 0;
    const bool _t4 = _t3;
    if (_t4) {
        KealStr* _t5 = keal_str_retain(_str8);
        keal_panic(_t5->bytes, NULL, KL33);
        keal_str_release(_t5);
    }
    KealStr* _t6 = keal_str_retain(k_s);
    KEAL_LOCAL KealStr* k_out = keal_str_retain(_t6);
    while (1) {
        KealStr* _t7 = keal_str_retain(k_out);
        const bool _t8 = (keal_str_length(_t7) < k_width);
        if (!_t8) {
            keal_str_release(_t7);
            break;
        }
        KealStr* _t9 = keal_str_retain(k_out);
        KealStr* _t10 = keal_str_retain(k_pad);
        KealStr* _t11 = keal_concat(_t9, _t10);
        keal_str_release(k_out);
        k_out = keal_str_retain(_t11);
        keal_str_release(_t11);
        keal_str_release(_t10);
        keal_str_release(_t9);
        keal_str_release(_t7);
    }
    KealStr* _t12 = keal_str_retain(k_out);
    keal_str_release(k_out);
    keal_str_release(_t6);
    keal_str_release(_t2);
    keal_str_release(_t1);
    return _t12;
    keal_str_release(k_out);
    keal_str_release(_t6);
    keal_str_release(_t2);
    keal_str_release(_t1);
}

KealList* k_lines(KealStr* k_s) {
    KealStr* _t1 = keal_str_retain(k_s);
    KEAL_LOCAL KealStr* k_text = keal_str_retain(_t1);
    KealStr* _t2 = keal_str_retain(k_text);
    KealStr* _t3 = keal_str_retain(_str9);
    const bool _t4 = keal_str_ends_with(_t2, _t3);
    const bool _t5 = _t4;
    if (_t5) {
        KealStr* _t6 = keal_str_retain(k_text);
        KealStr* _t7 = keal_str_retain(k_text);
        const int64_t _t8 = keal_sub(keal_str_length(_t7), INT64_C(1), KL34);
        KealStr* _t9 = keal_str_substring(_t6, INT64_C(0), _t8, KL35);
        keal_str_release(k_text);
        k_text = keal_str_retain(_t9);
        keal_str_release(_t9);
        keal_str_release(_t7);
        keal_str_release(_t6);
    }
    KealStr* _t10 = keal_str_retain(k_text);
    KealStr* _t11 = keal_str_retain(_str6);
    const bool _t12 = keal_str_cmp(_t10, _t11) == 0;
    const bool _t13 = _t12;
    if (_t13) {
        KealList* _t14 = keal_list_new(rel_keal_str);
        keal_str_release(_t11);
        keal_str_release(_t10);
        keal_str_release(_t3);
        keal_str_release(_t2);
        keal_str_release(k_text);
        keal_str_release(_t1);
        return _t14;
    }
    KealStr* _t15 = keal_str_retain(k_text);
    KealStr* _t16 = keal_str_retain(_str9);
    KealList* _t17 = keal_str_split(_t15, _t16);
    keal_str_release(_t16);
    keal_str_release(_t15);
    keal_str_release(_t11);
    keal_str_release(_t10);
    keal_str_release(_t3);
    keal_str_release(_t2);
    keal_str_release(k_text);
    keal_str_release(_t1);
    return _t17;
    keal_str_release(_t16);
    keal_str_release(_t15);
    keal_str_release(_t11);
    keal_str_release(_t10);
    keal_str_release(_t3);
    keal_str_release(_t2);
    keal_str_release(k_text);
    keal_str_release(_t1);
}

KealStr* k_twoDigits(int64_t k_n) {
    KealStr* _t1 = keal_str_from_int(k_n);
    KealStr* _t2 = keal_str_retain(_str5);
    keal_enter(KF36, KL37);
    KealStr* _t3 = k_padStart(_t1, INT64_C(2), _t2);
    keal_leave();
    keal_str_release(_t2);
    keal_str_release(_t1);
    return _t3;
    keal_str_release(_t2);
    keal_str_release(_t1);
}

int64_t k_floorDiv(int64_t k_a, int64_t k_b) {
    const int64_t _t1 = keal_div(k_a, k_b, KL38);
    KEAL_LOCAL int64_t k_q = _t1;
    const int64_t _t2 = keal_rem(k_a, k_b, KL39);
    bool _t3;
    if (!(_t2 != INT64_C(0))) {
        _t3 = false;
    } else {
        _t3 = ((_t2 != INT64_C(0)) && ((k_a < INT64_C(0)) != (k_b < INT64_C(0))));
    }
    const bool _t4 = _t3;
    if (_t4) {
        const int64_t _t5 = keal_sub(k_q, INT64_C(1), KL40);
        k_q = _t5;
    }
    int64_t _t6 = k_q;
    return _t6;
}

int64_t k_floorMod(int64_t k_a, int64_t k_b) {
    keal_enter(KF41, KL42);
    const int64_t _t1 = k_floorDiv(k_a, k_b);
    keal_leave();
    const int64_t _t2 = keal_mul(_t1, k_b, KL43);
    const int64_t _t3 = keal_sub(k_a, _t2, KL44);
    int64_t _t4 = _t3;
    return _t4;
}

K_DateTime* k_utcAt(int64_t k_seconds) {
    keal_enter(KF45, KL46);
    const int64_t _t1 = k_floorDiv(k_seconds, INT64_C(86400));
    keal_leave();
    KEAL_LOCAL int64_t k_days = _t1;
    keal_enter(KF47, KL48);
    const int64_t _t2 = k_floorMod(k_seconds, INT64_C(86400));
    keal_leave();
    KEAL_LOCAL int64_t k_rest = _t2;
    const int64_t _t3 = keal_add(k_days, INT64_C(719468), KL49);
    KEAL_LOCAL int64_t k_z = _t3;
    keal_enter(KF50, KL51);
    const int64_t _t4 = k_floorDiv(k_z, INT64_C(146097));
    keal_leave();
    KEAL_LOCAL int64_t k_era = _t4;
    const int64_t _t5 = keal_mul(k_era, INT64_C(146097), KL52);
    const int64_t _t6 = keal_sub(k_z, _t5, KL53);
    KEAL_LOCAL int64_t k_doe = _t6;
    const int64_t _t7 = keal_div(k_doe, INT64_C(1460), KL54);
    const int64_t _t8 = keal_sub(k_doe, _t7, KL55);
    const int64_t _t9 = keal_div(k_doe, INT64_C(36524), KL56);
    const int64_t _t10 = keal_add(_t8, _t9, KL57);
    const int64_t _t11 = keal_div(k_doe, INT64_C(146096), KL58);
    const int64_t _t12 = keal_sub(_t10, _t11, KL59);
    const int64_t _t13 = keal_div(_t12, INT64_C(365), KL60);
    KEAL_LOCAL int64_t k_yoe = _t13;
    const int64_t _t14 = keal_mul(INT64_C(365), k_yoe, KL61);
    const int64_t _t15 = keal_div(k_yoe, INT64_C(4), KL62);
    const int64_t _t16 = keal_add(_t14, _t15, KL63);
    const int64_t _t17 = keal_div(k_yoe, INT64_C(100), KL64);
    const int64_t _t18 = keal_sub(_t16, _t17, KL65);
    const int64_t _t19 = keal_sub(k_doe, _t18, KL66);
    KEAL_LOCAL int64_t k_doy = _t19;
    const int64_t _t20 = keal_mul(INT64_C(5), k_doy, KL67);
    const int64_t _t21 = keal_add(_t20, INT64_C(2), KL68);
    const int64_t _t22 = keal_div(_t21, INT64_C(153), KL69);
    KEAL_LOCAL int64_t k_mp = _t22;
    const int64_t _t23 = keal_mul(INT64_C(153), k_mp, KL70);
    const int64_t _t24 = keal_add(_t23, INT64_C(2), KL71);
    const int64_t _t25 = keal_div(_t24, INT64_C(5), KL72);
    const int64_t _t26 = keal_sub(k_doy, _t25, KL73);
    const int64_t _t27 = keal_add(_t26, INT64_C(1), KL74);
    KEAL_LOCAL int64_t k_d = _t27;
    const int64_t _t28 = keal_add(k_mp, INT64_C(3), KL75);
    KEAL_LOCAL int64_t k_m = _t28;
    const bool _t29 = (k_mp >= INT64_C(10));
    if (_t29) {
        const int64_t _t30 = keal_sub(k_mp, INT64_C(9), KL76);
        k_m = _t30;
    }
    const int64_t _t31 = keal_mul(k_era, INT64_C(400), KL77);
    const int64_t _t32 = keal_add(k_yoe, _t31, KL78);
    KEAL_LOCAL int64_t k_y = _t32;
    const bool _t33 = (k_m <= INT64_C(2));
    if (_t33) {
        const int64_t _t34 = keal_add(k_y, INT64_C(1), KL79);
        k_y = _t34;
    }
    const int64_t _t35 = keal_add(k_days, INT64_C(4), KL80);
    keal_enter(KF81, KL82);
    const int64_t _t36 = k_floorMod(_t35, INT64_C(7));
    keal_leave();
    KEAL_LOCAL int64_t k_weekday = _t36;
    const int64_t _t37 = keal_div(k_rest, INT64_C(3600), KL83);
    const int64_t _t38 = keal_div(k_rest, INT64_C(60), KL84);
    const int64_t _t39 = keal_rem(_t38, INT64_C(60), KL85);
    const int64_t _t40 = keal_rem(k_rest, INT64_C(60), KL86);
    K_DateTime* _t41 = K_DateTime_new(k_y, k_m, k_d, _t37, _t39, _t40, k_weekday, INT64_C(0));
    return _t41;
}

K_DateTime* k_localAt(int64_t k_seconds) {
    const int64_t _t1 = keal_local_offset(k_seconds);
    KEAL_LOCAL int64_t k_east = _t1;
    const int64_t _t2 = keal_add(k_seconds, k_east, KL87);
    keal_enter(KF88, KL89);
    K_DateTime* _t3 = k_utcAt(_t2);
    keal_leave();
    KEAL_LOCAL K_DateTime* k_shifted = K_DateTime_retain(_t3);
    K_DateTime* _t4 = K_DateTime_new(k_shifted->k_year, k_shifted->k_month, k_shifted->k_day, k_shifted->k_hour, k_shifted->k_minute, k_shifted->k_second, k_shifted->k_weekday, k_east);
    K_DateTime_release(k_shifted);
    K_DateTime_release(_t3);
    return _t4;
    K_DateTime_release(k_shifted);
    K_DateTime_release(_t3);
}

K_DateTime* k_localNow(void) {
    const double _t1 = keal_time();
    const int64_t _t2 = keal_f2i(_t1);
    keal_enter(KF90, KL91);
    K_DateTime* _t3 = k_localAt(_t2);
    keal_leave();
    return _t3;
}

int64_t k_daysFromCivil(int64_t k_year, int64_t k_month, int64_t k_day) {
    KEAL_LOCAL int64_t k_y = k_year;
    const bool _t1 = (k_month <= INT64_C(2));
    if (_t1) {
        const int64_t _t2 = keal_sub(k_y, INT64_C(1), KL92);
        k_y = _t2;
    }
    keal_enter(KF93, KL94);
    const int64_t _t3 = k_floorDiv(k_y, INT64_C(400));
    keal_leave();
    KEAL_LOCAL int64_t k_era = _t3;
    const int64_t _t4 = keal_mul(k_era, INT64_C(400), KL95);
    const int64_t _t5 = keal_sub(k_y, _t4, KL96);
    KEAL_LOCAL int64_t k_yoe = _t5;
    const int64_t _t6 = keal_sub(k_month, INT64_C(3), KL97);
    KEAL_LOCAL int64_t k_mp = _t6;
    const bool _t7 = (k_month <= INT64_C(2));
    if (_t7) {
        const int64_t _t8 = keal_add(k_month, INT64_C(9), KL98);
        k_mp = _t8;
    }
    const int64_t _t9 = keal_mul(INT64_C(153), k_mp, KL99);
    const int64_t _t10 = keal_add(_t9, INT64_C(2), KL100);
    const int64_t _t11 = keal_div(_t10, INT64_C(5), KL101);
    const int64_t _t12 = keal_add(_t11, k_day, KL102);
    const int64_t _t13 = keal_sub(_t12, INT64_C(1), KL103);
    KEAL_LOCAL int64_t k_doy = _t13;
    const int64_t _t14 = keal_mul(k_yoe, INT64_C(365), KL104);
    const int64_t _t15 = keal_div(k_yoe, INT64_C(4), KL105);
    const int64_t _t16 = keal_add(_t14, _t15, KL106);
    const int64_t _t17 = keal_div(k_yoe, INT64_C(100), KL107);
    const int64_t _t18 = keal_sub(_t16, _t17, KL108);
    const int64_t _t19 = keal_add(_t18, k_doy, KL109);
    KEAL_LOCAL int64_t k_doe = _t19;
    const int64_t _t20 = keal_mul(k_era, INT64_C(146097), KL110);
    const int64_t _t21 = keal_add(_t20, k_doe, KL111);
    const int64_t _t22 = keal_sub(_t21, INT64_C(719468), KL112);
    int64_t _t23 = _t22;
    return _t23;
}

K_DateTime* k_utcNow(void) {
    const double _t1 = keal_time();
    const int64_t _t2 = keal_f2i(_t1);
    keal_enter(KF113, KL114);
    K_DateTime* _t3 = k_utcAt(_t2);
    keal_leave();
    return _t3;
}

KealStr* k_monthName(int64_t k_month) {
    KealList* _t1 = keal_list_new(rel_keal_str);
    KealStr* _t2 = keal_str_retain(_str6);
    keal_list_push(_t1, (KealWord){ .p = (KealStr*)keal_str_retain(_t2) });
    KealStr* _t3 = keal_str_retain(_str10);
    keal_list_push(_t1, (KealWord){ .p = (KealStr*)keal_str_retain(_t3) });
    KealStr* _t4 = keal_str_retain(_str11);
    keal_list_push(_t1, (KealWord){ .p = (KealStr*)keal_str_retain(_t4) });
    KealStr* _t5 = keal_str_retain(_str12);
    keal_list_push(_t1, (KealWord){ .p = (KealStr*)keal_str_retain(_t5) });
    KealStr* _t6 = keal_str_retain(_str13);
    keal_list_push(_t1, (KealWord){ .p = (KealStr*)keal_str_retain(_t6) });
    KealStr* _t7 = keal_str_retain(_str14);
    keal_list_push(_t1, (KealWord){ .p = (KealStr*)keal_str_retain(_t7) });
    KealStr* _t8 = keal_str_retain(_str15);
    keal_list_push(_t1, (KealWord){ .p = (KealStr*)keal_str_retain(_t8) });
    KealStr* _t9 = keal_str_retain(_str16);
    keal_list_push(_t1, (KealWord){ .p = (KealStr*)keal_str_retain(_t9) });
    KealStr* _t10 = keal_str_retain(_str17);
    keal_list_push(_t1, (KealWord){ .p = (KealStr*)keal_str_retain(_t10) });
    KealStr* _t11 = keal_str_retain(_str18);
    keal_list_push(_t1, (KealWord){ .p = (KealStr*)keal_str_retain(_t11) });
    KealStr* _t12 = keal_str_retain(_str19);
    keal_list_push(_t1, (KealWord){ .p = (KealStr*)keal_str_retain(_t12) });
    KealStr* _t13 = keal_str_retain(_str20);
    keal_list_push(_t1, (KealWord){ .p = (KealStr*)keal_str_retain(_t13) });
    KealStr* _t14 = keal_str_retain(_str21);
    keal_list_push(_t1, (KealWord){ .p = (KealStr*)keal_str_retain(_t14) });
    KEAL_LOCAL KealList* k_names = keal_list_retain(_t1);
    bool _t15;
    if (k_month < INT64_C(1)) {
        _t15 = true;
    } else {
        _t15 = ((k_month < INT64_C(1)) || (k_month > INT64_C(12)));
    }
    const bool _t16 = _t15;
    if (_t16) {
        KealStr* _t17 = keal_str_retain(_str22);
        keal_list_release(k_names);
        keal_str_release(_t14);
        keal_str_release(_t13);
        keal_str_release(_t12);
        keal_str_release(_t11);
        keal_str_release(_t10);
        keal_str_release(_t9);
        keal_str_release(_t8);
        keal_str_release(_t7);
        keal_str_release(_t6);
        keal_str_release(_t5);
        keal_str_release(_t4);
        keal_str_release(_t3);
        keal_str_release(_t2);
        keal_list_release(_t1);
        return _t17;
    }
    KealList* _t18 = keal_list_retain(k_names);
    const KealWord _t19 = keal_list_get(_t18, k_month, KL115);
    KealStr* _t20 = keal_str_retain(((KealStr*)_t19.p));
    keal_list_release(_t18);
    keal_list_release(k_names);
    keal_str_release(_t14);
    keal_str_release(_t13);
    keal_str_release(_t12);
    keal_str_release(_t11);
    keal_str_release(_t10);
    keal_str_release(_t9);
    keal_str_release(_t8);
    keal_str_release(_t7);
    keal_str_release(_t6);
    keal_str_release(_t5);
    keal_str_release(_t4);
    keal_str_release(_t3);
    keal_str_release(_t2);
    keal_list_release(_t1);
    return _t20;
    keal_list_release(_t18);
    keal_list_release(k_names);
    keal_str_release(_t14);
    keal_str_release(_t13);
    keal_str_release(_t12);
    keal_str_release(_t11);
    keal_str_release(_t10);
    keal_str_release(_t9);
    keal_str_release(_t8);
    keal_str_release(_t7);
    keal_str_release(_t6);
    keal_str_release(_t5);
    keal_str_release(_t4);
    keal_str_release(_t3);
    keal_str_release(_t2);
    keal_list_release(_t1);
}

KealStr* k_weekdayName(int64_t k_weekday) {
    KealList* _t1 = keal_list_new(rel_keal_str);
    KealStr* _t2 = keal_str_retain(_str23);
    keal_list_push(_t1, (KealWord){ .p = (KealStr*)keal_str_retain(_t2) });
    KealStr* _t3 = keal_str_retain(_str24);
    keal_list_push(_t1, (KealWord){ .p = (KealStr*)keal_str_retain(_t3) });
    KealStr* _t4 = keal_str_retain(_str25);
    keal_list_push(_t1, (KealWord){ .p = (KealStr*)keal_str_retain(_t4) });
    KealStr* _t5 = keal_str_retain(_str26);
    keal_list_push(_t1, (KealWord){ .p = (KealStr*)keal_str_retain(_t5) });
    KealStr* _t6 = keal_str_retain(_str27);
    keal_list_push(_t1, (KealWord){ .p = (KealStr*)keal_str_retain(_t6) });
    KealStr* _t7 = keal_str_retain(_str28);
    keal_list_push(_t1, (KealWord){ .p = (KealStr*)keal_str_retain(_t7) });
    KealStr* _t8 = keal_str_retain(_str29);
    keal_list_push(_t1, (KealWord){ .p = (KealStr*)keal_str_retain(_t8) });
    KEAL_LOCAL KealList* k_names = keal_list_retain(_t1);
    bool _t9;
    if (k_weekday < INT64_C(0)) {
        _t9 = true;
    } else {
        _t9 = ((k_weekday < INT64_C(0)) || (k_weekday > INT64_C(6)));
    }
    const bool _t10 = _t9;
    if (_t10) {
        KealStr* _t11 = keal_str_retain(_str22);
        keal_list_release(k_names);
        keal_str_release(_t8);
        keal_str_release(_t7);
        keal_str_release(_t6);
        keal_str_release(_t5);
        keal_str_release(_t4);
        keal_str_release(_t3);
        keal_str_release(_t2);
        keal_list_release(_t1);
        return _t11;
    }
    KealList* _t12 = keal_list_retain(k_names);
    const KealWord _t13 = keal_list_get(_t12, k_weekday, KL116);
    KealStr* _t14 = keal_str_retain(((KealStr*)_t13.p));
    keal_list_release(_t12);
    keal_list_release(k_names);
    keal_str_release(_t8);
    keal_str_release(_t7);
    keal_str_release(_t6);
    keal_str_release(_t5);
    keal_str_release(_t4);
    keal_str_release(_t3);
    keal_str_release(_t2);
    keal_list_release(_t1);
    return _t14;
    keal_list_release(_t12);
    keal_list_release(k_names);
    keal_str_release(_t8);
    keal_str_release(_t7);
    keal_str_release(_t6);
    keal_str_release(_t5);
    keal_str_release(_t4);
    keal_str_release(_t3);
    keal_str_release(_t2);
    keal_list_release(_t1);
}

bool k_isLeapYear(int64_t k_year) {
    const int64_t _t1 = keal_rem(k_year, INT64_C(4), KL117);
    bool _t2;
    if (!(_t1 == INT64_C(0))) {
        _t2 = false;
    } else {
        const int64_t _t3 = keal_rem(k_year, INT64_C(100), KL118);
        bool _t4;
        if (_t3 != INT64_C(0)) {
            _t4 = true;
        } else {
            const int64_t _t5 = keal_rem(k_year, INT64_C(400), KL119);
            _t4 = ((_t3 != INT64_C(0)) || (_t5 == INT64_C(0)));
        }
        _t2 = ((_t1 == INT64_C(0)) && _t4);
    }
    bool _t6 = _t2;
    return _t6;
}

bool k_exists(KealStr* k_path) {
    KealStr* _t1 = keal_str_retain(k_path);
    const int64_t _t2 = keal_path_kind(_t1);
    bool _t3 = (_t2 != INT64_C(0));
    keal_str_release(_t1);
    return _t3;
    keal_str_release(_t1);
}

bool k_isFile(KealStr* k_path) {
    KealStr* _t1 = keal_str_retain(k_path);
    const int64_t _t2 = keal_path_kind(_t1);
    bool _t3 = (_t2 == INT64_C(1));
    keal_str_release(_t1);
    return _t3;
    keal_str_release(_t1);
}

bool k_isDir(KealStr* k_path) {
    KealStr* _t1 = keal_str_retain(k_path);
    const int64_t _t2 = keal_path_kind(_t1);
    bool _t3 = (_t2 == INT64_C(2));
    keal_str_release(_t1);
    return _t3;
    keal_str_release(_t1);
}

KealList* k_walkDir(KealStr* k_path) {
    KealList* _t1 = keal_list_new(rel_keal_str);
    KEAL_LOCAL KealList* k_out = keal_list_retain(_t1);
    KealStr* _t2 = keal_str_retain(k_path);
    KealList* _t3 = keal_list_dir(_t2);
    KEAL_LOCAL KealList* k_here = keal_list_retain(_t3);
    KealList* _t4 = keal_list_retain(k_here);
    const bool _t5 = (_t4 == NULL);
    if (_t5) {
        KealList* _t6 = keal_list_retain(k_out);
        keal_list_release(_t4);
        keal_list_release(k_here);
        keal_list_release(_t3);
        keal_str_release(_t2);
        keal_list_release(k_out);
        keal_list_release(_t1);
        return _t6;
    }
    KealStr* _t7 = keal_str_retain(k_path);
    KEAL_LOCAL KealStr* k_base = keal_str_retain(_t7);
    KealStr* _t8 = keal_str_retain(k_base);
    KealStr* _t9 = keal_str_retain(_str30);
    const bool _t10 = keal_str_ends_with(_t8, _t9);
    const bool _t11 = _t10;
    if (_t11) {
        KealStr* _t12 = keal_str_retain(k_base);
        KealStr* _t13 = keal_str_retain(k_base);
        const int64_t _t14 = keal_sub(keal_str_length(_t13), INT64_C(1), KL120);
        KealStr* _t15 = keal_str_substring(_t12, INT64_C(0), _t14, KL121);
        keal_str_release(k_base);
        k_base = keal_str_retain(_t15);
        keal_str_release(_t15);
        keal_str_release(_t13);
        keal_str_release(_t12);
    }
    KealList* _t16 = keal_list_retain(k_here);
    KealList* _t17 = keal_list_snapshot(_t16);
    for (int64_t _t18 = 0; _t18 < _t17->len; _t18++) {
        KealStr* k_name = ((KealStr*)_t17->data[_t18].p);
        KealStr* _t19 = keal_str_retain(k_base);
        KealStr* _t20 = keal_concat(_t19, keal_str_retain(_str30));
        KealStr* _t21 = keal_str_retain(k_name);
        KealStr* _t22 = keal_concat(_t20, _t21);
        KEAL_LOCAL KealStr* k_child = keal_str_retain(_t22);
        KealStr* _t23 = keal_str_retain(k_child);
        keal_enter(KF122, KL123);
        const bool _t24 = k_isDir(_t23);
        keal_leave();
        const bool _t25 = _t24;
        if (_t25) {
            KealList* _t26 = keal_list_retain(k_out);
            KealStr* _t27 = keal_str_retain(k_child);
            keal_enter(KF124, KL125);
            KealList* _t28 = k_walkDir(_t27);
            keal_leave();
            keal_list_add_all(_t26, _t28);
            keal_list_release(_t28);
            keal_str_release(_t27);
            keal_list_release(_t26);
        } else {
            KealList* _t29 = keal_list_retain(k_out);
            KealStr* _t30 = keal_str_retain(k_child);
            keal_list_push(_t29, (KealWord){ .p = (KealStr*)keal_str_retain(_t30) });
            keal_str_release(_t30);
            keal_list_release(_t29);
        }
        keal_str_release(_t23);
        keal_str_release(k_child);
        keal_str_release(_t22);
        keal_str_release(_t21);
        keal_str_release(_t20);
        keal_str_release(_t19);
    }
    keal_list_release(_t17);
    keal_list_release(_t16);
    KealList* _t31 = keal_list_retain(k_out);
    keal_str_release(_t9);
    keal_str_release(_t8);
    keal_str_release(k_base);
    keal_str_release(_t7);
    keal_list_release(_t4);
    keal_list_release(k_here);
    keal_list_release(_t3);
    keal_str_release(_t2);
    keal_list_release(k_out);
    keal_list_release(_t1);
    return _t31;
    keal_str_release(_t9);
    keal_str_release(_t8);
    keal_str_release(k_base);
    keal_str_release(_t7);
    keal_list_release(_t4);
    keal_list_release(k_here);
    keal_list_release(_t3);
    keal_str_release(_t2);
    keal_list_release(k_out);
    keal_list_release(_t1);
}

int keal_program_run(void) {
    KealList* _t32 = keal_list_new(rel_keal_str);
    KealList* _t33 = keal_run_command(_t32, NULL);
    KealStr* _t34 = keal_str_retain(_str31);
    if (!((_t33 == NULL))) { keal_panic(_t34->bytes, NULL, KL126); }
    KealList* _t35 = keal_list_new(rel_keal_str);
    KealStr* _t36 = keal_str_retain(_str32);
    keal_list_push(_t35, (KealWord){ .p = (KealStr*)keal_str_retain(_t36) });
    KealList* _t37 = keal_run_command(_t35, NULL);
    KealStr* _t38 = keal_str_retain(_str33);
    if (!((_t37 == NULL))) { keal_panic(_t38->bytes, NULL, KL127); }
    KealStr* _t39 = keal_str_retain(_str34);
    keal_enter(KF128, KL129);
    const bool _t40 = k_isFile(_t39);
    keal_leave();
    const bool _t41 = _t40;
    if (_t41) {
        KealList* _t42 = keal_list_new(rel_keal_str);
        KealStr* _t43 = keal_str_retain(_str34);
        keal_list_push(_t42, (KealWord){ .p = (KealStr*)keal_str_retain(_t43) });
        KealStr* _t44 = keal_str_retain(_str35);
        keal_list_push(_t42, (KealWord){ .p = (KealStr*)keal_str_retain(_t44) });
        KealStr* _t45 = keal_str_retain(_str36);
        keal_list_push(_t42, (KealWord){ .p = (KealStr*)keal_str_retain(_t45) });
        KealList* _t46 = keal_run_command(_t42, NULL);
        KEAL_LOCAL KealList* k_ok = keal_list_retain(_t46);
        KealList* _t47 = keal_list_retain(k_ok);
        KealStr* _t48 = keal_str_retain(_str37);
        if (!((_t47 != NULL))) { keal_panic(_t48->bytes, NULL, KL130); }
        KealList* _t49 = keal_list_retain(k_ok);
        KealList* _t50;
        if (_t49 != NULL) {
            _t50 = keal_list_retain(_t49);
        } else {
            KealList* _t51 = keal_list_new(rel_keal_str);
            _t50 = keal_list_retain(_t51);
            keal_list_release(_t51);
        }
        KEAL_LOCAL KealList* k_r = keal_list_retain(_t50);
        KealList* _t52 = keal_list_retain(k_r);
        KealStr* _t53 = keal_str_retain(_str38);
        if (!((_t52->len == INT64_C(3)))) { keal_panic(_t53->bytes, NULL, KL131); }
        KealList* _t54 = keal_list_retain(k_r);
        const KealWord _t55 = keal_list_get(_t54, INT64_C(0), KL132);
        KealStr* _t56 = keal_str_retain(((KealStr*)_t55.p));
        KealStr* _t57 = keal_str_retain(_str39);
        const bool _t58 = keal_str_cmp(_t56, _t57) == 0;
        KealList* _t59 = keal_list_retain(k_r);
        const KealWord _t60 = keal_list_get(_t59, INT64_C(0), KL133);
        KealStr* _t61 = keal_str_retain(((KealStr*)_t60.p));
        KealStr* _t62 = keal_concat(keal_str_retain(_str40), _t61);
        if (!(_t58)) { keal_panic(_t62->bytes, NULL, KL134); }
        KealList* _t63 = keal_list_retain(k_r);
        const KealWord _t64 = keal_list_get(_t63, INT64_C(1), KL135);
        KealStr* _t65 = keal_str_retain(((KealStr*)_t64.p));
        keal_enter(KF136, KL137);
        KealList* _t66 = k_lines(_t65);
        keal_leave();
        KealList* _t67 = keal_list_new(rel_keal_str);
        KealStr* _t68 = keal_str_retain(_str41);
        keal_list_push(_t67, (KealWord){ .p = (KealStr*)keal_str_retain(_t68) });
        const bool _t69 = keal_list_eq(_t66, _t67, keal_key_eq_str);
        KealList* _t70 = keal_list_retain(k_r);
        const KealWord _t71 = keal_list_get(_t70, INT64_C(1), KL138);
        KealStr* _t72 = keal_str_retain(((KealStr*)_t71.p));
        KealStr* _t73 = keal_concat(keal_str_retain(_str42), _t72);
        if (!(_t69)) { keal_panic(_t73->bytes, NULL, KL139); }
        KealList* _t74 = keal_list_retain(k_r);
        const KealWord _t75 = keal_list_get(_t74, INT64_C(2), KL140);
        KealStr* _t76 = keal_str_retain(((KealStr*)_t75.p));
        keal_enter(KF141, KL142);
        KealList* _t77 = k_lines(_t76);
        keal_leave();
        KealList* _t78 = keal_list_new(rel_keal_str);
        KealStr* _t79 = keal_str_retain(_str43);
        keal_list_push(_t78, (KealWord){ .p = (KealStr*)keal_str_retain(_t79) });
        const bool _t80 = keal_list_eq(_t77, _t78, keal_key_eq_str);
        KealList* _t81 = keal_list_retain(k_r);
        const KealWord _t82 = keal_list_get(_t81, INT64_C(2), KL143);
        KealStr* _t83 = keal_str_retain(((KealStr*)_t82.p));
        KealStr* _t84 = keal_concat(keal_str_retain(_str44), _t83);
        if (!(_t80)) { keal_panic(_t84->bytes, NULL, KL144); }
        KealList* _t85 = keal_list_new(rel_keal_str);
        KealStr* _t86 = keal_str_retain(_str34);
        keal_list_push(_t85, (KealWord){ .p = (KealStr*)keal_str_retain(_t86) });
        KealStr* _t87 = keal_str_retain(_str35);
        keal_list_push(_t85, (KealWord){ .p = (KealStr*)keal_str_retain(_t87) });
        KealStr* _t88 = keal_str_retain(_str45);
        keal_list_push(_t85, (KealWord){ .p = (KealStr*)keal_str_retain(_t88) });
        KealList* _t89 = keal_run_command(_t85, NULL);
        KealList* _t90;
        if (_t89 != NULL) {
            _t90 = keal_list_retain(_t89);
        } else {
            KealList* _t91 = keal_list_new(rel_keal_str);
            _t90 = keal_list_retain(_t91);
            keal_list_release(_t91);
        }
        KEAL_LOCAL KealList* k_failed = keal_list_retain(_t90);
        KealList* _t92 = keal_list_retain(k_failed);
        bool _t93;
        if (!(_t92->len == INT64_C(3))) {
            _t93 = false;
        } else {
            KealList* _t94 = keal_list_retain(k_failed);
            const KealWord _t95 = keal_list_get(_t94, INT64_C(0), KL145);
            KealStr* _t96 = keal_str_retain(((KealStr*)_t95.p));
            KealStr* _t97 = keal_str_retain(_str46);
            const bool _t98 = keal_str_cmp(_t96, _t97) == 0;
            _t93 = ((_t92->len == INT64_C(3)) && _t98);
            keal_str_release(_t97);
            keal_str_release(_t96);
            keal_list_release(_t94);
        }
        KealStr* _t99 = keal_str_retain(_str47);
        if (!(_t93)) { keal_panic(_t99->bytes, NULL, KL146); }
        KealList* _t100 = keal_list_new(rel_keal_str);
        KealStr* _t101 = keal_str_retain(_str34);
        keal_list_push(_t100, (KealWord){ .p = (KealStr*)keal_str_retain(_t101) });
        KealStr* _t102 = keal_str_retain(_str35);
        keal_list_push(_t100, (KealWord){ .p = (KealStr*)keal_str_retain(_t102) });
        KealStr* _t103 = keal_str_retain(_str48);
        keal_list_push(_t100, (KealWord){ .p = (KealStr*)keal_str_retain(_t103) });
        KealStr* _t104 = keal_str_retain(_str49);
        keal_list_push(_t100, (KealWord){ .p = (KealStr*)keal_str_retain(_t104) });
        KealStr* _t105 = keal_str_retain(_str50);
        keal_list_push(_t100, (KealWord){ .p = (KealStr*)keal_str_retain(_t105) });
        KealList* _t106 = keal_run_command(_t100, NULL);
        KealList* _t107;
        if (_t106 != NULL) {
            _t107 = keal_list_retain(_t106);
        } else {
            KealList* _t108 = keal_list_new(rel_keal_str);
            _t107 = keal_list_retain(_t108);
            keal_list_release(_t108);
        }
        KEAL_LOCAL KealList* k_spaced = keal_list_retain(_t107);
        KealList* _t109 = keal_list_retain(k_spaced);
        const KealWord _t110 = keal_list_get(_t109, INT64_C(1), KL147);
        KealStr* _t111 = keal_str_retain(((KealStr*)_t110.p));
        KealStr* _t112 = keal_str_retain(_str50);
        const bool _t113 = keal_str_cmp(_t111, _t112) == 0;
        KealList* _t114 = keal_list_retain(k_spaced);
        const KealWord _t115 = keal_list_get(_t114, INT64_C(1), KL148);
        KealStr* _t116 = keal_str_retain(((KealStr*)_t115.p));
        KealStr* _t117 = keal_concat(keal_str_retain(_str51), _t116);
        if (!(_t113)) { keal_panic(_t117->bytes, NULL, KL149); }
        keal_str_release(_t117);
        keal_str_release(_t116);
        keal_list_release(_t114);
        keal_str_release(_t112);
        keal_str_release(_t111);
        keal_list_release(_t109);
        keal_list_release(k_spaced);
        keal_list_release(_t107);
        keal_list_release(_t106);
        keal_str_release(_t105);
        keal_str_release(_t104);
        keal_str_release(_t103);
        keal_str_release(_t102);
        keal_str_release(_t101);
        keal_list_release(_t100);
        keal_str_release(_t99);
        keal_list_release(_t92);
        keal_list_release(k_failed);
        keal_list_release(_t90);
        keal_list_release(_t89);
        keal_str_release(_t88);
        keal_str_release(_t87);
        keal_str_release(_t86);
        keal_list_release(_t85);
        keal_str_release(_t84);
        keal_str_release(_t83);
        keal_list_release(_t81);
        keal_str_release(_t79);
        keal_list_release(_t78);
        keal_list_release(_t77);
        keal_str_release(_t76);
        keal_list_release(_t74);
        keal_str_release(_t73);
        keal_str_release(_t72);
        keal_list_release(_t70);
        keal_str_release(_t68);
        keal_list_release(_t67);
        keal_list_release(_t66);
        keal_str_release(_t65);
        keal_list_release(_t63);
        keal_str_release(_t62);
        keal_str_release(_t61);
        keal_list_release(_t59);
        keal_str_release(_t57);
        keal_str_release(_t56);
        keal_list_release(_t54);
        keal_str_release(_t53);
        keal_list_release(_t52);
        keal_list_release(k_r);
        keal_list_release(_t50);
        keal_list_release(_t49);
        keal_str_release(_t48);
        keal_list_release(_t47);
        keal_list_release(k_ok);
        keal_list_release(_t46);
        keal_str_release(_t45);
        keal_str_release(_t44);
        keal_str_release(_t43);
        keal_list_release(_t42);
    }
    KealStr* _t118;
    KealStr* _t119 = keal_str_retain(_str34);
    keal_enter(KF150, KL151);
    const bool _t120 = k_isFile(_t119);
    keal_leave();
    const bool _t121 = _t120;
    if (_t121) {
        KealStr* _t122 = keal_str_retain(_str49);
        _t118 = keal_str_retain(_t122);
        keal_str_release(_t122);
    } else {
        KealStr* _t123 = keal_str_retain(_str52);
        _t118 = keal_str_retain(_t123);
        keal_str_release(_t123);
    }
    k_shell = keal_str_retain(_t118);
    KealStr* _t124;
    KealStr* _t125 = keal_str_retain(_str34);
    keal_enter(KF152, KL153);
    const bool _t126 = k_isFile(_t125);
    keal_leave();
    const bool _t127 = _t126;
    if (_t127) {
        KealStr* _t128 = keal_str_retain(_str35);
        _t124 = keal_str_retain(_t128);
        keal_str_release(_t128);
    } else {
        KealStr* _t129 = keal_str_retain(_str53);
        _t124 = keal_str_retain(_t129);
        keal_str_release(_t129);
    }
    k_flag = keal_str_retain(_t124);
    KealStr* _t130 = keal_str_retain(_str34);
    keal_enter(KF154, KL155);
    const bool _t131 = k_isFile(_t130);
    keal_leave();
    bool _t132;
    if (_t131) {
        _t132 = true;
    } else {
        KealStr* _t133 = keal_str_retain(_str54);
        keal_enter(KF156, KL157);
        const bool _t134 = k_isFile(_t133);
        keal_leave();
        _t132 = (_t131 || _t134);
        keal_str_release(_t133);
    }
    const bool _t135 = _t132;
    if (_t135) {
        KealList* _t136 = keal_list_new(rel_keal_str);
        KealStr* _t137 = keal_str_retain(k_shell);
        keal_list_push(_t136, (KealWord){ .p = (KealStr*)keal_str_retain(_t137) });
        KealStr* _t138 = keal_str_retain(k_flag);
        keal_list_push(_t136, (KealWord){ .p = (KealStr*)keal_str_retain(_t138) });
        KealStr* _t139 = keal_str_retain(_str55);
        keal_list_push(_t136, (KealWord){ .p = (KealStr*)keal_str_retain(_t139) });
        KealList* _t140 = keal_run_command(_t136, NULL);
        KealList* _t141;
        if (_t140 != NULL) {
            _t141 = keal_list_retain(_t140);
        } else {
            KealList* _t142 = keal_list_new(rel_keal_str);
            _t141 = keal_list_retain(_t142);
            keal_list_release(_t142);
        }
        KEAL_LOCAL KealList* k_named = keal_list_retain(_t141);
        KealList* _t143 = keal_list_retain(k_named);
        KealStr* _t144 = keal_str_retain(k_shell);
        KealStr* _t145 = keal_concat(keal_str_retain(_str56), _t144);
        KealStr* _t146 = keal_concat(_t145, keal_str_retain(_str57));
        if (!((_t143->len == INT64_C(3)))) { keal_panic(_t146->bytes, NULL, KL158); }
        KealList* _t147 = keal_list_retain(k_named);
        const KealWord _t148 = keal_list_get(_t147, INT64_C(0), KL159);
        KealStr* _t149 = keal_str_retain(((KealStr*)_t148.p));
        KealStr* _t150 = keal_str_retain(_str58);
        const bool _t151 = keal_str_cmp(_t149, _t150) == 0;
        KealList* _t152 = keal_list_retain(k_named);
        const KealWord _t153 = keal_list_get(_t152, INT64_C(0), KL160);
        KealStr* _t154 = keal_str_retain(((KealStr*)_t153.p));
        KealStr* _t155 = keal_concat(keal_str_retain(_str59), _t154);
        if (!(_t151)) { keal_panic(_t155->bytes, NULL, KL161); }
        keal_str_release(_t155);
        keal_str_release(_t154);
        keal_list_release(_t152);
        keal_str_release(_t150);
        keal_str_release(_t149);
        keal_list_release(_t147);
        keal_str_release(_t146);
        keal_str_release(_t145);
        keal_str_release(_t144);
        keal_list_release(_t143);
        keal_list_release(k_named);
        keal_list_release(_t141);
        keal_list_release(_t140);
        keal_str_release(_t139);
        keal_str_release(_t138);
        keal_str_release(_t137);
        keal_list_release(_t136);
    }
    KealList* _t156 = keal_list_new(rel_keal_str);
    KealStr* _t157 = keal_str_retain(_str60);
    KealStr* _t158 = keal_str_retain(k_shell);
    KealStr* _t159 = keal_concat(_t157, _t158);
    keal_list_push(_t156, (KealWord){ .p = (KealStr*)keal_str_retain(_t159) });
    KealStr* _t160 = keal_str_retain(k_flag);
    keal_list_push(_t156, (KealWord){ .p = (KealStr*)keal_str_retain(_t160) });
    KealStr* _t161 = keal_str_retain(_str61);
    keal_list_push(_t156, (KealWord){ .p = (KealStr*)keal_str_retain(_t161) });
    KealList* _t162 = keal_run_command(_t156, NULL);
    KealStr* _t163 = keal_str_retain(_str62);
    if (!((_t162 == NULL))) { keal_panic(_t163->bytes, NULL, KL162); }
    KealStr* _t164 = keal_str_retain(_str34);
    keal_enter(KF163, KL164);
    const bool _t165 = k_isFile(_t164);
    keal_leave();
    const bool _t166 = _t165;
    if (_t166) {
        KealList* _t167 = keal_list_new(rel_keal_str);
        KealStr* _t168 = keal_str_retain(_str63);
        keal_list_push(_t167, (KealWord){ .p = (KealStr*)keal_str_retain(_t168) });
        KealStr* _t169 = keal_str_retain(_str35);
        keal_list_push(_t167, (KealWord){ .p = (KealStr*)keal_str_retain(_t169) });
        KealStr* _t170 = keal_str_retain(_str64);
        keal_list_push(_t167, (KealWord){ .p = (KealStr*)keal_str_retain(_t170) });
        KealList* _t171 = keal_run_command(_t167, NULL);
        KealList* _t172;
        if (_t171 != NULL) {
            _t172 = keal_list_retain(_t171);
        } else {
            KealList* _t173 = keal_list_new(rel_keal_str);
            _t172 = keal_list_retain(_t173);
            keal_list_release(_t173);
        }
        KEAL_LOCAL KealList* k_viaDir = keal_list_retain(_t172);
        KealList* _t174 = keal_list_retain(k_viaDir);
        bool _t175;
        if (!(_t174->len == INT64_C(3))) {
            _t175 = false;
        } else {
            KealList* _t176 = keal_list_retain(k_viaDir);
            const KealWord _t177 = keal_list_get(_t176, INT64_C(0), KL165);
            KealStr* _t178 = keal_str_retain(((KealStr*)_t177.p));
            KealStr* _t179 = keal_str_retain(_str65);
            const bool _t180 = keal_str_cmp(_t178, _t179) == 0;
            _t175 = ((_t174->len == INT64_C(3)) && _t180);
            keal_str_release(_t179);
            keal_str_release(_t178);
            keal_list_release(_t176);
        }
        KealStr* _t181 = keal_str_retain(_str66);
        if (!(_t175)) { keal_panic(_t181->bytes, NULL, KL166); }
        keal_str_release(_t181);
        keal_list_release(_t174);
        keal_list_release(k_viaDir);
        keal_list_release(_t172);
        keal_list_release(_t171);
        keal_str_release(_t170);
        keal_str_release(_t169);
        keal_str_release(_t168);
        keal_list_release(_t167);
    }
    KealStr* _t182;
    KealStr* _t183 = keal_str_retain(_str67);
    keal_enter(KF167, KL168);
    const bool _t184 = k_isFile(_t183);
    keal_leave();
    const bool _t185 = _t184;
    if (_t185) {
        KealStr* _t186 = keal_str_retain(_str68);
        _t182 = keal_str_retain(_t186);
        keal_str_release(_t186);
    } else {
        KealStr* _t187 = keal_str_retain(_str69);
        _t182 = keal_str_retain(_t187);
        keal_str_release(_t187);
    }
    k_built = keal_str_retain(_t182);
    KealList* _t188 = keal_list_new(rel_keal_str);
    KealStr* _t189 = keal_str_retain(k_built);
    keal_list_push(_t188, (KealWord){ .p = (KealStr*)keal_str_retain(_t189) });
    KealStr* _t190 = keal_str_retain(_str70);
    keal_list_push(_t188, (KealWord){ .p = (KealStr*)keal_str_retain(_t190) });
    KealList* _t191 = keal_run_command(_t188, NULL);
    KealList* _t192;
    if (_t191 != NULL) {
        _t192 = keal_list_retain(_t191);
    } else {
        KealList* _t193 = keal_list_new(rel_keal_str);
        _t192 = keal_list_retain(_t193);
        keal_list_release(_t193);
    }
    k_own = keal_list_retain(_t192);
    KealList* _t194 = keal_list_retain(k_own);
    bool _t195;
    if (!(_t194->len == INT64_C(3))) {
        _t195 = false;
    } else {
        KealList* _t196 = keal_list_retain(k_own);
        const KealWord _t197 = keal_list_get(_t196, INT64_C(0), KL169);
        KealStr* _t198 = keal_str_retain(((KealStr*)_t197.p));
        KealStr* _t199 = keal_str_retain(_str5);
        const bool _t200 = keal_str_cmp(_t198, _t199) == 0;
        _t195 = ((_t194->len == INT64_C(3)) && _t200);
        keal_str_release(_t199);
        keal_str_release(_t198);
        keal_list_release(_t196);
    }
    KealStr* _t201 = keal_str_retain(k_built);
    KealStr* _t202 = keal_concat(keal_str_retain(_str71), _t201);
    if (!(_t195)) { keal_panic(_t202->bytes, NULL, KL170); }
    KealStr* _t203 = keal_str_retain(_str34);
    keal_enter(KF171, KL172);
    const bool _t204 = k_isFile(_t203);
    keal_leave();
    const bool _t205 = _t204;
    if (_t205) {
        KealList* _t206 = keal_list_new(rel_keal_str);
        KealStr* _t207 = keal_str_retain(_str34);
        keal_list_push(_t206, (KealWord){ .p = (KealStr*)keal_str_retain(_t207) });
        KealStr* _t208 = keal_str_retain(_str35);
        keal_list_push(_t206, (KealWord){ .p = (KealStr*)keal_str_retain(_t208) });
        KealStr* _t209 = keal_str_retain(_str72);
        keal_list_push(_t206, (KealWord){ .p = (KealStr*)keal_str_retain(_t209) });
        KealStr* _t210 = keal_str_retain(_str73);
        KealList* _t211 = keal_run_command(_t206, _t210);
        KealList* _t212;
        if (_t211 != NULL) {
            _t212 = keal_list_retain(_t211);
        } else {
            KealList* _t213 = keal_list_new(rel_keal_str);
            _t212 = keal_list_retain(_t213);
            keal_list_release(_t213);
        }
        KEAL_LOCAL KealList* k_fed = keal_list_retain(_t212);
        KealList* _t214 = keal_list_retain(k_fed);
        const KealWord _t215 = keal_list_get(_t214, INT64_C(1), KL173);
        KealStr* _t216 = keal_str_retain(((KealStr*)_t215.p));
        KealStr* _t217 = keal_str_retain(_str73);
        const bool _t218 = keal_str_cmp(_t216, _t217) == 0;
        KealList* _t219 = keal_list_retain(k_fed);
        const KealWord _t220 = keal_list_get(_t219, INT64_C(1), KL174);
        KealStr* _t221 = keal_str_retain(((KealStr*)_t220.p));
        KealStr* _t222 = keal_concat(keal_str_retain(_str74), _t221);
        if (!(_t218)) { keal_panic(_t222->bytes, NULL, KL175); }
        KealList* _t223 = keal_list_new(rel_keal_str);
        KealStr* _t224 = keal_str_retain(_str34);
        keal_list_push(_t223, (KealWord){ .p = (KealStr*)keal_str_retain(_t224) });
        KealStr* _t225 = keal_str_retain(_str35);
        keal_list_push(_t223, (KealWord){ .p = (KealStr*)keal_str_retain(_t225) });
        KealStr* _t226 = keal_str_retain(_str75);
        keal_list_push(_t223, (KealWord){ .p = (KealStr*)keal_str_retain(_t226) });
        KealStr* _t227 = keal_str_retain(_str76);
        KealList* _t228 = keal_run_command(_t223, _t227);
        KealList* _t229;
        if (_t228 != NULL) {
            _t229 = keal_list_retain(_t228);
        } else {
            KealList* _t230 = keal_list_new(rel_keal_str);
            _t229 = keal_list_retain(_t230);
            keal_list_release(_t230);
        }
        KEAL_LOCAL KealList* k_counted = keal_list_retain(_t229);
        KealList* _t231 = keal_list_retain(k_counted);
        const KealWord _t232 = keal_list_get(_t231, INT64_C(1), KL176);
        KealStr* _t233 = keal_str_retain(((KealStr*)_t232.p));
        KealStr* _t234 = keal_str_trim(_t233);
        KealStr* _t235 = keal_str_retain(_str65);
        const bool _t236 = keal_str_cmp(_t234, _t235) == 0;
        KealList* _t237 = keal_list_retain(k_counted);
        const KealWord _t238 = keal_list_get(_t237, INT64_C(1), KL177);
        KealStr* _t239 = keal_str_retain(((KealStr*)_t238.p));
        KealStr* _t240 = keal_str_trim(_t239);
        KealStr* _t241 = keal_concat(keal_str_retain(_str77), _t240);
        if (!(_t236)) { keal_panic(_t241->bytes, NULL, KL178); }
        KealList* _t242 = keal_list_new(rel_keal_str);
        KealStr* _t243 = keal_str_retain(_str34);
        keal_list_push(_t242, (KealWord){ .p = (KealStr*)keal_str_retain(_t243) });
        KealStr* _t244 = keal_str_retain(_str35);
        keal_list_push(_t242, (KealWord){ .p = (KealStr*)keal_str_retain(_t244) });
        KealStr* _t245 = keal_str_retain(_str78);
        keal_list_push(_t242, (KealWord){ .p = (KealStr*)keal_str_retain(_t245) });
        KealStr* _t246 = keal_str_retain(_str6);
        KealList* _t247 = keal_run_command(_t242, _t246);
        KealList* _t248;
        if (_t247 != NULL) {
            _t248 = keal_list_retain(_t247);
        } else {
            KealList* _t249 = keal_list_new(rel_keal_str);
            _t248 = keal_list_retain(_t249);
            keal_list_release(_t249);
        }
        KEAL_LOCAL KealList* k_none = keal_list_retain(_t248);
        KealList* _t250 = keal_list_retain(k_none);
        const KealWord _t251 = keal_list_get(_t250, INT64_C(1), KL179);
        KealStr* _t252 = keal_str_retain(((KealStr*)_t251.p));
        KealStr* _t253 = keal_str_trim(_t252);
        KealStr* _t254 = keal_str_retain(_str79);
        const bool _t255 = keal_str_cmp(_t253, _t254) == 0;
        KealStr* _t256 = keal_str_retain(_str80);
        if (!(_t255)) { keal_panic(_t256->bytes, NULL, KL180); }
        KealStr* _t257 = keal_str_retain(_str81);
        KEAL_LOCAL KealStr* k_big = keal_str_retain(_t257);
        KEAL_LOCAL int64_t k_k = INT64_C(0);
        while (1) {
            const bool _t258 = (k_k < INT64_C(15));
            if (!_t258) {
                break;
            }
            KealStr* _t259 = keal_str_retain(k_big);
            KealStr* _t260 = keal_str_retain(k_big);
            KealStr* _t261 = keal_concat(_t259, _t260);
            keal_str_release(k_big);
            k_big = keal_str_retain(_t261);
            const int64_t _t262 = keal_add(k_k, INT64_C(1), KL181);
            k_k = _t262;
            keal_str_release(_t261);
            keal_str_release(_t260);
            keal_str_release(_t259);
        }
        KealList* _t263 = keal_list_new(rel_keal_str);
        KealStr* _t264 = keal_str_retain(_str34);
        keal_list_push(_t263, (KealWord){ .p = (KealStr*)keal_str_retain(_t264) });
        KealStr* _t265 = keal_str_retain(_str35);
        keal_list_push(_t263, (KealWord){ .p = (KealStr*)keal_str_retain(_t265) });
        KealStr* _t266 = keal_str_retain(_str72);
        keal_list_push(_t263, (KealWord){ .p = (KealStr*)keal_str_retain(_t266) });
        KealStr* _t267 = keal_str_retain(k_big);
        KealList* _t268 = keal_run_command(_t263, _t267);
        KealList* _t269;
        if (_t268 != NULL) {
            _t269 = keal_list_retain(_t268);
        } else {
            KealList* _t270 = keal_list_new(rel_keal_str);
            _t269 = keal_list_retain(_t270);
            keal_list_release(_t270);
        }
        KEAL_LOCAL KealList* k_echoed = keal_list_retain(_t269);
        KealList* _t271 = keal_list_retain(k_echoed);
        const KealWord _t272 = keal_list_get(_t271, INT64_C(1), KL182);
        KealStr* _t273 = keal_str_retain(((KealStr*)_t272.p));
        KealStr* _t274 = keal_str_retain(k_big);
        KealList* _t275 = keal_list_retain(k_echoed);
        const KealWord _t276 = keal_list_get(_t275, INT64_C(1), KL183);
        KealStr* _t277 = keal_str_retain(((KealStr*)_t276.p));
        KealStr* _t278 = keal_str_from_int(keal_str_length(_t277));
        KealStr* _t279 = keal_concat(keal_str_retain(_str82), _t278);
        if (!((keal_str_length(_t273) == keal_str_length(_t274)))) { keal_panic(_t279->bytes, NULL, KL184); }
        KealList* _t280 = keal_list_retain(k_echoed);
        const KealWord _t281 = keal_list_get(_t280, INT64_C(0), KL185);
        KealStr* _t282 = keal_str_retain(((KealStr*)_t281.p));
        KealStr* _t283 = keal_str_retain(_str5);
        const bool _t284 = keal_str_cmp(_t282, _t283) == 0;
        KealStr* _t285 = keal_str_retain(_str83);
        if (!(_t284)) { keal_panic(_t285->bytes, NULL, KL186); }
        KealList* _t286 = keal_list_new(rel_keal_str);
        KealStr* _t287 = keal_str_retain(_str34);
        keal_list_push(_t286, (KealWord){ .p = (KealStr*)keal_str_retain(_t287) });
        KealStr* _t288 = keal_str_retain(_str35);
        keal_list_push(_t286, (KealWord){ .p = (KealStr*)keal_str_retain(_t288) });
        KealStr* _t289 = keal_str_retain(_str55);
        keal_list_push(_t286, (KealWord){ .p = (KealStr*)keal_str_retain(_t289) });
        KealStr* _t290 = keal_str_retain(k_big);
        KealList* _t291 = keal_run_command(_t286, _t290);
        KealList* _t292;
        if (_t291 != NULL) {
            _t292 = keal_list_retain(_t291);
        } else {
            KealList* _t293 = keal_list_new(rel_keal_str);
            _t292 = keal_list_retain(_t293);
            keal_list_release(_t293);
        }
        KEAL_LOCAL KealList* k_deaf = keal_list_retain(_t292);
        KealList* _t294 = keal_list_retain(k_deaf);
        bool _t295;
        if (!(_t294->len == INT64_C(3))) {
            _t295 = false;
        } else {
            KealList* _t296 = keal_list_retain(k_deaf);
            const KealWord _t297 = keal_list_get(_t296, INT64_C(0), KL187);
            KealStr* _t298 = keal_str_retain(((KealStr*)_t297.p));
            KealStr* _t299 = keal_str_retain(_str58);
            const bool _t300 = keal_str_cmp(_t298, _t299) == 0;
            _t295 = ((_t294->len == INT64_C(3)) && _t300);
            keal_str_release(_t299);
            keal_str_release(_t298);
            keal_list_release(_t296);
        }
        KealStr* _t301 = keal_str_retain(_str84);
        if (!(_t295)) { keal_panic(_t301->bytes, NULL, KL188); }
        keal_str_release(_t301);
        keal_list_release(_t294);
        keal_list_release(k_deaf);
        keal_list_release(_t292);
        keal_list_release(_t291);
        keal_str_release(_t290);
        keal_str_release(_t289);
        keal_str_release(_t288);
        keal_str_release(_t287);
        keal_list_release(_t286);
        keal_str_release(_t285);
        keal_str_release(_t283);
        keal_str_release(_t282);
        keal_list_release(_t280);
        keal_str_release(_t279);
        keal_str_release(_t278);
        keal_str_release(_t277);
        keal_list_release(_t275);
        keal_str_release(_t274);
        keal_str_release(_t273);
        keal_list_release(_t271);
        keal_list_release(k_echoed);
        keal_list_release(_t269);
        keal_list_release(_t268);
        keal_str_release(_t267);
        keal_str_release(_t266);
        keal_str_release(_t265);
        keal_str_release(_t264);
        keal_list_release(_t263);
        keal_str_release(k_big);
        keal_str_release(_t257);
        keal_str_release(_t256);
        keal_str_release(_t254);
        keal_str_release(_t253);
        keal_str_release(_t252);
        keal_list_release(_t250);
        keal_list_release(k_none);
        keal_list_release(_t248);
        keal_list_release(_t247);
        keal_str_release(_t246);
        keal_str_release(_t245);
        keal_str_release(_t244);
        keal_str_release(_t243);
        keal_list_release(_t242);
        keal_str_release(_t241);
        keal_str_release(_t240);
        keal_str_release(_t239);
        keal_list_release(_t237);
        keal_str_release(_t235);
        keal_str_release(_t234);
        keal_str_release(_t233);
        keal_list_release(_t231);
        keal_list_release(k_counted);
        keal_list_release(_t229);
        keal_list_release(_t228);
        keal_str_release(_t227);
        keal_str_release(_t226);
        keal_str_release(_t225);
        keal_str_release(_t224);
        keal_list_release(_t223);
        keal_str_release(_t222);
        keal_str_release(_t221);
        keal_list_release(_t219);
        keal_str_release(_t217);
        keal_str_release(_t216);
        keal_list_release(_t214);
        keal_list_release(k_fed);
        keal_list_release(_t212);
        keal_list_release(_t211);
        keal_str_release(_t210);
        keal_str_release(_t209);
        keal_str_release(_t208);
        keal_str_release(_t207);
        keal_list_release(_t206);
    }
    keal_str_release(_t203);
    keal_str_release(_t202);
    keal_str_release(_t201);
    keal_list_release(_t194);
    keal_list_release(_t192);
    keal_list_release(_t191);
    keal_str_release(_t190);
    keal_str_release(_t189);
    keal_list_release(_t188);
    keal_str_release(_t183);
    keal_str_release(_t182);
    keal_str_release(_t164);
    keal_str_release(_t163);
    keal_list_release(_t162);
    keal_str_release(_t161);
    keal_str_release(_t160);
    keal_str_release(_t159);
    keal_str_release(_t158);
    keal_str_release(_t157);
    keal_list_release(_t156);
    keal_str_release(_t130);
    keal_str_release(_t125);
    keal_str_release(_t124);
    keal_str_release(_t119);
    keal_str_release(_t118);
    keal_str_release(_t39);
    keal_str_release(_t38);
    keal_list_release(_t37);
    keal_str_release(_t36);
    keal_list_release(_t35);
    keal_str_release(_t34);
    keal_list_release(_t33);
    keal_list_release(_t32);
    return 0;
}

int main(int argc, char** argv) {
    keal_argc = argc > 1 ? argc - 1 : 0;
    keal_argv = argv + 1;
    keal_runtime_init();
    return keal_program_run();
}
